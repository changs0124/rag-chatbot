# 프론트엔드 환경변수 예시 - 실제 값은 .env.local 에 두고 커밋하지 않음
#
# 백엔드 API 베이스 URL (스킴 포함, 끝에 슬래시 없이)
# - 로컬 : http://localhost:8080
# - 배포 : 백엔드 공개 주소. **Vercel 프로젝트 환경변수에도 같은 이름으로 넣어야 함** -
#          빠뜨리면 프로덕션 번들이 localhost 를 호출해 브라우저에서 전부 실패함
#          (빌드가 이 경우를 경고로 알림 - src/lib/api.ts)
VITE_API_BASE_URL=http://localhost:8080
