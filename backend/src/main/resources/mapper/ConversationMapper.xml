<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ragchatbot.mapper.ConversationMapper">

	<resultMap id="conversationResult" type="com.ragchatbot.domain.Conversation">
		<constructor>
			<idArg column="id" javaType="java.util.UUID"/>
			<arg column="user_id" javaType="java.util.UUID"/>
			<arg column="title" javaType="java.lang.String"/>
			<arg column="vector_store_id" javaType="java.lang.String"/>
			<arg column="created_at" javaType="java.time.OffsetDateTime"/>
			<arg column="updated_at" javaType="java.time.OffsetDateTime"/>
		</constructor>
	</resultMap>

	<insert id="insert" parameterType="com.ragchatbot.domain.Conversation">
		insert into conversations (id, user_id, title, vector_store_id)
		values (#{id}, #{userId}, #{title}, #{vectorStoreId})
	</insert>

	<select id="findByIdAndUser" resultMap="conversationResult">
		select id, user_id, title, vector_store_id, created_at, updated_at
		from conversations
		where id = #{id} and user_id = #{userId}
	</select>

	<select id="listByUser" resultMap="conversationResult">
		select id, user_id, title, vector_store_id, created_at, updated_at
		from conversations
		where user_id = #{userId}
		order by updated_at desc
	</select>

	<delete id="deleteByIdAndUser">
		delete from conversations where id = #{id} and user_id = #{userId}
	</delete>

</mapper>
