<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ragchatbot.mapper.UserMapper">

	<!-- record 매핑은 생성자 기반. uuid/시각 컬럼은 등록된 TypeHandler가 처리 -->
	<resultMap id="userResult" type="com.ragchatbot.domain.User">
		<constructor>
			<idArg column="id" javaType="java.util.UUID"/>
			<arg column="email" javaType="java.lang.String"/>
			<arg column="password_hash" javaType="java.lang.String"/>
			<arg column="name" javaType="java.lang.String"/>
			<arg column="theme" javaType="java.lang.String"/>
			<arg column="created_at" javaType="java.time.OffsetDateTime"/>
			<arg column="updated_at" javaType="java.time.OffsetDateTime"/>
		</constructor>
	</resultMap>

	<insert id="insert" parameterType="com.ragchatbot.domain.User">
		insert into users (id, email, password_hash, name, theme)
		values (#{id}, #{email}, #{passwordHash}, #{name}, #{theme})
	</insert>

	<select id="findById" resultMap="userResult">
		select id, email, password_hash, name, theme, created_at, updated_at
		from users
		where id = #{id}
	</select>

	<select id="findByEmail" resultMap="userResult">
		select id, email, password_hash, name, theme, created_at, updated_at
		from users
		where email = #{email}
	</select>

	<update id="updateName">
		update users set name = #{name} where id = #{id}
	</update>

	<!-- 변경 시각을 함께 올려 이전 발급 토큰을 무효화함(JwtAuthenticationFilter가 대조).
	     시각은 앱이 넘김 - JWT iat 와 같은 시계를 써야 새 토큰이 자기 기준선에 걸리지 않음 -->
	<update id="updatePasswordHash">
		update users set password_hash = #{passwordHash}, password_changed_at = #{changedAt}
		where id = #{id}
	</update>

	<select id="findPasswordChangedAt" resultType="java.time.OffsetDateTime">
		select password_changed_at from users where id = #{id}
	</select>

	<update id="updateTheme">
		update users set theme = #{theme} where id = #{id}
	</update>

</mapper>
