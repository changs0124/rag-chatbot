<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<!-- Spring Boot 4.1.1 - 3.5 는 OSS 지원이 끝나(2026-06-30) 마지막 무상 패치 3.5.16 에 묶여 있었음(#48).
	     4.0 이 아니라 4.1 인 이유 : 4.0 은 2026-12, 4.1 은 2027-07 까지임 -->
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/>
	</parent>

	<groupId>com.ragchatbot</groupId>
	<artifactId>backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>backend</name>
	<description>rag-chatbot backend - OpenAI RAG chatbot (Spring Boot + MyBatis)</description>

	<properties>
		<java.version>17</java.version>
		<!-- postgresql 수동 고정을 뺐음(#48). Boot 3.5.16 BOM 관리분이 42.7.11이라
		     CVE-2026-54291(HIGH, SCRAM-SHA-256-PLUS 다운그레이드)을 피하려고 42.7.13을 박아 뒀는데,
		     4.1.1 BOM 관리분이 정확히 42.7.13임. 고정을 남기면 앞으로 BOM이 더 올려도 우리가
		     낮은 값에 묶어 두게 되어 원래 고정한 이유(보안)와 반대로 작동함. -->
	</properties>

	<dependencies>
		<!-- Phase 0 기반: webmvc + validation. (Boot 4 에서 starter-web → starter-webmvc 로 개명됨) MyBatis/PostgreSQL(Phase 1), Security/JWT(Phase 2)는 해당 Phase에서 추가 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!-- Phase 2 : 인증 (Spring Security + JWT + BCrypt) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>com.auth0</groupId>
			<artifactId>java-jwt</artifactId>
			<version>4.6.0</version>
		</dependency>

		<!-- Phase 1 : 퍼시스턴스 (MyBatis 4.1.0 - SB4.1 트랙, PostgreSQL, Flyway 마이그레이션) -->
		<dependency>
			<groupId>org.mybatis.spring.boot</groupId>
			<artifactId>mybatis-spring-boot-starter</artifactId>
			<version>4.1.0</version>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!-- Boot 4 에서 Flyway 자동설정이 spring-boot-autoconfigure 밖으로 나와 별도 모듈이 됐음(#48).
		     flyway-core 만 두면 라이브러리는 있는데 마이그레이션을 돌려 줄 자동설정이 없어,
		     스키마가 비어 있는 채로 뜨고 "relation \"users\" does not exist" 로 전 케이스가 죽음.
		     starter 가 flyway-core 를 포함하므로 그 선언은 뺐음. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Boot 4 에서 @SpringBootTest 가 TestRestTemplate 을 더 이상 자동 제공하지 않음.
		     전용 모듈을 명시하고 @AutoConfigureTestRestTemplate 을 붙여야 함(#48) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-resttestclient</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- 위 모듈의 자동설정(TestRestTemplateTestAutoConfiguration)이 RestTemplateBuilder 를 참조함.
		     빠지면 ClassNotFoundException 으로 ApplicationContext 자체가 안 떠서 전 케이스가 죽음.
		     본 코드는 RestClient.builder() 를 직접 쓰므로 테스트 클래스패스에만 넣음 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-restclient</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Phase 1 테스트 : Testcontainers(실 PostgreSQL) + MyBatis 테스트 지원 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mybatis.spring.boot</groupId>
			<artifactId>mybatis-spring-boot-starter-test</artifactId>
			<version>4.1.0</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
