# after - 역방향 링크를 넣은 뒤
# 측정 시각: 2026-09-12T10:58:14Z

## 1) 양방향이 됐는가

  backend.md 안의 "current-sprint"                  before 0건 -> after 2건
  current-sprint.md 안의 "02_architecture/backend"   1건 (그대로)

## 2) 추가된 단락과 위치

  위치: 314행 (## 배포 는 309행, 안내 2줄 바로 뒤)

  314: **지금 무엇이 남았는지는 이 문서에 적지 않는다(#177).** 여기는 **절차**의 정본이고,
  315: **남은 준비물 · 대기 중인 이슈 · 키 투입 직후 순서**는
  316: [current-sprint](../04_tasks/current-sprint.md) 「진행 중」의 런칭 항목이 정본이다(#174).
  317: **배포를 시작하기 전에 그쪽을 먼저 읽는다** — 키를 넣는 그 순간에만 값싸게 끝나는 일이 있다.
  318: `#37` 은 `curl` 한 줄에 2초이고, 그때를 놓치면 키를 다시 꺼내야 한다.
  319: 같은 목록을 양쪽에 두지 않는다 — 한쪽만 고치면 갈라지기 때문이다.
  320: 

#   -> 이슈가 제안한 「상시 공개」 끝부분(536행 근처)이 아니라 ## 배포 맨 위 안내 직후다.
#      근거는 before 증거 2절과 계획 3절에 적었다. 코멘트에도 적는다.

## 3) 내용이 옮겨지지 않았는가 - 완료 조건이다

  문자열                      before   after
  --------------------------  ------   -----
  TUNNEL_TOKEN                    4       4   <- 증가 0
  OPENAI_VECTOR_STORE_ID          2       2   <- 증가 0
  --dry-run                       2       2   <- 증가 0
  ALLOWED_ORIGINS                 3       3   <- 증가 0

#   -> 네 문자열 전부 그대로다. 링크만 넣고 준비물 목록은 옮기지 않았다.
#      #174 가 한 곳으로 모아 둔 것이 다시 갈라지지 않았다.

## 4) 게이트 - 섹션 이름이 이 변경의 핵심 위험이었다

  check-doc-sections.sh 는 **파일 참조가 바로 앞에 붙은** 「섹션」만 검사한다.
  링크 뒤에 「진행 중」을 썼고 current-sprint.md 의 `## 진행 중` 과 맞아야 통과한다.

  섹션 참조 검사 통과
  참조 수집 261건 · 실재 검사 249건

  섹션 참조 대조 건수가 27건 -> 29건으로 늘었다 = 새 참조가 실제로 검사 대상에 들어갔다.
  (단순히 통과했다가 아니라, 검사 대상이 늘고 통과했다는 것이 근거다)

  전체 문서 게이트 : 셸 구문 · 셸 정적 분석 · 문서 참조 실재 · 문서 섹션 이름 대조 - 전부 통과
  건너뜀 0건 (shellcheck 는 로컬에 바이너리가 없어 Docker 이미지를 PATH 에 얹어 실제 실행)
