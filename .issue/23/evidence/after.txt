# after — 변경 후 절 단위 전수 대조

절차 규칙
  1. grep 1:1 대응이 없는 외연 열거('만/뿐')를 쓰지 않는다. 내포 판별('거쳤는가')로 쓴다.
  2. **명제의 주어가 바뀌면**(서버 -> 브라우저, 핸들러 -> 사용자) 그 계층의 판별 기준을
     새로 세우거나, 그 문장을 쓰지 않는다. 4차가 이 축에서 뚫렸다.
  3. 역방향 검사는 새 명제에도 돌리고, 발견한 사실을 다른 문장에 전파한다.
  4. 뒷받침하는 명제가 없는 표는 싣지 않는다.

================================================================
문서> - **오류 상태 코드를 컨트롤러가 만들지 않는다.** ... **서블릿 필터 단계에서 나가는 오류는
문서>   이 경로를 거치지 않는다** — 미인증 401 · CORS 403. 어느 쪽도 `ApiError` 가 아니다.
검증> 401:
      47:						(req, res, ex) -> res.sendError(401, "Unauthorized")))
검증> 403 — cors() 가 CorsFilter 를 체인에 넣고 허용 목록은 출처·메서드·헤더 셋:
      34:				.cors(cors -> {
      25:		config.setAllowedOrigins(allowedOrigins);
      26:		config.setAllowedMethods(List.of("GET", "POST", "PATCH", "DELETE", "OPTIONS"));
      27:		config.setAllowedHeaders(List.of("Authorization", "Content-Type"));
검증> 이 범주에 빠진 것이 없는가 — 필터가 예외를 밖으로 던지는가:
      67:			} catch (Exception ex) {
      -> JwtAuthenticationFilter 는 모든 예외를 삼킨다. @PreAuthorize · csrf 0건.

      ※ 4차 판본의 '프론트는 두 경우 요청 실패 (n) 로 떨어진다' 는 삭제했다.
        api.ts 의 if (!res.ok) 는 Response 를 전제하는데, CORS 거절은 브라우저가
        fetch 를 reject 해 그 줄에 도달하지 않는다. 같은 문서 92행이 이미
        '브라우저가 값을 숨겨 조용히 아무 일도 일어나지 않는다' 고 적고 있었다.
        백엔드 문서가 검증할 수단이 없는 계층을 주장한 자리다.

================================================================
문서> - **성공 상태는 컨트롤러가 정한다** — @ResponseStatus(CREATED)(201) · noContent()(204) ×3
문서>   · ResponseEntity.ok()(200+Content-Type). 그 밖의 핸들러는 반환값을 그대로 돌려주고
문서>   상태를 지정하지 않는다. 채팅만 SseEmitter 다.
검증> 상태를 지정하는 자리 — 다섯 종류 각각 grep (합이 전수여야 한다):
      backend/src/main/java/com/ragchatbot/controller/AdminController.java:54:	@ResponseStatus(HttpStatus.CREATED)
      backend/src/main/java/com/ragchatbot/controller/AdminController.java:65:		return ResponseEntity.noContent().build();
      backend/src/main/java/com/ragchatbot/controller/ConversationController.java:59:		return ResponseEntity.noContent().build();
      backend/src/main/java/com/ragchatbot/controller/FileController.java:42:		return ResponseEntity.noContent().build();
      backend/src/main/java/com/ragchatbot/controller/FileController.java:53:		return ResponseEntity.ok()
      .status( · .created( · .accepted( = 0건
      backend/src/main/java/com/ragchatbot/controller/ChatController.java:45:	public SseEmitter chat(@RequestBody ChatRequest req) {
      ※ '그 밖은 DTO 를' 이라 썼다가 고쳤다 — HealthController 는 Map<String,String> 이다.

================================================================
문서> - **`GlobalExceptionHandler` 를 거친 오류 응답 본문은 `ApiError(code, message)` 다.**
검증> 핸들러 8개 전부 ResponseEntity<ApiError> 반환
검증> 클래스 밖 @ExceptionHandler · @ControllerAdvice:
      1건

================================================================
## 역방향 검사 (명령과 결과를 함께 남긴다)
  $ grep -rn '만 예외|뿐 예외' docs/
  $ grep -rn '502' docs/
  $ grep -rn '연결 해제를 시도' docs/
  $ grep -rn '요청 실패' docs/    (브라우저 계층 주장이 남았는가)
      docs/01_specs/api.md:69:**서버가 `message` 를 주면 프론트는 그대로 보여준다**(없으면 `요청 실패 (n)`). 따라서 서버 메시지는 사용자가 읽을 수 있는
      docs/01_specs/features.md:375:| 프론트 | `data?.message` 를 찾다 실패해 `요청 실패 (500)` 만 띄운다 |
      docs/02_architecture/backend.md:64:`message` 를 못 찾아 "요청 실패 (500)" 만 띄웠다.
      docs/02_architecture/frontend.md:51:- **응답이 도착한** 실패는 `ApiError(status, message)` 로 통일하고, 서버가 `message` 를 주면 그대로 보여준다 — 없으면 `요청 실패 (n)` 로 떨어진다. 네트워크 단계 실패(CORS 거절·오프라인·중단)는 `fetch` 가 reject 되어 이 경로를 타지 않는다.
      docs/06_changelog/CHANGELOG.md:172:- **예외 최종 폴백 + 요청 상관 ID (FEAT-OPS-002)** — `GlobalExceptionHandler` 에 `Exception` 핸들러가 없어, 예상 못 한 예외만 Spring 기본 형태(`timestamp`·`status`·`error`·`path`)로 나가 **이 경로에서만 응답 계약이 깨졌음**. 프론트는 `message` 를 못 찾아 "요청 실패 (500)" 만 띄웠고 서버 로그에도 어느 요청이었는지 상관 지을 기록이 없었음
      docs/CONVENTIONS.md:73:- **응답이 도착한** 실패는 `ApiError(status, message)`로 통일. 서버가 `message`를 주면 그대로 보여주고, 없으면 `요청 실패 (n)`로 떨어진다. 네트워크 단계 실패(CORS 거절·오프라인·중단)는 응답이 없어 이 경로를 타지 않는다.
      -> frontend.md · CONVENTIONS.md 는 '응답이 도착한 실패는' 으로 범위를 좁혔고
         네트워크 단계 실패가 이 경로를 안 탄다는 사실을 함께 적었다.
         api.md 는 '서버가 message 를 주면' 조건이 이미 붙어 있다.

================================================================
## 별건으로 분리한 것
  backend.md 「예외 -> 상태 매핑」의 '400·404 가 500 으로 뭉개지지 않는다' 는 Spring 내장
  MVC 예외에서 거짓일 가능성이 높다 — GlobalExceptionHandler 가
  ResponseEntityExceptionHandler 를 상속하지 않고 @ExceptionHandler(Exception.class) 를
  두고 있어 전용 핸들러가 없는 400·404·405 가 폴백으로 떨어진다.
      31:public class GlobalExceptionHandler {
  원인이 이 이슈와 다르고(문서 서술 vs 예외 해석 순서), 고치려면 문서를 맞출지 코드를
  맞출지 설계 결정이 필요하며 후자는 코드 변경이다. 실측 후 정할 일이라 backlog 에 등재했다.

## check-doc-refs
  참조 수집 182건 · 실재 검사 180건
  문서 참조 검사 통과

================================================================
## 5차 추가 — 같은 절의 이웃 줄

문서> - API 호출은 lib/api.ts 의 api.get/... 을 통한다. 예외는 둘 — 채팅 스트림(아래)과
문서>   첨부 저장(ImageLightbox.download() 이 blob 으로 받아야 해서 fetch 를 직접 쓴다).
검증> 저장소 전체의 fetch( 전수 = 3곳. 새 문장의 래퍼 + 예외 둘과 1:1 대응:
      frontend/src/lib/api.ts:91:  const res = await fetch(BASE + path, {
      frontend/src/lib/endpoints.ts:82:  const res = await fetch(API_BASE + '/api/chat', {
      frontend/src/components/chat/ImageLightbox.tsx:109:      const res = await fetch(current.src)

      ※ 직전 판본은 '컴포넌트가 fetch 를 직접 부르지 않는다' 였고 거짓이었다.
        ImageLightbox.tsx:109 가 API_BASE + a.url(API-GET-006)을 직접 부른다.
        내가 고친 두 줄의 바로 두 줄 위, 같은 절이다 — '건드린 줄이 속한 절 전체'
        규칙을 세워 놓고 이웃 줄을 스윕하지 않았다.

## check-doc-refs
  참조 수집 182건 · 실재 검사 180건
  문서 참조 검사 통과
