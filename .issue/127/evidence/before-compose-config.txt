# before — docker compose config (현재 main 기준)
생성: 2026-09-10T05:19:57Z

name: "127"
services:
  app:
    build:
      context: D:\dev\rag-chatbot\.issue\worktrees\127\backend
      dockerfile: Dockerfile
    depends_on:
      db:
        condition: service_healthy
        required: true
    environment:
      ALLOWED_ORIGINS: http://localhost:5173
      APP_MODE: mock
      DB_PASSWORD: dummy
      DB_URL: jdbc:postgresql://db:5432/ragchatbot
      DB_USERNAME: postgres
      FILE_STORAGE_ROOT: /data/uploads
      JWT_SECRET: dummy-secret-for-compose-config-validation-only-32b
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O - http://localhost:8080/api/health || exit 1
      timeout: 5s
      interval: 30s
      retries: 3
      start_period: 1m30s
    logging:
      driver: json-file
      options:
        max-file: "3"
        max-size: 10m
    networks:
      default: null
    ports:
      - mode: ingress
        host_ip: 127.0.0.1
        target: 8080
        published: "8080"
        protocol: tcp
    restart: unless-stopped
    volumes:
      - type: volume
        source: uploads
        target: /data/uploads
        volume: {}
  cloudflared:
    command:
      - tunnel
      - --no-autoupdate
      - run
    depends_on:
      app:
        condition: service_started
        required: true
    environment:
      TUNNEL_TOKEN: dummy
    image: cloudflare/cloudflared:latest
    networks:
      default: null
    restart: unless-stopped
  db:
    environment:
      POSTGRES_DB: ragchatbot
      POSTGRES_PASSWORD: dummy
      POSTGRES_USER: postgres
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U postgres -d ragchatbot
      timeout: 5s
      interval: 5s
      retries: 10
    image: postgres:16-alpine
    networks:
      default: null
    restart: unless-stopped
    volumes:
      - type: volume
        source: pgdata
        target: /var/lib/postgresql/data
        volume: {}
networks:
  default:
    name: 127_default
volumes:
  pgdata:
    name: 127_pgdata
  uploads:
    name: 127_uploads
