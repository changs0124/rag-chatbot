# 변경 전 — 목록형 서술 대조

측정 커밋 : `origin/main` (`9c7c4e9`) · 파일 무수정

## 1. `docs/INDEX.md:18` — CI 잡 목록에 `contract` 가 없다

```text
문서 : backend · frontend · docker · docs · secrets · deps × 2        (7개)
실제 : backend · frontend · docker · contract · docs · secrets · deps × 2  (8개)
                                     ^^^^^^^^
```

```text
$ grep -E "^    name: " .github/workflows/ci.yml
backend (test + build + 케이스 수 하한)
frontend (lint + test + build + 케이스 수 하한)
docker (백엔드 이미지 빌드)
contract (응답 계약 대조)          ← 문서에 없음
docs (참조 실재)
secrets (시크릿 스캔)
deps (백엔드 의존성 취약점)
deps (프론트 의존성 취약점)
```

## 2. `docs/CONVENTIONS.md` 게이트 목록에 `check-doc-versions.sh` 가 없다

「테스트·CI 게이트」 절(89행~)이 열거하는 것 :

```text
scripts/case-floors.env      케이스 수 하한(래칫)
check-doc-refs.sh            참조 실재
check-runtime-versions.sh    런타임 버전 대조
check-response-contract.sh   응답 계약 필드 대조
gitleaks · Trivy · npm audit 시크릿·취약점
                             ← check-doc-versions.sh 가 없음
```

```text
$ ls scripts/*.sh
check-case-floor.sh  check-doc-refs.sh  check-doc-versions.sh  check-response-contract.sh  check-runtime-versions.sh
                                        ^^^^^^^^^^^^^^^^^^^^^
```

**#52 가 자기 자신을 목록에 넣지 않았다.** 게이트를 만들면서 게이트를 나열하는 문서를 갱신하지 않은 것으로, **#48 → #51 과 같은 종류의 누락**이다(그때는 버전 값, 이번엔 구성 목록).

## 어떤 게이트도 이걸 못 잡는다

```text
check-doc-versions.sh (#52)  버전 주장만 봄 → 이건 목록이라 대상 아님
check-doc-refs.sh            경로 실재만 봄 → ci.yml 이 존재하므로 통과
check-runtime-versions.sh    java.version · .nvmrc 만
check-response-contract.sh   DocumentResponse 필드만
check-case-floor.sh          케이스 수만
```

실제로 다섯 게이트가 전부 초록인 상태에서 이 두 문장이 거짓이다.

## 다른 목록형 서술은 없는지 훑었다

```text
INDEX.md:94    "문서에 없는 파일 경로를 적으면 CI의 scripts/check-doc-refs.sh가 실패한다"
               → 목록이 아니라 개별 진술. 여전히 참
INDEX.md 실행절 문서 읽는 순서 안내 — 구성 목록 아님
frontend.md:103 개별 테스트 언급 — 목록 아님
```

**고칠 곳은 위 두 곳뿐이다.**
