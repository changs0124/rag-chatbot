# before — 변경 전 (커밋 ff172e9)

## GlobalExceptionHandler 가 처리하는 예외 8종
```
@ExceptionHandler(MethodArgumentNotValidException.class)
@ExceptionHandler(ConflictException.class)
@ExceptionHandler(UnauthorizedException.class)
@ExceptionHandler(NotFoundException.class)
@ExceptionHandler(BadRequestException.class)
@ExceptionHandler(MaxUploadSizeExceededException.class)
@ExceptionHandler(RateLimitException.class)
@ExceptionHandler(Exception.class)
```

HttpMessageNotReadableException · MethodArgumentTypeMismatchException ·
MissingServletRequestPartException 가 없다 → @ExceptionHandler(Exception.class) 폴백으로 떨어진다.

## 실기동 실측 (mock, 2026-09-07)
```
$ curl -X POST /api/chat -H "Content-Type: application/json" -d '{"conversationId": BROKEN}'
{"code":"INTERNAL_ERROR","message":"처리 중 오류가 발생했습니다 (오류 번호: d01cb772)"}  HTTP 500

# 서버 로그
ERROR c.r.exception.GlobalExceptionHandler : 처리되지 않은 예외 [4c02b1c7] POST /api/chat
org.springframework.http.converter.HttpMessageNotReadableException: JSON parse error: Invalid UTF-8 start byte 0xbf
```

## 명세
```
docs/01_specs/api.md:74   | `BAD_REQUEST` | 400 | 검증 실패 · 파일 형식/크기 |
docs/01_specs/api.md:89   | 400 | 잘못된 요청 | |
docs/01_specs/api.md:76   | `INTERNAL_ERROR` | 500 | **예상하지 못한 예외** …
```
깨진 본문은 "예상하지 못한 예외" 가 아니라 잘못된 요청이다.

## 기존 테스트 (TC-OPS-010~013)
```
20: * 예외 최종 폴백 (FEAT-OPS-002 · TC-OPS-010~013).
52:	/** TC-OPS-010 : 예상하지 못한 예외가 ApiError 형태로 나간다 */
55:	void unexpected_exception_returns_api_error_shape() {
64:	/** TC-OPS-011 : 응답에 상관 ID가 실린다 - 이 번호로 서버 로그를 찾는다 */
67:	void response_carries_trace_id() {
79:	 * TC-OPS-012 : 내부 예외 메시지가 응답에 노출되지 않는다.
84:	void internal_message_is_not_exposed() {
93:	 * TC-OPS-013 : 도메인 예외는 폴백에 걸리지 않는다.
100:	void domain_exception_still_maps_to_its_own_status() {
```

## 케이스 수 하한
```
BACKEND_MIN=167
FRONTEND_MIN=76
```
