# before — 변경 전 (커밋 c0e1d65)

## 허용 판정 — 서버는 contentType, 화면은 확장자
```java
// AdminDocumentService.java:45-52
	/** PDF 매직바이트. txt·md 는 매직바이트가 없고 docx 는 zip 이라 `PK` 만으로는 다른 zip 과 구분되지 않는다 */
	private static final byte[] PDF_MAGIC = { 0x25, 0x50, 0x44, 0x46 }; // %PDF

	private static final Map<String, String> ALLOWED = Map.of(
			"application/pdf", "pdf",
			"text/plain", "txt",
			"text/markdown", "md",
			"application/vnd.openxmlformats-officedocument.wordprocessingml.document", "docx");
```
```ts
// AdminPage.tsx:33
33:const ACCEPT = '.pdf,.txt,.md,.docx'
178:              accept={ACCEPT}
```

## 실기동 실측 (mock, 2026-09-07)
```
$ curl -X POST /api/admin/documents -F "file=@note.md;type=application/octet-stream"
{"code":"BAD_REQUEST","message":"지원하지 않는 문서 형식: application/octet-stream (허용: PDF · TXT · MD · DOCX)"}
HTTP 400

# 같은 파일을 text/plain 으로 보내면
$ curl -X POST /api/admin/documents -F "file=@policy.txt;type=text/plain"
{"id":"6ed3d2f0-...","filename":"policy.txt","byteSize":181,"status":"in_progress",...}  HTTP 201
```

## 매직바이트로는 풀 수 없다 — 코드가 이미 인정
```java
// AdminDocumentService.java:45
	/** PDF 매직바이트. txt·md 는 매직바이트가 없고 docx 는 zip 이라 `PK` 만으로는 다른 zip 과 구분되지 않는다 */
```

## 기존 업로드 테스트에 octet-stream 케이스가 없다
```
$ grep -c "octet-stream" backend/src/test/java/com/ragchatbot/controller/AdminDocumentFlowTest.java
0
```
