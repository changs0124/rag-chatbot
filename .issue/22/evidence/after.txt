# after — 변경 후

## 교체된 문장 (backend.md)
- **이 예외는 조회에만 적용된다.** 쓰기 경로는 `dto` 를 매핑하지 않는다. 등록은 `entity` 를 통째로 받고
  (`insert(RagDocument)`), 수정·삭제는 식별자와 바뀔 값만 스칼라 파라미터로 받는다
  (`updateStatus(id, status)` · `softDelete(id, deletedAt)`). `entity/` 의 record 는 값을 담기만 하고
  규칙은 `service/` 에 있으므로, 수정·삭제까지 굳이 entity 를 거칠 이유가 없다.

## 새 문장이 코드에서 참인가 — 항목별 대조

주장 1) 등록은 entity 를 통째로 받는다
  	void insert(Attachment attachment);
  	void insert(Citation citation);
  	void insert(Conversation conversation);
  	void insert(Message message);
  	void insert(RagDocument document);
  	void insert(User user);

주장 2) 수정·삭제는 식별자와 바뀔 값만 스칼라로 받는다  (entity 를 받는 update/delete 0건)
  	int deleteByIdAndUser(@Param("id") UUID id, @Param("userId") UUID userId);
  	int deleteById(@Param("id") UUID id);
  	int deleteByIdAndUser(@Param("id") UUID id, @Param("userId") UUID userId);
  	int updateTitle(@Param("id") UUID id, @Param("userId") UUID userId, @Param("title") String title);
  	int updateStatus(@Param("id") UUID id, @Param("status") String status);
  	int softDelete(@Param("id") UUID id, @Param("deletedAt") OffsetDateTime deletedAt);
  	void updateName(@Param("id") UUID id, @Param("name") String name);
  	void updatePasswordHash(@Param("id") UUID id, @Param("passwordHash") String passwordHash,
  	void updateTheme(@Param("id") UUID id, @Param("theme") String theme);
  	int demoteAdminsNotIn(@Param("emails") List<String> emails);
  	int promoteAdmins(@Param("emails") List<String> emails);

주장 3) entity record 는 값만 담고 규칙은 service 에 있다
  Attachment.java      메서드 0개
  Citation.java        메서드 0개
  Conversation.java    메서드 0개
  Message.java         메서드 0개
  RagDocument.java     메서드 0개
  User.java            메서드 0개

## CHANGELOG [Unreleased] 에 추가됨
8:- **`RagDocumentSummary` 를 없애고 문서 목록 조회를 Response DTO 로 직접 매핑(#21).** 이 타입은 자기
21:- **백엔드 패키지 이름을 계층형 표준 어휘로 통일(#19, 71파일).** `web/` → `controller/` ·

## check-doc-refs
  참조 수집 181건 · 실재 검사 179건 / 문서 참조 검사 통과

## 변경 규모
 docs/02_architecture/backend.md |  6 ++++--
 docs/06_changelog/CHANGELOG.md  | 26 ++++++++++++++++++++++++++
 2 files changed, 30 insertions(+), 2 deletions(-)
