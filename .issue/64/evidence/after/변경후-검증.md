# 변경 후 — deprecation 0건

측정 커밋 : `de1cd07`

```text
$ ./mvnw clean test-compile -Dmaven.compiler.showDeprecation=true
deprecation : 0건

$ ./mvnw clean verify
[INFO] Tests run: 184, Failures: 0, Errors: 0, Skipped: 0   BUILD SUCCESS
```

`asText` 9건(#63) + 이번 4건 = **13건이 전부 사라졌다.**

## 바꾼 것 — 3파일 8줄

| 파일 | 변경 |
|------|------|
| `LocalFileStorage.java:44` | `PathResource` → `FileSystemResource` (import 포함 2줄) |
| `GlobalExceptionHandler.java:76` | `PAYLOAD_TOO_LARGE` → `CONTENT_TOO_LARGE` + 왜 코드 문자열은 안 바꾸는지 주석 |
| `AbstractPgIntegrationTest.java:47` | 패키지 이동 + **제네릭 파라미터 제거** |

## Testcontainers 2 는 제네릭이 아니다

패키지만 옮겼더니 컴파일이 깨졌다.

```text
[ERROR] type org.testcontainers.postgresql.PostgreSQLContainer does not take parameters
```

자기 타입 파라미터(`PostgreSQLContainer<SELF>`)가 사라져 `<?>` 와 `<>` 를 함께 걷어야 했다. **이 클래스는 통합 테스트 18개의 뿌리라 여기가 깨지면 184건이 전부 죽는데, 그대로 통과한다.**

## 응답 코드 문자열은 건드리지 않았다

```java
return ResponseEntity.status(HttpStatus.CONTENT_TOO_LARGE)
        .body(new ApiError("PAYLOAD_TOO_LARGE", "업로드 용량 한도 초과"));
```

상수 이름만 바뀌었고 `code` 는 `api.md:79` 의 계약이라 그대로다. **왜 그런지 코드 옆에 적었다** — 다음 사람이 "일관성" 을 이유로 함께 바꾸지 않도록.

## 413 테스트를 넣으려다 다른 것을 발견했다

완료 기준이 "**응답이 여전히 413 인지 테스트로 확인**" 이었다. 먼저 확인한 것은 **413 경로를 잠그는 테스트가 하나도 없다**는 사실이다(`TC-ADMIN-025` 는 그 반대인 400 을 단언한다).

그래서 26MB+1KB 업로드 테스트를 넣어 봤더니 이렇게 됐다.

```text
org.springframework.web.client.ResourceAccessException: I/O error on POST …
Caused by: java.io.IOException: chunked transfer encoding, state: READING_LENGTH
```

**Tomcat 이 한도 초과 시 남은 본문을 삼키지 않고 연결을 끊는다.** `server.tomcat.max-swallow-size` 가 설정돼 있지 않아 기본 2MB 이고, 그보다 많이 넘긴 요청은 **클라이언트가 413 을 읽지 못한다.**

즉 `api.md:79` 가 약속한 413 이 **현실적인 초과량에서는 관측되지 않는다.**

### 그래서 테스트를 뺐다

- 이건 **#64 와 원인이 다르다** — 상수 교체가 만든 문제가 아니라 원래 그랬다
- 고치려면 `max-swallow-size` 를 올려야 하는데, **큰 본문을 끝까지 읽는 비용**이 드는 런타임 동작 변경이다. deprecation 정리에 섞을 판단이 아니다
- `backlog.md` 에 근거와 고를 것(설정을 올릴지 / 관측되지 않음을 `api.md` 에 적을지)을 함께 남겼다

**상수 교체 자체의 안전성은 실측으로 확인했다** — `CONTENT_TOO_LARGE.value() == 413`.

## 게이트

```text
check-case-floor.sh (backend)  OK  184/184
check-response-contract.sh     OK
check-doc-refs.sh              OK
check-doc-sections.sh          OK
check-doc-versions.sh          OK
check-runtime-versions.sh      OK
```
