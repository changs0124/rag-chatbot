# before — 변경 전 (커밋 96a8999)

## 상한을 말하는 위치 8곳 — 숫자가 셋
```
backend/src/main/java/com/ragchatbot/service/AdminDocumentService.java:43:	private static final long MAX_SIZE = 50 * MB;
backend/src/main/resources/application.yml:15:      # 타입별 상한(이미지 10MB/문서 20MB)은 코드가 400으로 먼저 검사. 이 하드 한도 초과는 413
backend/src/main/resources/application.yml:16:      max-file-size: 25MB
backend/src/main/resources/application.yml:17:      max-request-size: 30MB
frontend/src/pages/AdminPage.tsx:174:            <span className="text-xs text-ink-muted">PDF · TXT · MD · DOCX · 최대 50MB</span>
docs/01_specs/api.md:273:| 최대 크기 | 50MB |
docs/01_specs/features.md:238:| file | File | Y | PDF · TXT · MD · DOCX · 최대 50MB · PDF는 매직바이트(`%PDF`) 검사 |
docs/01_specs/features.md:243:**50MB로 잡은 이유** : OpenAI 파일 상한(512MB)보다 훨씬 낮게 둔다. 상한에 맞추면 요청 하나가
docs/01_specs/live-integration.md:220:| 업로드 | `POST /api/admin/documents` (multipart, 필드명 `file`). PDF · TXT · MD · DOCX, 최대 50MB |
docs/02_architecture/overview.md:151:| 업로드 하드 한도 | 해당 없음 — yml 고정값 | 파일 25MB · 요청 30MB (타입별 상한은 코드가 400으로 선검사) |
```

## 실측 경계 (mock 실기동, 2026-09-07)
```
20MB          → 201
26,214,400 B  → 201   (= 정확히 25MiB)
26,215,424 B  → 413   {"code":"PAYLOAD_TOO_LARGE","message":"업로드 용량 한도 초과"}
51MB          → 413
```
경계는 26,214,400 바이트. application.yml 의 max-file-size 가 이긴다.

## 도달 불가능한 분기
```java
// AdminDocumentService.java:43
43:	private static final long MAX_SIZE = 50 * MB;
// :103-105 — 25~50MB 구간에서 절대 실행되지 않는다
141:			throw new BadRequestException("용량 초과(최대 " + (MAX_SIZE / MB) + "MB)");
```

## 이미지 경로는 정상 — 2단 설계가 도는 유일한 곳
```
image 상한 10MB < 멀티파트 25MB  →  서비스 검사가 먼저 걸려 400
$ POST /api/files -F "file=@img.jpg;type=image/jpeg"   # 11MB
{"code":"BAD_REQUEST","message":"용량 초과(image 최대 10MB)"}  HTTP 400
```
