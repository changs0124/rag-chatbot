관련 이슈: [#172 docs(ci): CONVENTIONS.md 의 필수 체크 목록이 축약형이라 그대로 옮기면 영구 pending 이 된다](https://github.com/changs0124/rag-chatbot/issues/172) (통합 테스트 뒤 close)

`CONVENTIONS.md` 가 필수 체크 7개를 `backend` · `frontend` · … 로 축약해 적었는데 실제 ruleset
context 는 `ci.yml` 의 잡 `name:` 전체 문자열이다. 거짓은 아니지만(어느 잡인지는 맞다)
**문서만 보고 ruleset 을 복원하면 일곱 체크가 전부 영구 pending 이 되어 merge 가 완전히 막힌다.**
문서가 그 실패 모드를 바로 옆에서 경고하면서 정작 목록은 그 함정에 걸리는 형태였다.

## 변경 내용

- `docs/CONVENTIONS.md` — 축약 목록 **바로 뒤**(`:166`)에 단락 추가
  - 위 목록은 축약이고 실제 context 는 `ci.yml` 의 잡 `name:` 전체 문자열이라는 사실
  - 여기에 옮겨 적지 않는 이유 — 사본이 셋으로 늘고 지킬 게이트가 없다
  - 복원해야 하면 정본에서 뽑는다 : `gh api …/rules/branches/main --jq …`
  - 규칙조차 못 읽으면 `ci.yml` 의 `name:` 을 쓴다
- `docs/06_changelog/CHANGELOG.md` — `Added`

실행 코드는 한 줄도 바뀌지 않는다. **ruleset 설정도 건드리지 않았다** — 집행은 원래 정확했다.

## 전체 문자열로 바꾸지 않은 이유

| | 1안 (전체 문자열) | **2안 (채택)** |
| --- | --- | --- |
| 옮겨 적기 | 안전 | 정본을 봐야 한다 |
| 잡 이름이 사는 곳 | `ci.yml` · ruleset · **문서 = 셋** | `ci.yml` · ruleset = **둘 그대로** |
| 늘어난 사본을 지키는 게이트 | **없음** | 해당 없음 |

`CONVENTIONS.md:147` 이 밝히듯 `check-doc-versions.sh` 는 「버전이 아닌 **목록형 서술**(CI 잡
목록·이 게이트 목록 자체가 그렇다)」을 **일부러 제외**한다. 1안이 들이는 사본은 어느 게이트도
지키지 않는다. 그리고 #105 항목 5 가 허용 목록을 놓고 같은 거래를 이미 거절했다 —
*"고치면 허용 목록 사본이 네 번째로 늘어 사슬이 길어진다"*.

## 검증

**적어 둔 명령을 문서에서 그대로 추출해 돌렸다.** #170 에서 이 검증이 한 줄 140자 버그를 잡았다.

| 항목 | 결과 |
| --- | --- |
| 정본 명령 실행 | context 7개 그대로 출력 |
| 배치 | `:164`(축약 목록) → **`:166`(축약 명시)** → 기존 서술 |
| before 0건이던 문자열 | `축약` · `정본` · `rules/branches/main` 각 1건 |
| 문서 게이트 | 전부 통과 · 건너뜀 0건 (`shellcheck` 을 Docker 로 실제 실행) |

### 사본 수는 0 이 아니라 1 이다

`docs/` 안의 전체 문자열이 **before 0건 → after 1건**으로 **늘었다.** 숨기지 않고 적는다.
늘어난 것은 설명용 예시 한 번(「예 : `backend` 가 아니라 `backend (test + build + …)`」)이다.

2안이 피하려던 것은 **일곱 개를 목록으로 옮겨 적는 것**이고 그 목록은 만들지 않았다. 사본이
위험한 이유는 「규칙이 바뀌면 같이 고쳐야 하는데 지키는 게이트가 없다」인데, 일곱 중 하나뿐인
예시는 **옮겨 적기의 원본으로 쓰일 수 없어** 그 위험을 만들지 않는다.
그래도 0 은 아니다 — 1안을 골랐으면 7건이었을 것이라는 점에서 차이가 의미 있다.

## 발견 경로

2026-09-12 세션 마무리의 **병렬 검증 네 축**(문서↔실제 · 설정 실측 · 기록 정합성 · 하드닝 재현)에서
「문서↔실제」축이 이것 하나를 잡았다. 나머지 세 축은 전부 통과했다. 저장소의 문서 게이트 셋은
참조 실재·섹션 이름·버전 문자열만 보고 **「문서의 주장이 사실인가」는 보지 않아** 걸리지 않았다.

## 증거

[전후 리포트 보기](https://github.com/changs0124/rag-chatbot/issues/172#issuecomment-5645041496)

캡처는 없다 — 문서만 바뀌는 변경이라 앱 화면이 없다. 대신 명령 실행 출력과 사본 수 계수를 남겼다.
원본은 `.issue/172/evidence/` (before 1건 / after 1건).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_018bBUzct65eWEaZqTgLsri7
