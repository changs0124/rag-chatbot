# before — 변경 전 (커밋 a700f50)

## 요청 바디에 instructions 도 tool_choice 도 없다
```java
// OpenAiRealService.java:108-119
		Map<String, Object> body = new HashMap<>();
		body.put("model", model);
		body.put("input", buildInput(input));
		body.put("stream", true);
		// 공용 Store 미설정이면 file_search 없이 일반 응답(출처 빈 리스트 → 무자료 접두)
		if (storeId != null && !storeId.isBlank()) {
			body.put("tools", List.of(Map.of(
					"type", "file_search",
					"vector_store_ids", List.of(storeId))));
			// 스니펫 노출을 위해 검색 결과 본문까지 받음
			body.put("include", List.of("file_search_call.results"));
		}
```

```
$ grep -rn "instructions|tool_choice|max_output_tokens" src/main/java/com/ragchatbot/openai/ src/main/java/com/ragchatbot/service/ChatService.java
(결과 없음)
```

## 그래서 검색 호출이 모델 재량이다
tool_choice 기본값은 auto. 모델이 file_search 를 부르지 않으면 annotation 이 0건이고,
noSource = citations.isEmpty() 가 true 가 되어 화면에 「자료 없음」이 뜬다.

## 같은 증상에 원인이 넷 — 화면만으로는 갈라지지 않는다
```
원인                          화면
OPENAI_VECTOR_STORE_ID 미주입  「자료 없음」
vs_ ID 오타                    「자료 없음」(채팅은 오류)
인용 추출 실패(가정 C)          「자료 없음」
모델이 검색을 건너뜀 (이 이슈)   「자료 없음」
```

## 기존 실서비스 테스트 방식
OpenAiRealDeleteResourcesTest 가 com.sun.net.httpserver.HttpServer 로 스텁을 띄우고
app.openai.base-url 을 그쪽으로 돌려 실제 요청을 잡는다. 같은 방식으로 바디를 검증할 수 있다.
