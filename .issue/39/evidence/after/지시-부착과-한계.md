# after — 변경 후 (커밋 34002ef)

## 요청 바디 — 검색 도구가 붙을 때만 instructions 를 싣는다
```java
			body.put("tools", List.of(Map.of(
					"type", "file_search",
					"vector_store_ids", List.of(storeId))));
			// 스니펫 노출을 위해 검색 결과 본문까지 받음
			body.put("include", List.of("file_search_call.results"));
			// 검색 도구가 붙을 때만 지시를 싣는다 - 스토어가 없으면 근거를 찾으라는 말이 거짓이 된다
			body.put("instructions", RAG_INSTRUCTIONS);
		}

```

## 스텁 서버가 실제로 받은 바디로 검증
```
$ ./mvnw -B test -Dtest=OpenAiRealInstructionsTest
[INFO] Tests run: 3, Failures: 0, Errors: 0, Skipped: 0

sends_rag_instructions_when_vector_store_is_attached   스토어 있음 → instructions + file_search 있음
omits_instructions_when_no_vector_store                스토어 없음 → 둘 다 없음
does_not_force_tool_choice                            tool_choice 를 보내지 않음
```

## 전량 회귀
```
$ ./mvnw -B clean verify
[INFO] Tests run: 180, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS

케이스 수(backend) : 실측 180 / 하한 180
OK
참조 수집 199건 · 실재 검사 192건
문서 참조 검사 통과
✓ 응답 계약 4곳 일치 (6필드)
```

## 이 테스트가 잠그지 못하는 것
모델이 지시를 따르는지는 여기서 잴 수 없다. 잠그는 것은 **보내는 쪽**뿐이다.
부착률은 실 연동에서만 측정 가능하며, live-integration.md 3-2 에 재판단 기준을 적었다.

## 변경 요약
```
 .../com/ragchatbot/openai/OpenAiRealService.java   |  27 ++++++
 .../openai/OpenAiRealInstructionsTest.java         | 107 +++++++++++++++++++++
 docs/01_specs/live-integration.md                  |  17 +++-
 scripts/case-floors.env                            |   2 +-
 4 files changed, 151 insertions(+), 2 deletions(-)
```
