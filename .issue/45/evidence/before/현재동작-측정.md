# 변경 전 측정 — 라우팅 단계 MVC 예외 셋

측정 커밋 : `origin/main` (`08f8d7e`) · 프로덕션 코드 무수정
측정 방법 : `GlobalExceptionFallbackTest` 에 케이스 3건을 기대값 4xx 로 넣고 실행 → 실패 출력이 곧 현재 동작
명령 : `cd backend && ./mvnw -Dtest=GlobalExceptionFallbackTest test`

## 결과 — 셋 다 500 이었다

```text
[ERROR] Tests run: 12, Failures: 3, Errors: 0, Skipped: 0

GlobalExceptionFallbackTest.unknown_url_is_not_found:219
expected: 404 NOT_FOUND
 but was: 500 INTERNAL_SERVER_ERROR

GlobalExceptionFallbackTest.unsupported_method_is_method_not_allowed:237
expected: 405 METHOD_NOT_ALLOWED
 but was: 500 INTERNAL_SERVER_ERROR

GlobalExceptionFallbackTest.unsupported_media_type_is_rejected:257
expected: 415 UNSUPPORTED_MEDIA_TYPE
 but was: 500 INTERNAL_SERVER_ERROR
```

**추정이 사실로 확정됐다.** `docs/02_architecture/backend.md` 가 "측정한 적이 없다" 고 적어 둔 셋이 전부 폴백으로 새고 있었다. #38 이 본문 파싱 쪽 넷을 실측했을 때와 같은 결과다.

## 실제로 던져진 예외

로그 스택트레이스에서 확인한 클래스 이름이다. **없는 URL 은 `NoHandlerFoundException` 이 아니라 `NoResourceFoundException` 이었다** — Spring Boot 3.2+ 의 정적 리소스 체인이 던지는 쪽이다. 계획 단계의 미확정 항목이 이것으로 닫혔다.

| 케이스 | 요청 | 예외 클래스 |
|--------|------|-------------|
| 없는 URL | `GET /api/nope` | `org.springframework.web.servlet.resource.NoResourceFoundException` |
| 미지원 메서드 | `DELETE /api/conversations` | `org.springframework.web.HttpRequestMethodNotSupportedException` |
| 미지원 미디어 타입 | `POST /api/chat` + `Content-Type: text/plain` | `org.springframework.web.HttpMediaTypeNotSupportedException` |

셋 다 유효한 토큰으로 쳤다. `SecurityConfig` 가 `anyRequest().authenticated()` 라 미인증이면 401 이 먼저 걸려 판정 자체가 되지 않는다.

## 원인

`ExceptionHandlerExceptionResolver`(`@ControllerAdvice` 처리)가 `DefaultHandlerExceptionResolver`(내장 MVC 예외를 4xx 로 바꿔 주는 기본 처리기)보다 **먼저** 돈다. `GlobalExceptionHandler` 의 `@ExceptionHandler(Exception.class)` 폴백이 그 앞에서 전부 가로채 500 으로 만든다.

## 변경 전 명세 — 두 코드가 표에 없다

`docs/01_specs/api.md` 오류 코드 표 : `BAD_REQUEST` · `UNAUTHORIZED` · `NOT_FOUND` · `CONFLICT` · `PAYLOAD_TOO_LARGE` · `RATE_LIMIT` · `INTERNAL_ERROR` — 7종. `METHOD_NOT_ALLOWED` · `UNSUPPORTED_MEDIA_TYPE` 없음.

`docs/01_specs/api.md` HTTP 상태 코드 표 : 200 · 201 · 204 · 400 · 401 · 403 · 404 · 409 · 413 · 429 · 500 — **405 · 415 없음.**

## 변경 전 래칫

```text
scripts/case-floors.env
BACKEND_MIN=181
```
