# 00_계획 - rag-chatbot

> 볼트 `10_Dev/Architecture/프로젝트 진행 가이드` **v3.9** 기준(착수 시점 v2.4 · 2026-07-28 대조).
>
> **정본은 볼트의 가이드 문서임.** 아래 요약과 가이드가 어긋나면 **가이드가 이김** - 요약은 발췌라 항상 더 느슨하게 읽히므로, 요약을 근거로 기준을 낮추지 않음.
>
> **상태 마커(🔴 🟠 ⚪ 🟡 🟢) 범례와 미결 상태의 정본은 `docs/02_운영.md`임**(S-6 규칙 5). 이 문서는 마커와 항목명만 참조함.

## 실행 규칙 요약 (가이드 S-1~S-3 발췌)

- **S-1 절대 금지** : 모르는 것을 채우지 않음(모호하면 멈추고 물음) · 확인 안 한 숫자를 판정에 쓰지 않음 · 검증 없이 "완료"라 하지 않음(명령 출력/응답/화면 근거 + 종료 코드 확인) · 버전을 기억으로 답하지 않음(공식 문서 + 확인일) · 기준을 낮춰 통과시키지 않음 · 테스트 skip/게이트 완화 금지 · 스텁/목을 완료로 위장 금지(미결 등재) · 시크릿 비커밋 · 작성과 리뷰를 같은 컨텍스트에서 하지 않음
- **S-2 상시 3** : 완료 판정 없는 항목에 착수 안 함 · 새 테스트는 반드시 한 번 깨뜨려 실패 확인 · 문서와 코드는 같은 커밋/PR에서 움직임
- **S-3 멈추고 물을 것** : 기술 스택 최종 채택 · 플랜 승인 · 비용 발생 · 도메인/계정/시크릿 · 대외 공개 · 법적 문구 · 모호한 요구 · 되돌릴 수 없는 파일 조작 · 승인 범위/AC 변경

**문서를 고칠 때는 가이드 S-6을 열어 볼 것**(발췌하지 않음 - 여기 옮겨 적으면 갈라짐). 이 저장소에 걸리는 두 지점만 적음 :

- **상태 마커 범례와 미결 상태의 정본은 `docs/02_운영.md`** - 다른 문서는 마커와 항목명만 참조함
- **값의 정본이 파일이면 문서는 숫자를 쓰지 않음** - 케이스 하한은 `scripts/case-floors.env`가 정본임

## 산출물 파일 지도

| 파일 | 담는 것 | 상태 |
|------|---------|------|
| `docs/00_계획.md` | 규칙 요약 · 개요 · 범위(포함/제외/변경) · 비기능 요구 · 진행 로그 · 게이트 체크 · CI 게이트 / (2단계) 원칙 · 결정 동인 · Phase · AC · 이관 · 실측 · 리스크 | 작성 중 |
| `docs/01_기술선택.md` | 채택 표 · 탈락 표 · 스파이크 표 · 리뷰 라운드 표 · 폴더 구조 표 · 구조 출처 표 | 1 · 1-A · 3 · 7단계 |
| `docs/02_운영.md` | 접근 권한 표 · **미결 표** · 처리 이력 표(가드 등록표 · 깨뜨릴 수 없는 가드 표는 행 0건 - 아래 각주) | 작성 중 |
| `docs/03_PR로그.md` | PR 표 | 작성 중(2026-07-27 생성) |
| `docs/04_런칭.md` | 런칭 표 | 작성 중 - 8단계에서 실물 확인 |
| `docs/05_동일성.md` | **해당 없음 - 유형이 신규라 리뉴얼 장을 건너뜀** | 생성하지 않음 |

- Phase 표 · AC 표 · 판정 이관 표 · 실측 표 · 리스크 표는 **2단계(플랜)에서 채울 행이 생길 때 생성함**(가이드 : 행 0인 표는 만들지 않음)
- **가드 등록표 · 깨뜨릴 수 없는 가드 표(`docs/02_운영.md`)** : **해당 없음 - 행 0건**. 사유 : 미래 시점에 발동하는 임시 조치를 아직 도입한 적이 없고(현행 가드 `AppModeGuard`는 발동 조건이 없는 상시 가드임), 새 테스트가 전부 실제로 깨뜨려 확인됐으므로 "깨뜨릴 수 없는 항목"도 0건임. 임시 조치를 도입하는 PR에서 표를 만듦
- **미결 표는 `docs/02_운영.md`가 원장임** - 2026-07-28 이 문서에서 이관함

---

## 개요

| 항목 | 값 |
|------|-----|
| 프로젝트명 | rag-chatbot |
| 유형 | 신규 |
| 규모 | 정식 (1주~1개월) |
| 한 줄 목적 | OpenAI GPT-4o + Store(VectorDB) 기반으로 출처를 항상 표기하는 RAG 챗봇 |
| 성공의 정의(상태) | 로그인한 사용자가 텍스트/이미지로 질문하면, 출처가 각주로 달린 답변(자료 없으면 **무자료 표시**)이 **스트리밍으로** 표시되고, 그 대화가 저장되어 다시 열람됨. API/VectorDB 미구축 상태에서도 **목업 모드로 이 흐름 전체가 동작함**. 단 R-11 진행 단계의 **체감 구간**은 예외임 - 목업은 첫 토큰까지가 사실상 즉시라 단계가 순간만 스침(라이브 실측은 이관-7) |
| 성공 확인 수단 | 화면(질문→출처 달린 답변 표시) · 응답(API 계약) · 저장 후 재조회 화면 |
| 담당 | changs0124 |
| 착수일 | 2026-07-24 |
| 목표 런칭일 | 미정 (2단계 플랜에서 확정) |
| 참조 구현 | `D:/dev/openai-rag-bot` (강한 참조 원장 - 동일 개념 선행 구현 + 역설계 PLAN v3.0). 유형은 신규 유지, 데이터/인증 계층은 MyBatis+자체로 새로 작성 |

---

## 포함 범위

> 번호 계열 주의 : 이 표의 `R-n`은 **요구**이고, 아래 리스크 표의 `R-n`은 **리스크**로 별개 계열임(요구 R-1~R-11 · 리스크 R-1~R-14로 값까지 전부 겹침). 다른 표에서 리스크를 가리킬 때는 `리스크 R-n`처럼 계열을 앞에 적음.

| 번호 | 항목 | 이 항목이 없으면 무엇이 성립하지 않는가 |
|------|------|------------------------------------------|
| R-1 | RAG 응답 코어 : 질문 → Store 검색 → 출처 포함 답변 | 챗봇의 존재 이유가 없음 |
| R-2 | 입력 : 텍스트 · 카메라 촬영 · 사진(갤러리) 업로드 (**문서 첨부는 2026-07-28 제외** - 범위 변경 이력 참조) | 멀티모달 질문이 불가 |
| R-3 | 첨부 이미지 GPT-4o 비전 전달 + 대화에 저장 | 사진 기반 질문이 처리되지 않고 이력에도 안 남음 |
| R-4 | 출처 표기(각주 번호 + 하단 출처 목록) · **무자료 시 별도 표시**(텍스트 접두가 아니라 출처 0건에서 파생, 2026-07-28 변경. 단 **중단된 답변은 출처 판정을 못 마쳤으므로 표시하지 않음**) | 신뢰성(R 핵심 요구) 미충족 |
| R-5 | 사용자 인증 : 자체 이메일/비밀번호 로그인 | 사용자별 대화/마이페이지가 불가 |
| R-6 | 마이페이지 : 이름 변경 · 비밀번호 변경 · 테마 변경(라이트/다크/시스템) | 개인화가 불가 |
| R-7 | 사용자별 대화 저장/조회 | 대화 이력 유지가 불가 |
| R-8 | 반응형 웹 UI (Claude/GPT 류 채팅 틀, Supernova로 마감) - PC + 모바일 브라우저 | PC/모바일 사용이 불가 |
| R-9 | 응답 스트리밍(타이핑 효과) | 응답 대기 UX가 저하됨 |
| R-10 | 목업 계층 : `APP_MODE=mock`, 실 연동 인터페이스는 완성 · 응답만 목 데이터 | API KEY/VectorDB 없이 개발·시연이 불가 |
| R-11 | 응답 진행 단계 표시 : 질문 분석 → 참조 문서 검색 → 답변 작성 3단계를 채팅 화면에 실시간 표시(**SSE 개시 이후 구간에 한정** - 첨부 업로드·prepare·429는 범위 외) | 첫 토큰 전 대기가 무반응으로 보이고, RAG가 실제로 문서를 참조했는지 사용자가 알 수 없음 |

---

## 제외 범위

| 항목 | 제외 사유 | 다시 논의할 시점 |
|------|-----------|-------------------|
| 소셜 로그인(구글/카카오 등 OAuth) | 자체 이메일 로그인으로 1차 충분, 범위 확대 방지 | 실사용자 유입 단계 |
| 다국어(i18n) | 1차 사용자는 한국어, 문자열 외부화 비용 회피 | 해외 사용자 요구 발생 시 |
| 관리자(어드민) 페이지 | 초기엔 운영자 시드/스크립트로 대체 | 문서/사용자 관리 수요 증가 시 |

---

## 범위 변경 이력

| 일자 | 항목 | 제외 ↔ 포함 | 사유 | 영향받는 Phase · AC |
|------|------|-------------|------|----------------------|
| 2026-07-28 | R-2 문서(PDF) 첨부 | 포함 → **제외** | 업로드·저장·UI 표시는 되는데 `buildInput`이 이미지만 전달해 **모델에는 전혀 안 감**(Phase 4 리뷰 H4-3). 사용자가 PDF를 근거로 답했다고 오해하는 상태였음. 실제 근거로 쓰려면 OpenAI Files + 대화 전용 Vector Store 업로드가 필요하고 실 키 없이는 검증 불가라, **받지 않는 것**을 계약으로 함. 사용자 결정 2026-07-28 | AC-11(문서 용량 갈래 소멸) · AC-12(파일 정리 대상이 이미지뿐) · Phase 3·4 |
| 2026-07-28 | R-4 무자료 표기 방식 | 텍스트 접두 → **플래그 + 화면 표시** | 라이브는 인용 0건을 스트림이 끝나야 알아 이미 보낸 토큰 앞에 접두를 못 붙임 → 화면엔 접두 없는 답변, 재조회하면 접두 있는 답변이 뜨는 불일치(Phase 4 리뷰 H4-1). 접두를 텍스트에서 빼고 `noSource`·출처 0건으로 판정·표시하면 Mock/Real이 한 규칙으로 수렴함. 사용자 결정 2026-07-28 | R-4 · P-8 · AC-6 · 성공의 정의 |
| 2026-07-28 | 무자료 표시 조건 | 「출처 0건이면 표시」 → **「출처 0건 + 판정을 마친 답변에만 표시」** | 중단을 `complete`로 저장하기로 하면서, 인용이 오기 전에 끊긴 답변이 표시 조건에 그대로 걸려 **100% 거짓 표시**가 됐음(재리뷰). 상태는 결정대로 `complete`로 두고 `messages.stopped`로 표시만 억제함 - 화면에 중단 전용 표시는 만들지 않음. 사용자 결정 2026-07-28의 파생 | R-4 · P-8 · AC-6 · AC-9 · Phase 5·6 |
| 2026-07-28 | AC-9 중단 상태 정책 | 「선택한 정책대로」(미정) → **부분 저장 + `complete`** | 서버는 `error`, 프론트는 `complete`로 같은 상황을 다르게 기록해 판정 자체가 불가능했음(Phase 5 리뷰). 사용자가 스스로 멈춘 것은 실패가 아니라는 쪽으로 확정하고, 진짜 오류만 `error`로 남기도록 구분함. 사용자 결정 2026-07-28 | AC-9 · R-1 · Phase 5 |
| 2026-07-28 | AC-17 판정 대상 | 「에러 바운더리가 SSE 실패도 처리」 → **렌더 예외 / SSE 실패를 분리** | React 에러 바운더리는 비동기 콜백을 구조적으로 못 잡아, 원문대로면 영원히 미판정임(Phase 6 리뷰). 실제 구현은 인라인 오류 표시이므로 문안을 실제 장치에 맞춰 갈라 적음. 사용자 결정 2026-07-28 | AC-17 · Phase 6 |
| 2026-07-28 | AC-18 잠금 지점 | 「운영 프로필에서 mock이면 실패」 → **모드 미설정이면 실패**(+ 기존 조건 유지) | 프로필을 켜는 경로가 저장소에 없어 가드가 생성되지 않을 수 있었음 - 사람이 배포 설정을 빠뜨리면 잠기지 않는 자물쇠였음(Phase 0 리뷰). 잠금을 프로필에서 값 자체로 옮김. 사용자 결정 2026-07-28 | AC-18 · P-2 · R-3 · Phase 0 |
| 2026-07-27 | R-11 응답 진행 단계 표시 | 미언급 → 포함 | 첫 토큰 전 대기 구간이 무반응으로 보임 + RAG가 실제로 검색을 수행했음을 사용자가 확인할 수단이 없음(D-4 신뢰성의 연장). 사용자 요구 2026-07-27 | Phase 7 신설 · AC-23·24 신설 · P-10 신설 · R-13·14 신설 |

---

## 비기능 요구

| 구분 | 요구 | 측정 방법 | 이 시점에 잴 수 있는가 | 출처 |
|------|------|-----------|------------------------|------|
| 브라우저/기기 지원 | 모던 브라우저 최신 2버전(Chrome/Edge/Safari/Firefox) + 모바일 Safari/Chrome. IE 미지원 | 대상 브라우저 스모크 확인 | 아니오 - UI 산출 이후 | 사용자 답변 2026-07-24 |
| 성능 | 목표 응답 지연 미정 | - | 아니오 | 미정 - 5단계 배포 실측 후 확정(미결) |
| 보안 | 비밀번호 안전 저장 · 시크릿 비커밋 · 카메라용 HTTPS | 해싱 알고리즘 확인 · 시크릿 스캔 게이트 · 배포 스킴 | 부분 | 1단계 확정 예정(기본안 BCrypt 제안) |
| 접근성 | 키보드 조작 + 명도 대비 기본 준수 · **진행 단계 표시는 `role="status"`로 메시지 `role="log"` 영역과 분리하고 원자적으로 교체함**(단계 3회 교체가 스크린리더 낭독으로 누적되지 않게 함, R-11) | 수동 점검 · 스크린리더로 단계 낭독 1회씩 확인 | 부분 - 단계 낭독은 Phase 7에서 확인 가능 | 사용자 확인 필요(미결) · 단계 항은 R-11 리뷰 2026-07-27 |
| 다국어 | 해당 없음 - 한국어만 | - | - | 제외 범위 표 참조 |

> 출처가 `미정`/`사용자 확인 필요`인 행은 `docs/02_운영.md` 미결 표에 등재함(가이드 0단계 통과 조건).

---

## 진행 로그

| 일자 | 단계 | 결과 | 근거(명령 출력 · 커밋 · URL) |
|------|------|------|-------------------------------|
| 2026-07-24 | 0-A 저장소 개통 | 통과 | 커밋 `bc78bf9` · 원격 push(github.com/changs0124/rag-chatbot) · `git ls-files`에 시크릿 0건 · `git check-ignore`로 `.env`/`.env.example` 검증 |
| 2026-07-24 | 0 요구 정리 | 통과 | 본 문서(개요·포함·제외·비기능·미결 표 작성) |
| 2026-07-24 | 참조 구현 발견·취급 결정 | - | `openai-rag-bot`(선행 구현 + PLAN v3.0) 발견 → 강한 참조 + 신규 유지로 확정. 저장소=로컬/서버 디스크 |
| 2026-07-24 | 1 기술 스택 결정 | 통과 | 공식 문서 조사(백/프/OpenAI) + `docs/01_기술선택.md` 작성 + 사용자 승인 4건 |
| 2026-07-24 | 2 플랜 작성 | 통과 | 원칙·Phase(0~6)·AC(22)·이관·실측·리스크(R-1~12) 표 + `docs/04_런칭.md` |
| 2026-07-24 | 3 플랜 리뷰 | 통과 | critic 별도 패스 라운드1 REVISE→전건 반영→라운드2 APPROVE(새 결정변경 0) |
| 2026-07-24 | 플랜 승인 | 통과 | 사용자 플랜 승인 → 4단계 착수 |
| 2026-07-24 | 4 CI 구축(Phase 0) | 통과 | 백엔드 스캐폴드(SB 3.5.16, mvnw) test 3건·verify · 프론트(Vite8/React19/TS6.0.2/oxlint) lint·test 1건·build · 문서 참조 게이트 깨뜨려 검증 · 원격 CI run 30056454074 3잡 전부 success(첫 실패=mvnw 실행비트 → 수정 후 초록) |
| 2026-07-24 | 6단계 Phase 1 스키마·MyBatis | 통과 | Flyway 5테이블 마이그레이션 + UserMapper + UuidTypeHandler. Testcontainers(실 PostgreSQL)로 uuid 왕복·5테이블 생성 검증 → 원격 CI run 30056913636 초록(M7·AC-20) |
| 2026-07-24 | 6단계 Phase 2 인증·인가 | 통과 | 자체 이메일/비번 + JWT(java-jwt) + BCrypt + Security stateless. Testcontainers 인증 플로우 7건 CI 초록(run 30057292049). AC-2·3·4 충족. **AC-1(교차 접근 404)은 리소스가 생기는 Phase 3으로 이관**(requireOwned 패턴은 Phase 3에서 대화/파일에 적용) |
| 2026-07-24 | 6단계 Phase 3 대화·파일 | 통과 | 대화 CRUD + requireOwned 404(AC-1) · 파일 검증(AC-11) · 서명 경로 토큰 서빙(AC-22) · 고아 정리(AC-13). Testcontainers 교차접근/파일 플로우 CI 초록(run 30058329481). 첫 실패=클래스별 컨테이너 → 싱글턴 컨테이너로 수정. AC-12 OpenAI 정리는 Phase 4 |
| 2026-07-24 | 6단계 Phase 4 OpenAI 경계·목업 | 통과 | OpenAiService(streamChat+deleteResources) + 기록형 Mock + Real 골격 + POST /api/chat(비스트리밍) + 출처 저장. Testcontainers 채팅 플로우 CI 초록(run 30058676424). AC-5·6·7·12·14·21 |
| 2026-07-24 | 6단계 Phase 5 스트리밍·레이트리밋 | 통과 | SSE(meta→token→citations→done) + 사용자별 레이트리밋 429. prepare(동기 검증)+stream(비동기). Testcontainers SSE/429 CI 초록(run 30059099825). 첫 실패=async 재디스패치 인증 → ASYNC/ERROR permitAll. AC-9·10 (AC-8 무절단은 배포 후 이관) |
| 2026-07-24 | 6단계 Phase 6a 프론트 로그인/회원가입 | 통과 | apiClient(JWT) + AuthProvider/ThemeProvider + 로그인/회원가입 + 라우팅 + Tailwind. CI 초록(run 30059506994) |
| 2026-07-24 | 6단계 Phase 6b 채팅 UI | 통과 | Sidebar·MessageList·Composer·CameraCapture·출처 각주 + SSE fetch 수신. CI 초록(run 30059839453) |
| 2026-07-24 | 6단계 Phase 6c 마이페이지 | 통과 | 백엔드 프로필 수정 API + MyPage(이름/비번/테마) + 계정 테마 동기화. CI 초록(run 30060121928). AC-15·16 |
| 2026-07-24 | 6단계 Phase 6d 에러바운더리·접근성·반응형 | 통과 | ErrorBoundary(AC-17) + aria-live + 반응형 + Claude/GPT 틀. CI 초록(run 30060284709) |
| 2026-07-24 | 6단계 구현 완료 | 통과 | Phase 0~6 전부 CI 초록. 백엔드 27건+ / 프론트 7건 |
| 2026-07-24 | 로컬 개발환경 기동 | 통과 | CORS 추가(프론트5173↔백8080). Docker postgres + 백엔드(mock) + Vite dev. 샘플계정 demo@ragchatbot.dev. login→대화→채팅 SSE(출처 포함) 실동작 확인 |
| 2026-07-24 | 실 OpenAI 연동 구조 구현(OpenAiRealService) | 통과(구조만) | Responses API(gpt-4o) 스트리밍 + 공용 Vector Store file_search(env `OPENAI_VECTOR_STORE_ID`) + 이미지 비전(base64 data URL) + annotations→CitationData + 무자료 접두 + deleteResources(공용 Store 삭제 금지 가드). `app.openai.*` 설정 추가. `./mvnw compile`·`./mvnw test`(mock) EXIT=0. 커밋 `8e7bde6` push → 원격 CI run 30065506819 3잡(backend/frontend/docs) 전부 success. **실호출 검증은 실 키 확보 후 M2 스파이크로 미룸(S-1, 완성 위장 금지)** |
| 2026-07-24 | 7단계 재리뷰(수정분) | 통과 | 별도 code-reviewer 패스(작성자와 다른 레인). HIGH 1건 즉시 수정 = 인용 `uri=null`이 `ChatService.Map.of`에서 NPE(라이브 인용 답변마다 깨짐) → non-null `""`로 파리티 복원. MEDIUM 2건 반영 = 완료 이벤트 없이 스트림 종료 시 무자료 위장 대신 예외 · live 모드 blank api-key fail-fast. 이월 3건(file_id 인용 상관·mime 추론·base64 메모리)은 M2로. `./mvnw test`(mock) EXIT=0 · 커밋 `6f1e133` push → 원격 CI run 30065903720 3잡 전부 success |
| 2026-07-27 | 범위 추가(R-11 진행 단계 표시) 플랜 반영 | 통과(문서 변경만 - 코드 변경 0) | 사용자 요구 → 표시 방식 승인 2건(한 줄 교체형 · 이력 비저장). 라이브 이벤트명을 공식 문서로 확인(`response.created`·`response.in_progress`·`response.file_search_call.in_progress`/`.searching`·`response.output_text.delta`, 확인일 2026-07-27, URL은 `docs/01_기술선택.md` 채택 표) → P-10 · Phase 7 · AC-23·24 · 리스크 R-13·14 · 이관-6 · 이벤트 매핑 표 등재. **구현·검증은 Phase 7에서 수행(현 시점 미구현이므로 완료로 적지 않음, S-1)** |
| 2026-07-27 | R-11 문서 리뷰(별도 2패스, 라운드 3까지) | 통과 - 지적 전건 반영 후 APPROVE | **결정** : 완료 판정을 SSE 순서·전환·잔류로 한정하고 체감은 이관-7로 분리 · 4단계 `citing` 미채택 · P-10을 "이벤트 또는 통과한 코드 경계"로 정정하고 목업 전용 라벨 도입 · `analyzing`을 라이브·목업 공통 서버 경계(`meta` 직후)로 통일. **근거** : 라운드 3에서 새 결정 변경 0건 · `check-doc-refs.sh` EXIT=0 · 변경 파일 전부 `docs/`. 라운드별 지적 수·처리는 `docs/01_기술선택.md` 리뷰 라운드 표. **구현은 이 시점 미착수** |
| 2026-07-27 | 6단계 Phase 7 진행 단계 표시 구현 | 통과(로컬) - 원격 CI 대기 | `OpenAiService`에 `Stage`(analyzing·searching·generating) + `stageLabel` 추가(라벨 소유자 = 구현, P-2) · `ChatService`가 `meta` 직후 `analyzing` 발행 + 첫 토큰 이후 `stage` 차단 · Mock은 코퍼스 조회 경계에서 목업 전용 라벨 · Real은 `file_search_call.in_progress`/`.searching`·첫 `output_text.delta`에서만 발행 · 프론트는 `stage` 수신 후 `role="status"`+`aria-atomic` 한 줄(log 영역 밖)로 표시하고 고정 문구 `응답 생성 중…` 제거. **근거** : `./mvnw verify` EXIT=0(백엔드 42건, 신규 `ChatStageHonestyTest` 4 · `ChatStreamContractTest` 1 · `OpenAiRealStageMappingTest` 3) · 프론트 `vitest` 12건·`oxlint` 0·`vite build` 성공 · `check-doc-refs.sh` EXIT=0. **S-2 깨뜨림 확인 3회** : 목업 `searching` 제거 → Honesty 3건 실패 · 프론트 라벨을 고정 문구로 → 단계 테스트 1건 실패 · 검색 근거 없이 `response.created`에 검색 단계 부여 → 매핑 테스트 1건 실패(전부 원복 후 재초록). AC-23·24 로컬 충족, 라이브 실호출은 이관-6·7로 잔존 |
| 2026-07-27 | Phase 7 원격 CI + 목업 가시성 보완 | 통과 | 커밋 `9912bdf` push → 원격 CI run 30230499675 3잡(backend/frontend/docs) 전부 success. 로컬 실동작 확인 : `rag-pg` 재기동 + 백엔드(mock) + Vite dev에서 `/api/chat` SSE에 `stage` 3종(`질문 분석 중(목업)`→`목업 코퍼스 조회 중`→`답변 작성 중(목업)`)이 `token` 앞에 실림. **다만 화면 표시 시간이 0에 가까워 사람이 볼 수 없는 문제 발견** → 사용자 결정으로 **프론트 최소 표시 시간(350ms/단계)** 도입(도달한 단계만 순서대로 유지 · 목업 백엔드 인위 지연안은 P-10 위반으로 탈락). P-10·AC-23·Phase 7 산출물·실측 표·탈락 표에 반영. `vitest` 16건(신규 `useChat.test.ts` 3 + 최소 표시 유지 1) · `oxlint` 경고 0 · build 성공 · S-2 깨뜨림 확인(큐 제거 → 훅 테스트 2건 실패 후 원복) |
| 2026-07-28 | 가이드 v3.8 적합성 정비 - 게이트 4종 신설 + 문서 참조 게이트 공회전 수정 + 의존성 취약점 2건 | 통과 | **결정** : `docs/02_운영.md` 신설 · 1-A 소급 작성 · 게이트 4종(케이스 하한 · 계획서 필수 표 · 시크릿 스캔 · 의존성 취약점) 구현 · `react-router` 8.3.0 전환과 pgjdbc 42.7.13 고정. **근거** : PR #3 6잡 pass(run 30321202240 → 최종 30323175290) · 로컬 게이트 4종 EXIT=0 · 깨뜨림 확인 8회. **세부는 옮겨 적지 않음**(S-6 규칙 4) - 낮춘 기준과 대체 방어가 못 막는 것은 `docs/02_운영.md` 처리 이력 표의 2026-07-24·28 행들, 리뷰 지적 수·판정은 `docs/01_기술선택.md` 리뷰 라운드 표 |
| 2026-07-28 | 7단계 Phase별 리뷰(Phase 0~6) + 지적 1차 반영 | 부분 통과 - 횟수 요건 충족 · 지적 처리 진행 중 | 별도 레인 4개(`rv-phase0`·`rv-phase12`·`rv-phase34`·`rv-phase56`)로 Phase 0~6을 리뷰 → **전 Phase REVISE**(결정 변경 51 · 표현 16). **즉시 수정 3건** : AC-2 서명·만료·변조 테스트 3건(`verify`→`decode` 치환 시 3건 실패 확인) · AC-18 컨텍스트 테스트 4종(`@Profile` 오타 시 3건 실패 확인) · AC-20 인덱스 단언(`create index` 제거 시 실패 확인). **판정 정정 6건** : AC-9·11·12·14·17·18을 부분/미판정으로 되돌림(근거가 주장을 덮지 못했음). **결정 대기 8건**은 `docs/02_운영.md` 미결 표 `[리뷰]` 항목으로 등재. 근거 : `./mvnw -B verify` EXIT=0(**49건**, 42→49 래칫 상향) |
| 2026-07-28 | 라이브 정합성 4건 반영(리뷰 지적 처리 2차) | 통과(로컬) | ① 무자료 표기를 텍스트 접두 → `noSource` 플래그 + 화면 표시로 전환(Mock·Real 모두 저장 텍스트 = 스트리밍 텍스트) ② 문서(PDF) 첨부를 업로드 단계에서 400으로 거부(R-2 축소, 사용자 승인) ③ 대화 삭제에서 DB 를 먼저 지우고 파일·OpenAI 정리를 그 뒤로 옮김(트랜잭션 안의 되돌릴 수 없는 부작용 제거) ④ 재조회 응답(`MessageResponse`)에 첨부 추가 - URL 은 조회 시점 재서명. **근거** : `./mvnw -B verify` EXIT=0(**51건**) · 프론트 `vitest` **18건**·lint 0·build 성공 · **역방향 확인 2회**(PDF 재허용 → 거부 테스트 실패 · 재조회 첨부 제거 → 재조회 테스트 실패, 둘 다 원복 후 재초록) |
| 2026-07-28 | 대기 결정 4건 반영(리뷰 지적 처리 3차) | 통과(로컬) | 사용자 결정 4건을 받아 구현함. ① **AC-18 활성화 방식** - 잠금 지점을 `@Profile("prod")`에서 `app.mode` 필수화로 옮김(`ProdModeGuard` → `AppModeGuard` 개명). 기본값 `mock` 제거로 **모드 미설정이면 어떤 환경에서도 기동 실패** ② **중단 상태 정책** - 서버가 중단을 `error`로 기록하던 것을 프론트와 같은 `complete`로 통일하고, SSE 전송 실패(클라이언트 종료)를 전용 예외로 분리해 진짜 오류와 구분. 받은 것이 없으면 서버·프론트 모두 빈 메시지를 남기지 않음 ③ **AC-17 문안 정정** - 렌더 예외(바운더리)와 SSE 실패(인라인 오류)를 갈라 적고 인라인 경로를 테스트로 잠금 ④ **보안·정합성 5건** - 이메일 소문자 정규화 + `lower(email)` 유일 인덱스 · 계정별 로그인 시도 상한 · 비밀번호 변경 시각 기준선으로 이전 발급 JWT 거부 · 고아 첨부 회수 스케줄러 기동(로직만 있고 부르는 곳이 없었음) + cutoff 클램프 · 파일 서빙 `nosniff` 고정. **근거** : `./mvnw -B clean verify` EXIT=0(**62건**, 51→62) · 프론트 `vitest` **21건**·`oxlint` 0·build 성공(gzip 82.36KB) · **역방향 확인** - 바꾼 지점 **열 군데를 한꺼번에 되돌려** 실패 목록이 의도한 테스트와 맞는지 확인함(실패 11건 = 백엔드 10 · 프론트 1. 이메일 정규화 한 곳이 정규화·중복차단 두 테스트를 동시에 깨서 지점 수보다 실패가 하나 많음). 전건 원복 후 재초록. AC-9·17·18을 통과로 재판정하고 문안을 실제 동작에 맞춤 |
| 2026-07-28 | PR #5·#6·#7 별도 레인 재리뷰 라운드 1 | 통과(로컬) - 수렴 미도달 | **결정** : 차단 3건(비밀번호 변경 후 본인 세션 401 회귀 · CI 게이트 표 하한 오기 · 리뷰 라운드 표 렌더 파손) + 결정 변경 4건(`messages.stopped`(V3)로 무자료 배너 억제 · 정리 실패분 미결 분리 · 레이트리밋 맵 분 단위 스윕 · AC-12 잔여 분리) 전건 반영. **근거** : `clean verify` 64건 · `vitest` 24건 · 역방향 6지점 → 실패 7건 후 원복 재초록. **세부는 `docs/01_기술선택.md` 리뷰 라운드 표와 `docs/02_운영.md` 처리 이력 「비밀번호 변경이 본인 세션까지 끊던 회귀」·「중단된 답변에 자료 없음이 거짓으로」 행** |
| 2026-07-28 | 재리뷰 라운드 2(코드·문서) | 통과(로컬) - 수렴 미도달 | 라운드 1 지적 19건 전부 닫힘 확인. **새 지적 13건 중 결정 변경 3건은 모두 라운드 1을 고치면서 새로 만든 것** : 레이트리밋 카운터 `volatile` · 무효화 기준선을 앱 시계로 통일 · `stopped` 코드를 넣고 원문 P-8·R-4·AC-6을 안 고쳐 규범 문서가 코드와 반대로 읽히던 것(S-2). **근거** : `clean verify` 64건 · `vitest` 25건 · 역방향 2지점. `volatile`·시계 통일은 되돌려도 CI가 초록이라 잠글 테스트가 없음(`docs/02_운영.md` 처리 이력 「회귀 테스트가 없는 수정 2건」) |
| 2026-07-28 | 재리뷰 라운드 3(코드·문서) - **수렴 도달** | 통과(로컬) | **두 레인 모두 결정 변경 0건**(코드 APPROVE · 문서는 표현 2건뿐). 표현 5건 반영 : `volatile` 주석의 효과 과장 정정(check-then-act 경합은 남음) · 무효화 기준선을 BCrypt 뒤로 이동(앞에서 재면 옛 토큰 생존 창이 0.1~0.5초 넓어짐) · 빈 버블 제거에 `complete` 가드 + 테스트 1건. **근거** : `clean verify` 64건 · `vitest` 26건 · 역방향 1지점. **라운드 1→2는 결정 변경 3건을 낳았으나 2→3은 0건**이므로 수렴으로 판정함 |
| 2026-07-28 | 8단계 전제 정비 - 리스크 표 테스트 이름 8건 부재 해소 | 통과 | **결정** : 이름을 `클래스#메서드`로 적고(클래스명만으로는 케이스 실재가 확인되지 않아 같은 낡음이 반복됨), 대조에서 드러난 완화 공백 3건을 테스트 5건으로 메움. 테스트로 잴 수 없는 R-8·R-11은 완화 문구를 실제 수단으로 되돌림. **근거** : PR #9 6잡 pass(run 30345860901) · `clean verify` 69건 · 역방향 2회. **세부는 `docs/02_운영.md` 처리 이력 「리스크 표 완화 공백 3건」·「경계 격리 테스트의 실패 형태」 행** |
| 2026-07-28 | 미결 `[리뷰]` 잔여 4건 반영 - 전건 종결 | 통과(로컬) | 사용자 결정 4건(전부 권장안). ① **레이트리밋 키 총량 상한 + LRU 축출** - 맵 접근을 `synchronized` 로 모아 종전의 락 밖 읽기 가시성·check-then-act 경합도 함께 사라짐 ② **localStorage 유지를 명시적으로 선택** - 코드 변경 0이고, 노출 창이 만료 120분·비번 변경 무효화로 제한된다는 근거를 남김(대외 공개 시 재검토) ③ **AC-12 문안을 현 구조에 맞게 정정** - 없는 기능(OpenAI 파일 업로드)을 전제하던 요구를 실물에 맞춤 ④ **저장소 스캔 회수** - 행이 사라진 뒤 남은 파일을 고아 회수 스케줄러 2차 패스로 지움. **근거** : `clean verify` EXIT=0(**82건**, 76 → 82) · **역방향 확인 2지점 → 실패 4건**(키 상한 제거 → 3건 · 참조 판정 제거 → 1건, 둘 다 원복 후 재초록). **감수한 약화 2건은 `docs/02_운영.md` 처리 이력에 등재**(축출이 곧 카운터 리셋 · `deleteResources` 가 사실상 no-op) |
| 2026-07-28 | 채팅 동시 스트림 상한(back-pressure · 미결 `[리뷰]` 1건 종결) | 통과(로컬) | **결정**(사용자) : 진입 시점 동시성 상한 · 사용자별 1 + 전역 N · SSE 타임아웃 유지. **거절을 `prepare` 앞에 둠** - `prepare` 가 사용자 메시지를 저장하므로 뒤에서 거절하면 답변 없는 메시지가 남음. 전역 N 은 `chatExecutor` 풀 크기와 **같은 프로퍼티**라 상한이 풀보다 커져 큐가 쌓이는 경로가 없음. 계수는 0 이 되면 항목을 지워 키가 누적되지 않음(미결 「레이트리밋 카운터 회수」와 다름). **근거** : `clean verify` EXIT=0(**76건**, 69 → 76 · 단위 5 + 통합 2) · **역방향 확인 2지점**(거절을 `prepare` 뒤로 → 고아 사용자 메시지 1건 잔존으로 실패 · 계수 0 항목 삭제 제거 → 키 50건 누적으로 실패, 둘 다 원복 후 재초록). **남는 것** : 다중 인스턴스에서는 상한이 인스턴스별로 갈려 실효 한도가 곱해짐(R-6 미결) |
| 2026-07-28 | 대화 전환 시 이전 스트림 중단(미결 `[리뷰]` 1건 종결) | 통과(로컬) | **결정** : 대화를 떠날 때 `abortActiveStream`(abort + 단계 큐 비움)을 부름. 중단 후 처리는 2026-07-28 확정된 정책(부분 저장 + `complete`)을 그대로 타므로 **새 결정 0건**이고, `send` 의 catch 가 도는 시점엔 `messages` 가 이미 새 대화 것이라 이전 턴 patch 는 대상을 못 찾고 흘러감. 적용 지점은 `selectConversation` · `newConversation` · `deleteConversation`(활성 대화 삭제도 전환임). **근거** : `vitest` **29건**(26 → 29, 신규 3) · `oxlint` 0 · build 성공(gzip 82.41KB) · **역방향 확인 1지점**(`abort()` 호출 제거 → 신규 3건 실패, 원복 후 재초록). 프론트 하한 26 → 29 |
| 2026-07-28 | 가이드 v3.9 적합성 정비(S-6 신설분) | 통과 | **결정** : 배경 서술을 5개 문서에서 걷고 · 진행 로그를 `결정 + 근거 + 정본 포인터`로 줄이고(라운드 세부는 리뷰 라운드 표로) · 케이스 하한 숫자를 문서에서 빼 정본을 `scripts/case-floors.env` 하나로 두고 · 상태 마커 5색을 도입해 범례·상태 정본을 `docs/02_운영.md`에 모음. **처리 이력은 줄이지 않음**(낮춘 기준·감수한 위험은 남긴다는 S-6 주의가 압축보다 우선). **삭제 1행** : 미결 표 `전역 Maven 미설치` - 사람 결정을 기다린 적이 없고 결정은 채택 표에 이미 있어 흔적임. **근거** : 게이트 3종 EXIT=0 · 마커와 상태 열 대조 27행 일치. 못 막는 것은 `docs/02_운영.md` 처리 이력 「S-6을 한 건도 지키지 않고 있었음」 행 |

---

## 게이트 체크

> 표기 규칙 : 구현 단위는 `Phase N`, 가이드 절차는 `N단계`로 적음(예 : `Phase 7`(진행 단계 표시) ≠ `7단계`(재리뷰)). 이 표의 "단계" 열은 가이드 절차임.

| 단계 | 통과 조건 | 판정 | 근거 | 일자 |
|------|-----------|------|------|------|
| 0-A | 원격 커밋 1+ · 시크릿 비추적 · 런타임 버전 고정 | 통과 | 원격 push 완료 · 시크릿 0건(명령 확인) · `.nvmrc`=22, Java17/Maven은 4단계로 이관 | 2026-07-24 |
| 0 | 제외 범위 비지 않음 · 모호 해소 · 비기능 출처 확보 · 성공 정의 확인 가능 | 통과 | 제외 3행 · 요구 확정 질의 완료 · 비기능 출처/미결 등재 · 성공 정의 화면으로 확인 가능 | 2026-07-24 |
| 1 | 기술 스택 확정 · 사용자 승인 | 통과 | `docs/01_기술선택.md` 채택/탈락 표(버전·URL·확인일 2026-07-24) · 사용자 승인(SB 3.5.16·PostgreSQL·JWT·RestClient) | 2026-07-24 |
| 1-A | 공식 스캐폴딩 기동/빌드 1회 성공 · `기본` 폴더 생성 또는 이탈 사유 · 구조 출처 표 · `.gitignore` 스택 갱신 | 통과 - **2026-07-28 소급 판정**(착수 시 가이드 v2.4에 1-A 장이 없었음) | 스캐폴딩 빌드 : Phase 0 진행 로그의 `mvnw verify` · `vite build` 성공 행이 근거 · 폴더 구조 표 이탈 20행 + 구조 출처 표 2행 기입(`docs/01_기술선택.md`) · `.gitignore`에 `target/`·`dist/`·`node_modules/`·`.env` 등재 및 `.env.example` 추적 유지 확인 | 2026-07-28 |
| 2 | 플랜 작성(원칙·Phase·AC·이관·실측·리스크·런칭) | 통과 - 사용자 승인 완료 | 표 작성 + 3단계 리뷰 14건 반영 + 플랜 승인(2026-07-24) | 2026-07-24 |
| 3 | 플랜 리뷰(별도 critic 패스) | 통과 | 라운드1 REVISE(결정변경 10) → 전건 반영 → 라운드2 APPROVE(새 결정변경 0). 잔여 문구 3건 정리 | 2026-07-24 |
| 4 | CI가 제품 코드보다 먼저 초록 · 케이스 수 하한 · 게이트 전체가 한 바퀴 · 시크릿/의존성 포함 | **통과 - 2026-07-28 재판정** | 2026-07-24의 "통과"는 **게이트 3종(시크릿 스캔 · 의존성 취약점 · 계획서 필수 표)과 케이스 수 하한이 없는 상태**였음(`docs/02_운영.md` 처리 이력 1·2행). 2026-07-28 전건 구현 후 **로컬에서 깨뜨려 확인** : 케이스 하한 `실측 42 / 하한 999` EXIT=1 → 원복 EXIT=0 · 계획서 표 3형태(EMPTY-CELL · MISSING-TABLE · STALE) 각 EXIT=1 → 원복 EXIT=0 · 문서 참조 3형태(MISSING · STALE · WIKILINK) 각 EXIT=1 → 원복 EXIT=0 · gitleaks 픽스처 커밋 `leaks found: 1` EXIT=1 → 폐기 후 EXIT=0 · `npm audit` 실취약점 2건 EXIT=1 → 수정 후 `found 0 vulnerabilities` EXIT=0 · Trivy `CVE-2026-54291 HIGH` EXIT=1 → 수정 후 `pom.xml 0` EXIT=0. **원격 확인 완료** : PR #3 CI run 30321202240 = **6잡 전부 success**(backend 47s · frontend 21s · docs 5s · secrets 9s · deps-backend 27s · deps-frontend 16s). 첫 실행(run 30321076637)에서 `deps (백엔드 의존성 취약점)` 1잡이 2초 만에 실패 = `Unable to resolve action aquasecurity/trivy-action@0.36.0`(태그에 `v` 접두 필요) → `v0.36.0`으로 수정 후 초록. 이후 리뷰 지적 반영분까지 **PR #3 체크 6잡 pass**(run 30323175290 - 머지 전 확인. 근거 run 이 HEAD 를 덮는지는 `docs/03_PR로그.md` 기입 규칙을 따름). **[이관-8] 런타임 버전 대조**는 서술이 아니라 `scripts/check-runtime-versions.sh`가 backend·frontend 잡에서 매번 판정함(`.nvmrc` ↔ `node -v` · `pom.xml java.version` ↔ `java -version`). **게이트 전체가 한 바퀴 돌았으므로 통과로 닫음** | 2026-07-28 |
| 5 | 배포 경로 개통(최소 산출물 + 롤백) · 예산 게이트 확정 · **[이관-3 수신] AC-8 무절단** · **[이관-4 수신] AC-19 실기기** | 대기 - 배포 대상 미결(S-3) | 배포 대상·계정·시크릿이 사용자 결정 사항. 목업 단계라 6단계와 병행 가능 | - |
| 6 구현 | Phase별 PR이 단독 CI 통과 | 진행 중 - **커밋 단위로는 전건 초록 · PR 단위 판정은 미측정** | Phase 0~6 원격 CI 초록(진행 로그 2026-07-24 "6단계 구현 완료") · Phase 7 원격 run 30230499675 · 보완분 run 30231123362 전부 3잡 success. **다만 Phase 0~7은 `main` 직접 push라 "PR이 단독으로 통과"를 PR 단위로 잰 적이 없음**(`docs/03_PR로그.md` 1행 = `측정 불가` · `docs/02_운영.md` 처리 이력 「PR 절차 미적용」 행) | 2026-07-28 |
| 7 | Phase마다 별도 리뷰 패스 1회 · 리뷰 담당·리뷰일·처리 결과 기록 | **통과 - 2026-07-28 재판정**(횟수·기록 요건 충족 + 결정 변경 0건 라운드 도달. 다만 `[리뷰]` 미결 6건은 **사람 결정 대기로 열려 있음** - S-3이라 리뷰 미처리와 다름) | Phase 0~6을 4개 레인(`rv-phase0`·`rv-phase12`·`rv-phase34`·`rv-phase56`)으로 나눠 리뷰 → 7단계 행 18개 ≥ Phase 8개로 **횟수 요건은 충족**. 전 Phase REVISE(결정 변경 51건)였고 **지적 처리는 미결 6건을 제외하고 종결** - 1차 즉시 수정 3건(AC-2·18·20 테스트) → 2차 라이브 정합성 4건 → 3차 대기 결정 4건 반영으로 `[리뷰]` 미결 12건 중 8건을 닫았고, **남은 6건**(대화 전환 시 스트림 abort · 채팅 풀 back-pressure · 레이트리밋 카운터 회수 · localStorage 토큰 결정 기록 · AC-12 문안 · 삭제 후 정리 실패분 회수)은 `docs/02_운영.md` 미결 표에 개별 행으로 있음. **머지된 PR #5·#6과 머지 대기 PR #7을 묶어 별도 레인 재리뷰를 라운드 3까지 돌림**(레인 6개 · 매 라운드 코드·문서 각 1). **라운드 1** `rv-code-567`·`rv-docs-567` 둘 다 REVISE - 지적 19건(차단 3 · 결정 변경 4 · 표현 12) 전건 반영. **라운드 2** `rv-code-r2`·`rv-docs-r2` 둘 다 REVISE - 지적 13건(결정 변경 3 · 표현 10) 전건 반영. **결정 변경 3건은 모두 라운드 1을 고치면서 새로 만든 것**이었음. **라운드 3** `rv-code-r3` **APPROVE** · `rv-docs-r3` REVISE(표현 2건뿐, 둘 다 기록 형식) - **두 레인 모두 결정 변경 0건**이므로 **수렴으로 판정함**. 각 라운드의 담당·일자·처리 결과는 `docs/01_기술선택.md` 리뷰 라운드 표 | 2026-07-28 |
| 8 | 런칭 표 전항목 실물 확인 · 성공의 정의 도달 · 리스크 표의 테스트 이름이 전부 실재 · 감수 결정 재검토 | 미통과 - 미도래(**전제 1건은 2026-07-28 해소**) | **「리스크 표의 테스트 이름이 전부 실재」는 충족함** : 이름 8건 부재를 정비해 12개 전부 `클래스#메서드`로 실재하고, 부족했던 완화는 테스트 5건을 새로 만들어 메움(`FileFlowTest` 3 · `OpenAiServiceBoundaryTest` 2, 백엔드 64 → 69). 테스트로 잴 수 없는 R-8·R-11 은 완화 문구를 실제 수단으로 되돌리고 `docs/02_운영.md` 처리 이력에 적음. **나머지 3개 조건은 미도래** - 런칭 표 실물 확인·성공의 정의 도달은 5단계 배포(미결)에 의존하고, 감수 결정 재검토 2건(Spring Boot 4.x · openai-java SDK)은 런칭 직전 항목임 | - |

---

## 다음 작업 순서

> 위에서부터 처리함. **각 항목의 세부·결정 사유·상태는 `docs/02_운영.md` 미결 표가 정본**이고 여기는 순서와 마커만 둠. 마커 범례도 그 문서에 있음.

- [x] 🟢 **① 미결 `[리뷰]` 6건 - 2026-07-28 전건 종결.** 결정과 근거는 `docs/02_운영.md` 미결 표
- [x] 🟢 **② 8단계 전제 정비** - 이름 8건 부재를 `클래스#메서드` 표기로 해소하고, 대조에서 드러난 완화 공백 3건(용량 상한 · 경계 격리 · 소유자 검증)을 테스트 5건으로 메움. 잴 수 없는 2건(R-8·R-11)은 완화 문구를 실제 수단으로 되돌림
- [ ] 🔴 **③ 5단계 배포 경로 개통** - 배포 대상·계정·비용이 S-3 사람 결정. 열리면 이관-3(AC-8 무절단) · 이관-4(AC-19 실기기) · 이관-5(예산 게이트) 3건이 함께 닫힘
- [ ] 🟠 **④ 실 키 확보 후 체크리스트 9항목** - `OPENAI_API_KEY`·공용 Vector Store가 준비된 뒤 M2 스파이크와 함께 순서대로 실행. 목록은 `docs/02_운영.md`
- [ ] 🟡 **⑤ 런칭 전 재검토 2건** - Spring Boot 4.x 업그레이드(3.5.16 OSS EOL) · 공식 openai-java SDK 전환. "개발 단계라 감수"의 근거가 런칭 시 소멸함

---

## 미결 · 운영

> **미결 표 · 실 키 확보 후 체크리스트 · 접근 권한 표 · 처리 이력 표의 정본은 `docs/02_운영.md`임.** 건수·상태를 여기 옮겨 적지 않음(S-6 규칙 4) - 무엇이 몇 건 열려 있는지는 그 표를 볼 것. 이 문서에서 볼 것은 「다음 작업 순서」의 처리 순서뿐임.

---

# 2단계 - 플랜 (사용자 승인 완료 2026-07-24)

## 원칙 (P)

- **P-1** : OpenAI 키·파일 접근은 백엔드 뒤에 둠. 프론트는 자체 API만 호출함(키 비노출)
- **P-2** : OpenAI 호출은 `OpenAiService` 인터페이스 경계 뒤로 격리하고, `APP_MODE=mock`로 키 없이 전 경로가 동작함. **기본값은 없음** - 모드를 명시하지 않으면 기동하지 않음(2026-07-28 결정, AC-18)
- **P-3** : 인가는 애플리케이션 코드가 전 경로에서 함(`requireOwned`). 남의 리소스는 403이 아니라 **404로 존재 은닉**
- **P-4** : 스키마의 소유자는 마이그레이션 SQL임. MyBatis 매퍼가 스키마를 바꾸지 않음
- **P-5** : 스트림 수명은 SSE emitter가 관장함(MVC async 타임아웃을 끄고 `sse-timeout`이 단독 결정)
- **P-6** : 출처는 DB(`citations` 테이블)에 영속화함. SSE로만 보내 재진입 시 소실시키지 않음(참조 구현 결함 개선)
- **P-7** : 파일 저장은 `LocalFileStorage` 인터페이스 뒤에 둠(S3 등으로 교체 가능). 경로는 서버가 소유하고 소유권 검증 후 서빙함
- **P-8** : 무자료 판정은 Mock/Real 공통 규칙임 - **판정 조건은 `citations.isEmpty()` 하나**이고, 표시는 텍스트 접두가 아니라 `done` 이벤트의 `noSource` 플래그·화면의 별도 표시로 함. **저장 텍스트는 스트리밍으로 내보낸 것과 정확히 같음**(라이브는 인용 0건을 스트림 종료 후에야 알아 접두를 토큰 앞에 붙일 수 없고, 붙이면 화면과 재조회가 어긋남)
  o **판정 조건과 표시 조건은 다름**(2026-07-28 추가) : 판정은 위 하나지만, **표시는 판정이 실제로 끝난 답변에만** 붙임. 중단으로 끝난 답변(`messages.stopped`)은 인용이 오기 전에 스트림이 닫혀 늘 0건이므로, 그대로 표시하면 "자료를 못 찾았다"가 100% 거짓이 됨. 화면 조건 = `complete` **그리고** `stopped` 아님 **그리고** 출처 0건
- **P-9** : 판단이 갈리면 이 원칙으로 되돌아옴. 원칙에 없으면 사람에게 물음
- **P-10** : 진행 단계(R-11)는 **실 스트림 이벤트 또는 서버가 실제로 통과한 코드 경계에만** 근거함. 타이머·인위 지연·추정으로 연출한 단계는 만들지 않고, 근거가 되는 이벤트·경계가 없으면 그 단계를 보내지 않음(S-1 위장 금지 · D-4 연장). 단계 라벨에는 검증되지 않은 수치(검색 건수·파일명 등)를 넣지 않고, **목업 단계는 목 데이터임이 드러나는 전용 문구를 씀**(라이브 라벨을 목업이 그대로 쓰지 않음). **최소 표시 시간은 연출이 아님** - 실제 도달한 단계를 도달 순서대로 최소 350ms 유지해 읽을 수 있게 하는 것은 허용하고(없는 단계 생성·순서 변경은 금지), 서버가 단계 사이에 인위 지연을 넣는 우회는 계속 금지함

## 결정 동인 (D)

- **D-1** : OpenAI 호출이 곧 비용임 → 레이트 리밋 + 목업 기본값이 비용 통제 수단
- **D-2** : 개인 데이터 격리 → 교차 접근 1건이 곧 사고. 소유권 검증이 최우선
- **D-3** : 모바일 우선·사진 업로드 → 카메라 촬영 → 업로드 → 비전 답변이 주 사용 흐름. 반응형 필수
- **D-4** : 출처 신뢰성 → 모든 답변에 출처 또는 "자료 없음"을 명시하는 것이 제품 정체성
- **D-5** : 목업으로 전 흐름을 시연할 수 있어야 함 → 실 연동 인터페이스를 먼저 완성

## Phase

| Phase | 선행 Phase | 함께 움직이는 항목 | 산출물 | 완료 판정 | 판정 수단 이 시점에 있는가 | AC |
|-------|-----------|--------------------|--------|-----------|----------------------------|-----|
| 0 기반·CI | 없음 | `.github` 워크플로 · TS/uuid 스파이크 · 운영(prod) 프로필 | 백엔드(SpringBoot+MyBatis, `mvnw`)·프론트(Vite+TS) 스캐폴드 · `/api/health` · **운영 프로필 정의(AC-18 대상)** · GitHub Actions(lint/test/build) | health 200 · CI 초록 · CI에서 OpenAI 호출 0 · TS7 스파이크 통과(실패 시 6.0.3) · prod에서 mock이면 기동 실패(AC-18) | 있음(health 응답·CI 로그) | AC-18 · TS7 스파이크 |
| 1 스키마·마이그레이션 | 0 | 없음 | 마이그레이션 SQL(users·conversations·messages·citations·attachments) + MyBatis 매퍼 골격 + **uuid TypeHandler 왕복 확인(uuid 스파이크)** | 빈 DB에 적용 → 기동 무오류 · 5테이블/인덱스 생성 · uuid insert/select 왕복 통과 | 있음(Testcontainers) | AC-20 |
| 2 인증·인가 | 1 | users 테이블 | 회원가입/로그인(BCrypt) · JWT 발급/검증 필터 · `requireOwned` 404 | 타 사용자 리소스 접근 전 경로 404 · 위조/만료 JWT 401 · 비번 평문 저장 0 | 있음(교차접근 전수 테스트) | AC-1~4 |
| 3 대화·파일 | 2 | LocalFileStorage | 대화 CRUD · 로컬 파일 저장/검증 · **서명 경로 토큰 기반 파일/이미지 서빙(M6)** · 고아 정리 | 업로드→서빙→삭제 흐름 · **서빙이 소유자만 접근(타인 404)** · 대화 삭제 시 첨부 정리 · 검증 위반 400/413 | 있음 | AC-11~13,22 |
| 4 OpenAI 경계·목업 | 3 | citations 테이블 | **`OpenAiService` 인터페이스 시그니처 확정(첨부 인자 · createVectorStore/uploadFile/attachFiles/deleteVectorStore · citation 반환)(M1)** · 기록형 Mock + Real 골격 · 비전 입력 구성 · 출처 반환/저장 · **무자료 판정 규칙 + Mock 무자료 트리거(M4)** | 시그니처 확정 · `APP_MODE=mock` 전 경로 통합 테스트 통과 · OpenAI 호출 0 · **Mock으로 무자료 0건 분기 재현 가능** · 무자료 접두 확인 | 있음(기록형 Mock) | AC-5~7,12,14,21 |
| 5 채팅 스트리밍·레이트리밋 | 4 | 없음 | SSE 스트리밍(meta→token→citations→done) · 토큰버킷 레이트리밋 | 긴 스트림 무절단 · 중단 시 상태 정책대로 · 초과 429 | 부분(무절단은 배포 후 M) | AC-8~10 |
| 6 프론트 전 화면(**PR 분해**) | 2·3·4·5 | 없음 | **6a** 로그인/회원가입 · **6b** 채팅(Sidebar·MessageList·Composer·CameraCapture·출처 각주 UI) · **6c** 마이페이지(이름/비번/테마) · **6d** 에러바운더리·접근성·반응형 (각 PR 단위)(M8) | 모바일 촬영→업로드→출처 달린 답변 전 흐름 · **대화 재조회 시 출처 유지(AC-7)** · 마이페이지 3종 반영 · 테마 적용. 실기기·HTTPS는 [이관-4] 배포 후 M | 부분(실기기·HTTPS는 배포 후) | AC-7,15~17,19 |
| 7 진행 단계 표시(R-11) | 5·6 | `OpenAiService.streamChat` 시그니처(Phase 4 M1 확정분) · SSE 계약(Phase 5) · `MessageList`의 `응답 생성 중…` 고정 문구(Phase 6b) | SSE `stage` 이벤트 추가(계약 : `meta` → `stage`* → `token`* → `citations` → `done`) · `OpenAiService.streamChat`에 단계 콜백 인자 추가 · Real 파서의 실 이벤트 → 단계 매핑 · Mock의 코드 경계 단계 · **라벨 문자열을 `OpenAiService` 구현이 소유(`ChatService`는 모드 비인지, P-2)** · 프론트 한 줄 진행 표시(`role="status"`+`aria-atomic="true"`로 log 영역과 분리 · 기존 고정 문구 제거 · **도달 단계를 큐에 담아 각 350ms 최소 표시 후 답변으로 교체** · 이력 비저장). **목업에 인위 지연을 넣어 체감을 만드는 우회는 금지(P-10)** | 목업 SSE 단계 목록이 **정확 목록·순서까지 일치**(자료 있음·무자료 두 분기 모두 동일한 `analyzing`→`searching`→`generating` 3종) · 첫 `token` 이후 `stage` 미전송이고 화면 표시가 답변 텍스트로 교체되며 문구 중복 0 · 중단(abort)·오류·스트림 절단 어느 경로로 끝나도 단계 표시 잔류 0 · 기존 채팅/SSE 회귀(AC-5·6·7·9·10·17) 초록 + 케이스 수 하한 실측 상향 · 스키마 변경 0(휘발) · 라이브 검색 단계는 실 `file_search_call` 이벤트에서만 발생 | 부분(라이브 단계는 [이관-6] · 첫 토큰 전 체감 구간은 [이관-7] - 둘 다 실키 대기) | AC-23·24 |

## AC

> `확인 PR` 열이 AC↔구현 대응의 **정본**임. 기입 규칙 : PR 절차 이전 구간(Phase 0~6)은 `PR로그 {행}행` + 그 커밋을 검사한 **CI run 번호**로 특정하고(`docs/03_PR로그.md`), 아직 판정되지 않은 AC는 이관 행 번호를 적음.

| AC | 내용(통과/실패가 갈리는 문장) | 소속 Phase | 검증 수단 | 확인 PR |
|----|-------------------------------|-----------|-----------|---------|
| AC-1 | 사용자 A의 토큰으로 B의 대화·메시지·첨부에 접근하면 전 경로에서 404임 | 2 | Testcontainers 교차접근 전수 테스트(CI 최우선) | PR로그 1행 - run 30058329481(Phase 3에서 충족) |
| AC-2 | 위조·만료·서명 불일치 JWT는 401임 | 2 | 테스트 | **PR로그 8행(2026-07-28 보강)** - 기존 근거(run 30057292049)의 테스트는 형식이 깨진 문자열만 보내 서명 검증에 도달하지 않았음. 다른 시크릿 서명·만료·payload 변조 3건을 추가하고 `verify`→`decode` 치환으로 3건 실패를 확인함 |
| AC-3 | 비인증 허용 경로(`/api/auth/**`·`/api/health`) 외 `/api/**`는 비인증 401임 | 2 | 테스트 | PR로그 1행 - run 30057292049 |
| AC-4 | 회원가입 시 비밀번호가 BCrypt 해시로 저장됨(평문·단순해시 아님) | 2 | 테스트(DB 값 검사) | PR로그 1행 - run 30057292049 |
| AC-5 | `APP_MODE=mock`로 전 경로 통합 테스트가 통과하고 OpenAI 호출이 0건임 | 4 | 통합 테스트 + Mock 호출 기록 | PR로그 1행 - run 30058676424 |
| AC-6 | 검색 결과 0건이면 `done` 이벤트에 `noSource=true`가 실리고 **정상 완료된 답변**의 화면에 무자료 표시가 뜨며 출처가 비어 있음(**저장 텍스트에는 접두를 넣지 않음** · **중단된 답변에는 표시하지 않음** - 판정을 못 마쳤으므로). **Mock에 무자료 트리거가 있어 CI에서 0건 분기를 재현함** | 4 | 테스트(Mock 무자료 트리거 + 중단 답변 미표시 2건) | PR로그 1행 - run 30058676424 · 중단 갈래는 11행 |
| AC-7 | 검색 결과가 있으면 각주 번호 + `citations` 저장, 대화 재조회 시 출처가 유지됨 | 4·6 | 테스트(저장) + 화면(재조회) | PR로그 1행 - 저장 run 30058676424 · 재조회 run 30059839453(이관-1 수신) |
| AC-8 | 5분 이상 지속되는 스트림이 잘리지 않음 | 5 | 수동(M, 배포 후) | **미판정 - 이관-3**(5단계 배포 후) |
| AC-9 | 스트림 강제 중단 시 받은 데까지 `complete`로 남고(부분 저장), 받은 것이 없으면 빈 메시지를 남기지 않음. 진짜 오류는 `error`로 구분해 남음 | 5 | 테스트 | **통과(2026-07-28)** - 정책을 하나로 확정하고 문안을 그에 맞게 정정함(사용자 결정). 근거 : `ChatStreamAbortTest` 4건(연결 종료 → `complete` · 무토큰 중단 → 미저장 · 상류 실패 → `error` · 저장 후 절단 시 중복 저장 0) + 훅 테스트 3건. 깨뜨림 확인 3회 |
| AC-10 | 레이트 리밋 초과 시 429임 | 5 | 테스트 | PR로그 1행 - run 30059099825 |
| AC-11 | 잘못된 MIME·매직바이트 불일치·타입별 용량 초과는 400, multipart 상한 초과는 413임 | 3 | 테스트 | **부분으로 정정(2026-07-28)** - run 30058329481이 검증한 것은 미지원 MIME 400·매직바이트 불일치 400 두 갈래뿐. **용량 초과 400과 multipart 413은 테스트 0건** |
| AC-12 | 대화 삭제 시 **DB 행(cascade)과 로컬 첨부 파일이 삭제**되고, 삭제에 실패해 남은 파일은 저장소 스캔으로 회수됨. OpenAI 정리는 **경계(`deleteResources`)를 통과하되 현재 인자가 비는 것이 정상임** - 이미지는 인라인 전송이라 OpenAI 파일 객체가 생기지 않고 문서 첨부는 받지 않으므로 지울 원격 자원이 없음 | 3·4 | 테스트(삭제 흐름 + 저장소 스캔 회수 3건 + Mock 호출 기록) | **통과(2026-07-28)** - 문안 정정(사용자 결정). 종전 문안은 **없는 기능**(OpenAI 파일 업로드)을 전제해 영원히 부분이었음. 파일 업로드가 실제로 생기면 이 AC를 다시 엶 |
| AC-13 | `message_id`가 null이고 cutoff 경과한 고아 첨부가 정리 호출로 회수됨 | 3 | 테스트 | PR로그 1행 - run 30058329481 |
| AC-14 | 이미지 첨부가 `OpenAiService` 호출 인자에 포함됨(비전 입력 구성) | 4 | Mock 호출 기록(CI) + 실키(M, 실연동) | **부분으로 정정(2026-07-28)** - Mock이 **첨부 개수**만 기록해 `fileType`이 document·null이어도 통과함. 실제 반영은 이관-2 |
| AC-15 | 마이페이지에서 이름·비밀번호·테마 변경이 저장·반영됨 | 6 | 테스트 + 화면 | PR로그 1행 - run 30060121928 |
| AC-16 | 테마 라이트/다크/시스템이 적용됨 | 6 | 화면 | PR로그 1행 - run 30060121928 |
| AC-17 | **렌더 예외는 에러 바운더리가** 복구 UI를 노출하고, **SSE 실패는 해당 메시지 자리에 인라인 오류로** 드러남. 어느 경로에서도 백지 화면이 안 됨 | 6 | 테스트 | **통과(2026-07-28)** - 문안 정정(사용자 결정). 두 경로가 서로 다른 장치로 처리되는데 하나로 적혀 있어 판정이 불가능했음. 근거 : `ErrorBoundary.test.tsx`(렌더 예외) + `MessageList.test.tsx`(인라인 오류 렌더) + `useChat.test.ts`(SSE 실패 시 상태 `error` · 사용자 메시지 잔존으로 백지 아님) |
| AC-18 | **실행 모드를 명시하지 않으면 기동이 실패**하고, 운영 프로필에서 `APP_MODE=mock`이면 기동이 실패함 | 0·4 | 테스트 | **통과(2026-07-28)** - 잠금 지점을 프로필에서 `app.mode` 필수화로 옮김(사용자 결정). 이전에는 `prod` 프로필을 켜는 경로가 저장소에 없어 가드가 생성되지 않을 수 있었음. 근거 : `AppModeGuardTest` 6건(미설정·오타·prod+mock 실패 / 명시 mock·prod+live·대소문자 기동) + 기본값 되돌림으로 실패 확인 |
| AC-19 | 모바일에서 촬영→업로드→답변 전 흐름이 동작함 | 6 | 실기기(M, 배포 후) | **미판정 - 이관-4**(HTTPS 배포 후) |
| AC-20 | 마이그레이션을 빈 DB에 적용하면 5테이블·인덱스가 전부 생성됨 | 1 | Testcontainers | **PR로그 8행(2026-07-28 보강)** - 기존 테스트는 표 5개만 셌음(인덱스를 지워도 초록). `pg_indexes` 5건 단언을 추가하고 `create index` 제거로 실패를 확인함 |
| AC-21 | 첨부(이미지/문서)가 있으면 `message` 텍스트가 비어도 전송이 허용됨(이미지 단독 질문) | 4 | 테스트(ChatRequest DTO 검증) | PR로그 1행 - run 30058676424 |
| AC-22 | 파일/이미지 서빙이 소유자만 접근 가능(서명 경로 토큰), 타인·무효 토큰은 404(존재 은닉, P-3) | 3 | 테스트 | PR로그 1행 - run 30058329481 |
| AC-23 | 화면 한 줄이 `stage` 이벤트 값으로 갱신되고(기존 고정 문구 `응답 생성 중…`은 제거되어 중복 표시 0건), 도달한 단계가 **각각 최소 표시 시간(350ms)** 동안 도달 순서대로 유지된 뒤 답변 텍스트로 교체됨. 중단(abort)·오류·스트림 절단 어느 경로로 끝나도 단계 표시가 남지 않고, 단계 교체가 원자적이라 스크린리더 낭독이 누적되지 않음 | 7 | 테스트(SSE에서 `stage`가 첫 `token`보다 앞 · 첫 `token` 이후 `stage` 0건) + 프론트 테스트(문구가 `stage` 값으로 바뀜 · 고정 문구 중복 0 · abort/오류/절단 3경로 잔류 0 · `role="status"`+`aria-atomic` 부착) + 훅 테스트(단계 큐가 순서대로 350ms씩 유지 후 비워짐 · 오류·중단 시 즉시 비움) + 화면 | PR로그 2행 - run 30230499675 · 최소 표시 시간은 3행 run 30231123362 |
| AC-24 | 표시되는 단계는 실 이벤트 또는 서버가 통과한 코드 경계에만 대응함 - 라이브에서 `file_search_call` 이벤트가 없으면 검색 단계가 0건이고, 목업은 라이브 라벨이 아닌 목업 전용 문구를 쓰며, 단계 라벨에 검색 건수·파일명 등 미검증 수치가 없음(P-10) | 7 | 테스트(목업 단계 목록·라벨 문구 정확 비교 · 라이브 매핑 단위 테스트에서 `file_search_call` 미발생 시 0건) + 실키(M, M2 스파이크) | PR로그 2행 - run 30230499675(목업·매핑) · **라이브 실이벤트는 이관-6** |

## 판정 이관

| 행 | 원래 단계 · Phase | 여기서 못 하는 이유 | 넘겨받는 단계 · Phase | 받는 쪽에 적었는가 |
|----|-----------|---------------------|----------------|---------------------|
| 이관-1 | 4 (AC-7 출처 저장) | 재조회 화면 확인은 프론트가 있어야 함 | 6 | 예 - Phase 6 완료 판정 문장에 "대화 재조회 시 출처 유지(AC-7)"로 명시 |
| 이관-2 | 4 (AC-14 비전 실제 반영) | 실 키·모델 응답이 있어야 내용 반영을 확인함 | 실연동(미결) | 예 - 미결 표 "실 API KEY"에 연결 |
| 이관-3 | 5 (AC-8 무절단) | 긴 스트림 무절단은 배포본에서만 실측 | 5단계 배포 | 예 - `docs/04_런칭.md` 「이관 수신」에 AC-8로 명시 · 게이트 체크 5단계 행 |
| 이관-4 | 6 (AC-19 실기기) | 카메라 getUserMedia는 HTTPS 필요 | 5단계 배포 후 | 예 - `docs/04_런칭.md` 「이관 수신」에 AC-19로 명시 · 런칭 표 HTTPS 도메인 행 |
| 이관-5 | 4단계 (예산 게이트) | 이 시점에 잴 번들이 없음 | 5단계 | 예 - 게이트 표 "미설정" |
| 이관-6 | 7 (AC-24 라이브 단계) | 실 이벤트 발생 순서·유무는 실 키·공용 Store로 호출해야 확인됨 | 실연동(M2 스파이크) | 예 - 미결 표 M2 행 범위에 등재 · Phase 7 판정 수단 "부분"으로 표기 |
| 이관-8 | 0-A (런타임 버전 대조) | 0-A 시점에는 CI가 없어 러너가 실제로 쓰는 버전을 잴 수 없었음 | 4단계 | 예 - 게이트 체크 4단계 근거 · `scripts/check-runtime-versions.sh`가 backend·frontend 잡에서 판정 |
| 이관-7 | 7 (R-11 체감 가치) | 목업은 첫 토큰까지가 사실상 즉시라(`OpenAiMockService`의 지연은 토큰 **사이**에만 걸림) **실제 대기 구간**의 해소를 목업으로 판정 못 함. 프론트 최소 표시 시간으로 단계 가독성은 확보했지만, 그것이 재는 것은 표시 시간이지 실제 대기 시간이 아님 | 실연동(M2 스파이크) | 예 - 미결 표 M2 행 ④에 체감 구간 실측 등재 · 개요 성공의 정의에 예외 문구 · Phase 7 판정 수단 "부분" |

## 실측

| 판정에 쓴 숫자 | 실측 출처 | 실측 시점 |
|----------------|-----------|-----------|
| 케이스 수 하한 N | Phase 0 스모크(1)에서 시작, 각 Phase 완료 시 실측 상향. Phase 7 실측 = 백엔드 42 · 프론트 16 → 7단계 리뷰 보강 후 백엔드 49 → 라이브 정합성 4건 반영 후 백엔드 51 · 프론트 18 → 대기 결정 4건 반영 후 백엔드 62 · 프론트 21 → 재리뷰 반영 후 백엔드 64 · 프론트 26 → 리스크 표 정비(완화 테스트 5건 신설) 후 **백엔드 69 · 프론트 26**(2026-07-28) | 각 Phase(최신 2026-07-28) |
| 번들 크기 예산 | 최초 배포 실측 × 여유율 | 5단계(현재 미설정) |
| SSE 타임아웃 10분 | 정책값(참조 구현 착지값, 실측 아님) | Phase 5에서 확정 |
| 레이트리밋 chat 20/분·files 30/분 | 정책값(참조 구현 착지값) | Phase 5에서 확정 |
| 진행 단계 최소 표시 시간 350ms | 정책값(가독 시간, 실측 아님). 사용자 승인 2026-07-27 | Phase 7 |
| 게이트 소요 - `backend (test + build + 케이스 수 하한)` | 원격 CI run 30321202240 잡 소요 `47s` | 2026-07-28 |
| 게이트 소요 - `frontend (lint + test + build + 케이스 수 하한)` | 원격 CI run 30321202240 잡 소요 `21s` | 2026-07-28 |
| 게이트 소요 - `docs (참조 실재 + 계획서 필수 표)` | 원격 CI run 30321202240 잡 소요 `5s` | 2026-07-28 |
| 게이트 소요 - `secrets (시크릿 스캔)` | 원격 CI run 30321202240 잡 소요 `9s` | 2026-07-28 |
| 게이트 소요 - `deps (백엔드 의존성 취약점)` | 원격 CI run 30321202240 잡 소요 `27s` | 2026-07-28 |
| 게이트 소요 - `deps (프론트 의존성 취약점)` | 원격 CI run 30321202240 잡 소요 `16s` | 2026-07-28 |
| 프론트 번들 gzip 82.27KB | `vite build` 출력 `dist/assets/index-*.js 260.72 kB │ gzip: 82.27 kB` | 2026-07-28 |
| 프론트 번들 gzip 82.40KB(재리뷰 라운드 3 반영 후 - **현재 착지값**) | `vite build` 출력 `dist/assets/index-*.js 261.08 kB │ gzip: 82.40 kB` | 2026-07-28 |
| 백엔드 케이스 수 64 · 프론트 26(하한 래칫 상향) | `./mvnw -B clean verify` 출력 `Tests run: 64, Failures: 0, Errors: 0, Skipped: 0` · `vitest` `Tests 26 passed (26)` | 2026-07-28 |
| 백엔드 케이스 수 69(리스크 표 정비 - **현재 착지값**) | `./mvnw -B clean verify` 출력 `Tests run: 69, Failures: 0, Errors: 0, Skipped: 0` · `scripts/check-case-floor.sh backend` 출력 `실측 69 / 하한 64` → 하한 69로 상향 | 2026-07-28 |
| 백엔드 케이스 수 82(미결 4건 반영 - **현재 착지값**) | `./mvnw -B clean verify` 출력 `Tests run: 82, Failures: 0, Errors: 0, Skipped: 0` → 하한 76 → 82 상향 | 2026-07-28 |
| 백엔드 케이스 수 76(back-pressure) | `./mvnw -B clean verify` 출력 `Tests run: 76, Failures: 0, Errors: 0, Skipped: 0` → 하한 69 → 76 상향 | 2026-07-28 |
| 프론트 케이스 수 29(대화 전환 abort - **현재 착지값**) | `vitest` 출력 `Tests 29 passed (29)` → 하한 26 → 29 상향 | 2026-07-28 |
| 프론트 번들 gzip 82.41KB(대화 전환 abort 반영 후 - **현재 착지값**) | `vite build` 출력 `dist/assets/index-*.js 261.15 kB │ gzip: 82.41 kB` | 2026-07-28 |

## 리스크

> 이 표의 `R-n`은 **리스크** 번호임(포함 범위 표의 요구 `R-n`과 별개 계열).
>
> **테스트 이름은 `클래스#메서드`로 적음** - 클래스명만으로는 그 안에 해당 케이스가 있는지 확인되지 않음.

| 리스크 | 영향 | 완화(테스트로) | 테스트 이름 |
|--------|------|----------------|-------------|
| R-1 스트리밍 중 프로세스 종료 시 답변 유실 | 답변 소실 | 중단·오류 시 부분 저장 정책 + 테스트 | `ChatStreamAbortTest`(4건) |
| R-2 소유권 검증 누락으로 교차 접근 | 데이터 유출(D-2) | 교차 접근 전수 테스트 게이트 - 대화(조회·삭제·개명·목록) + 파일(삭제) | `ConversationCrossAccessTest#owner_can_access_stranger_gets_404` · `#stranger_cannot_rename_404` · `FileFlowTest#stranger_cannot_delete_others_file_404` |
| R-3 `APP_MODE=mock` 운영 방치로 가짜 응답 | 무증상 결함 | 모드 미설정·prod+mock 기동 실패 가드 | `AppModeGuardTest`(6건) |
| R-4 무자료인데 출처 표기(또는 반대) | 신뢰성 훼손(D-4) | Mock/Real 공통 무자료 판정 테스트 | `ChatFlowTest#unknown_topic_streams_no_source`(Mock) · `OpenAiRealStageMappingTest`(Real - `noSource` 참/거짓 각 1건) |
| R-5 악성/과대 파일 업로드 | 저장 남용 | MIME+매직바이트+용량 테스트 | `FileFlowTest#upload_unsupported_type_400` · `#upload_bad_magic_400` · `#upload_oversized_image_400` |
| R-6 레이트리밋 단일 인스턴스 가정 | 확장 시 한도 곱 | **완화 수단이 배포 형태에 의존 → 미결 표로 이관**(단일 인스턴스 전제 명시) | 해당 없음 - 미결(배포 형태 확정 시) |
| R-7 고아 첨부 누적 | 저장 비용 | 고아 회수 테스트 - **행 기준**(미연결 첨부)과 **저장소 기준**(행이 사라진 뒤 남은 파일) 두 갈래를 함께 잼 | `FileFlowTest#orphan_cleanup_removes_unlinked` · `#orphan_cleanup_keeps_fresh_upload_even_with_future_cutoff` · `#unreferenced_old_file_is_removed_by_scan` · `#referenced_file_is_kept_by_scan` · `#fresh_unreferenced_file_is_kept_by_scan` |
| R-8 TS7 툴 호환 불확실 | 빌드/lint 실패 | **Phase 0에서 해소됨**(TS 6.0.2 채택으로 착지, 폴백 6.0.3 미사용). 상시 완화는 테스트가 아니라 **CI `frontend` 잡**(`oxlint` + `tsc` + `vite build`)이 매 PR에서 툴 호환을 재는 것임 | 해당 없음 - CI `frontend` 잡이 담당(스파이크 전용 테스트는 만든 적 없음) |
| R-9 Vector Stores 베타 API 변경 | 실연동 실패 | `OpenAiService` 경계 격리(Mock 주입 시 Real 미호출) | `OpenAiServiceBoundaryTest#mock_mode_registers_only_mock_implementation` · `#mock_mode_has_no_real_service_bean` |
| R-10 stateless JWT 하 이미지/파일 서빙 인증 우회 | 타 사용자 파일 노출 | 서명 경로 토큰 + 소유자 검증 테스트 | `FileFlowTest#serve_without_token_404` · `#serve_with_tampered_token_404`(서명) · `#valid_token_of_other_user_404`(소유자) |
| R-11 문서 첨부 → Vector Store 수명주기 누락(생성/첨부/정리) | 비용 누수·기능 공백 | **생성·첨부 갈래는 소멸함** - 문서(PDF) 첨부를 받지 않기로 해(2026-07-28 R-2 축소) 대화 전용 Store 를 만드는 경로 자체가 없음. 남은 것은 정리 호출뿐이며 기록형 Mock 으로 잼. **인자가 늘 비어 있는 문제는 AC-12 미결로 열려 있음** | `ChatFlowTest#delete_conversation_calls_openai_cleanup`(정리 갈래만) |
| R-12 로컬 디스크 휘발성(배포가 ephemeral FS면 파일 소실) | 첨부 유실 | S3 교체 경계(P-7) + 배포 시 영속 볼륨 확인(미결) | 해당 없음 - 미결(배포 형태 확정 시) |
| R-13 진행 단계 라벨이 실제 동작과 어긋남(연출된 진행) | 신뢰성 훼손(D-4·P-10) - "검색 중"을 봤는데 검색이 없었던 상태 | 단계를 실 이벤트/코드 경계에만 연결하고, 이벤트 미발생 시 단계 0건 · **목업은 목업 전용 라벨**임을 테스트로 고정(라벨 문구까지 정확 비교) | `ChatStageHonestyTest`(4건) |
| R-14 SSE 계약에 이벤트를 추가하면서 기존 수신부 파싱이 깨짐 | 채팅 전면 실패 | 미지 이벤트를 무시하는 수신부 동작 유지 + meta→…→done 순서 계약 테스트 | `ChatStreamContractTest`(1건) |
| R-15 동시 스트림이 풀을 넘겨 요청이 무응답으로 대기 | 신뢰성 훼손(D-4) - 실패가 아니라 **무반응**으로 보이고 최대 SSE 타임아웃(10분)까지 끌림. 2026-07-28 CI 에서 실제 발생 | 큐에 쌓지 않고 진입 시점에 429로 거절하고, 거절이 `prepare` 앞이라 고아 사용자 메시지를 만들지 않음 | `ChatConcurrencyLimiterTest`(5건) · `ChatBackPressureTest#rejected_by_backpressure_returns_429` · `#rejected_request_leaves_no_orphan_user_message` |

## 진행 단계(R-11) 이벤트 매핑

> 표시 방식 승인값(2026-07-27) : **한 줄 교체형**(현재 단계 하나만 표시 · 첫 토큰 시 소멸) · **이력 비저장**(휘발, 재조회 시 답변·출처만) · **목업 전용 라벨**(목 데이터임이 화면에 드러남). 이 표가 단계 원천의 단일 출처임(P-10).

| 단계 키 | 화면 라벨(라이브) | 화면 라벨(목업) | 라이브 원천(실 스트림 이벤트) | 목업 원천(통과하는 코드 경계) |
|---------|-------------------|-----------------|-------------------------------|-------------------------------|
| `analyzing` | 질문 분석 중 | 질문 분석 중(목업) | prepare 통과 후 스트림 개시 시점(`meta` 직후) - 라이브·목업 공통 서버 경계 | prepare 통과 후 스트림 개시 시점(`meta` 직후) |
| `searching` | 참조 문서 검색 중 | 목업 코퍼스 조회 중 | `response.file_search_call.in_progress` · `response.file_search_call.searching` | 코퍼스 키워드 매칭 직전 |
| `generating` | 답변 작성 중 | 답변 작성 중(목업) | 첫 `response.output_text.delta` | 첫 `onToken` 직전 |

- **라이브 이벤트명 근거** : OpenAI Responses API 스트리밍 이벤트 공식 문서(확인일 2026-07-27, URL은 `docs/01_기술선택.md` 채택 표). **발생 순서·무검색 시 미발생은 실호출로 확인 전임**(이관-6 · M2)
- **`analyzing`은 서버 경계에서 발생시킴** : 라이브도 `response.created`·`response.in_progress`를 원천으로 쓰지 않음. 요청 본문(이미지 base64 포함) 업로드와 TTFB가 `meta` 이후에 걸려 첫 이벤트까지 표시가 비고, 목업(`meta` 직후 즉시)과도 어긋나기 때문임. 두 이벤트는 단계를 새로 만들지 않는 **도달 확인 신호**로만 두고, 도달 시점 확인은 이관-6 범위에 남김
- **라벨 소유자 · 발행 주체** : 단계 키는 공통이고 **라벨 문자열은 `OpenAiService` 구현이 소유함**(Mock/Real 각자). `ChatService`는 모드를 알지 않고 구현에서 받은 라벨을 그대로 실어 보냄(P-2 경계 유지). `analyzing`은 `ChatService`가 `meta` 전송 직후(= `streamChat` 진입 시점)에 발행하고, `searching`·`generating`은 구현이 자기 경계에서 콜백으로 올림
- **4번째 단계(`citing`, 검색 결과 정리 중) 미채택** : R-11 승인 범위가 3단계이고, 라이브에서 검색 결과 0건에도 `response.file_search_call.completed`가 오는지 미확인이라 채택하면 무자료 분기에서 라이브·목업 표시가 어긋남(P-8 공통 규칙 위반). 필요해지면 이관-6 확인 후 별건 승인으로 추가함
- **범위 한정** : SSE 개시 이후 구간만 다룸. 첨부 업로드(프론트) · prepare 검증 · 레이트리밋 429(스트림 개시 전 동기 응답)에는 단계 표시가 없는 것이 정상 동작임. 업로드 구간 표시가 필요하면 별도 요구로 등재함
- **표시 규칙(프론트)** : 도달한 단계를 큐에 담아 각 **350ms 최소 표시** 후 다음으로 넘기고, 큐가 마르면 답변 텍스트로 교체함. 중단·오류·절단에서는 큐를 즉시 버림(잔류 0). 목업은 단계가 같은 순간에 도착해 이 장치 없이는 표시 시간이 0임(연출이 아니라 가독 시간 확보, P-10)
- **전송 규칙** : 단계는 라벨 문자열만 보냄(수치 미포함, P-10) · 단조 진행이라 되돌아가지 않음 · 첫 `token` 이후에는 `stage`를 보내지 않음(재검색 라운드도 드롭) · 갱신이 없으면 마지막 라벨을 그대로 유지하고 추가 연출을 만들지 않음 · 프론트는 모르는 단계 키를 무시함(리스크 R-14)
- **목업 단계의 성격** : `APP_MODE=mock`의 단계는 목 응답의 일부임(P-2 범위). 라이브 전용 단계를 목업이 따라 하지 않고, 목업이 실제로 지나는 코드 경계에서만 발생시키며, 라벨도 목업 전용 문구를 씀(P-10 · 목업 코퍼스 매칭은 사용자 문자열 키워드 비교라 문서 검색이 아님)

---

# 4단계 - CI 게이트

> 구현은 `.github/workflows/ci.yml` 과 `scripts/` 아래 검사 스크립트가 정본임.

| 게이트 | 기준 | 조이는 방향 | 구현 상태(2026-07-28) | 판정 주체 |
|--------|------|-------------|------------------------|-----------|
| lint(프론트 oxlint) | 경고/오류 0 | 래칫 | **구현** | `frontend` 잡 |
| test | 백엔드 `mvnw verify` · 프론트 `vitest run` 성공 | - | **구현** | `backend` · `frontend` 잡 |
| 케이스 수 하한 | **값의 정본은 `scripts/case-floors.env`** - 문서에 숫자를 옮겨 적지 않음(S-6 규칙 4) | 래칫 | **구현** - 검사는 `scripts/check-case-floor.sh`(skip 제외 계수) | `backend` · `frontend` 잡 |
| build | 백엔드 `mvnw verify` · 프론트 `tsc -b && vite build` 성공 | - | **구현** | `backend` · `frontend` 잡 |
| 문서 참조 실재 + 위키링크 | MISSING 0 · STALE 0 · 위키링크 0 | 래칫 | **구현** - 2026-07-28 재작성(백틱 경로까지 수집 · **0건 수집을 실패로 단언**) | `docs` 잡 |
| 계획서 필수 표 검사 | 필수 8종 표 헤더 존재 + 빈 칸 0 + 폴더 구조 표가 실제 트리와 일치 | 래칫 | **구현** - `scripts/check-plan-tables.sh` | `docs` 잡 |
| 시크릿 스캔 | 검출 0(커밋된 이력 전체) | 래칫 - 예외는 사유 + 만료일 | **구현** - gitleaks 8.30.1 바이너리(버전·SHA256 고정), `gitleaks git` | `secrets` 잡 |
| 의존성 취약점 | high 이상 0건 | 래칫 - 예외는 사유 + 재검토일 | **구현** - 백엔드 Trivy(action 0.36.0) · 프론트 `npm audit --audit-level=high` | `deps-backend` · `deps-frontend` 잡 |
| 런타임 버전 대조(이관-8) | `.nvmrc` ↔ `node -v` · `pom.xml java.version` ↔ `java -version` 메이저 일치 | - | **구현** - `scripts/check-runtime-versions.sh` | `backend` · `frontend` 잡 |
| 예산(번들 크기) | 최초 배포 실측 × 여유율 | 재설정 | **미설정** - 5단계에서 확정(이관-5) | - |

게이트 표를 읽는 법 :

- **여유율은 아직 정하지 않음** - 배포 실측이 없어 기준을 세울 수 없음(미설정이 정상, 가이드 4단계)
- **의존성 취약점·`npm audit`는 네트워크 의존 게이트임** - 도달 실패 시 빨간불이 되므로, 레지스트리·DB 도달 실패와 실제 검출을 로그로 구분해 읽을 것. 도달 실패가 반복되면 advisory 전환이 아니라 **원인을 먼저 볼 것**(시크릿·의존성은 어느 규모에서도 생략 대상이 아님, S-5)
- **케이스 수 하한을 내릴 때는 "무엇이 줄었는지"를 `docs/02_운영.md` 처리 이력 표에 함께 적음** - 검사 대상이 줄어 케이스가 준 것과 테스트를 지운 것은 숫자만으로 구분되지 않음
- **`backend` 잡은 Testcontainers가 실 PostgreSQL·ryuk 이미지를 내려받으므로 네트워크에 의존함** - 레지스트리 도달 실패와 테스트 단언 실패는 다른 사건이며 로그의 `Retrying pull for image` 유무로 갈림. 도달 실패로 재실행했으면 **그 사실을 `docs/02_운영.md` 처리 이력에 남김**(재실행이 조용한 관행이 되지 않게 함)
- **`.linkignore` 예외는 죽으면 실패함** - 예외에 적힌 경로가 실재하게 되면 STALE로 빨간불이 되어 방치되지 않음
- **참조 착지값** : 프론트 번들 gzip 60.67KB(2026-07-27, react-router-dom 7.18.1 시점) → 82.27KB(2026-07-28, react-router 8.3.0 전환 후) → **82.40KB**(2026-07-28, 결정 4건 + 재리뷰 라운드 1~3 반영 후). 60→82 증가분은 라우터 전환으로 봄(그 구간의 유일한 코드 변경). 5단계 예산 기준을 세울 때 쓰는 **착지값은 최신값 82.40KB**이며 `docs/04_런칭.md` 이관-5도 같은 값을 가리킴

- **케이스 수 하한** : 각 Phase 완료 시 **실측값으로만** 올림(예상값·목표값 금지). **현재값을 이 문서에 적지 않음** - 종전에 `case-floors.env`·CI 게이트 표·실측 표·비고 4곳에 흩어져 두 번 어긋났고, 기계 게이트는 `env` 하나만 보므로 나머지는 전부 갈라질 자리였음. 아래 실측 표는 **그때그때의 실측 기록**이지 현재 하한이 아님
- **게이트별 소요 시간은 실측 표에 행으로 있음** - 원격 CI 잡별 소요를 인용함(run 30321202240)
