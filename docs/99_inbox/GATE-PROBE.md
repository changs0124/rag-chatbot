# CI 게이트 차단 확인용 임시 문서 (#166)

이 파일은 **일부러 깨진 문서 참조**를 담고 있다. `static` 잡의 `check-doc-refs.sh` 가
실패해야 정상이다. required status checks 가 merge 를 실제로 막는지 확인한 뒤 바로 삭제한다.

- 존재하지 않는 경로를 일부러 가리킨다 : `docs/99_inbox/THIS-FILE-DOES-NOT-EXIST.md`
