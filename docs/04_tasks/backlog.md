# Backlog

코드베이스에서 확인된 미결 항목. 우선순위는 초안이므로 필요에 맞게 조정한다.

## 높은 우선순위

- [ ] OpenAI 실 연동 마무리 — `APP_MODE=live` 경로(`OpenAiRealService`)를 실 응답과 대조. 무자료 임계·완료 이벤트명이 실물과 맞는지 확인
- [ ] OpenAI 공용 Vector Store 구축 후 `OPENAI_VECTOR_STORE_ID` 주입 — 없으면 답변은 되지만 출처가 늘 0건
## 보통

- [ ] **Spring Boot 4.1 의 OSS 지원은 2027-07 까지다** — #48 로 4.1.1 에 올렸다. 3.5 때처럼 기한이 지난 뒤에 알아채지 않도록 그 전에 다시 판단한다. 근거 : <https://www.danvega.dev/blog/spring-boot-end-of-life>
- [ ] 단일 인스턴스 전제 해소 — 레이트리밋·동시 스트림 카운터가 인메모리라 수평 확장 시 실효 한도가 인스턴스 수만큼 곱해짐
- [ ] **`postgresql` 드라이버 버전 하한을 주장하는 것이 없다** — #48 에서 `<postgresql.version>` 수동 고정을 뺐다(4.1.1 BOM 관리분이 같은 값이라는 근거). **현재 해석값 42.7.13 이라 노출은 없다**(`dependency:list` 확인).
  - 없는 것 : `backend/pom.xml` 에 enforcer 규칙, `scripts/check-doc-versions.sh` 검사표의 PostgreSQL 항목
  - `ci.yml` 의 Trivy 는 pom 의 BOM 해석에 의존하고, `list-all-pkgs` 는 로그만 남길 뿐 해석 실패를 실패시키지 않는다
  - 앞으로 BOM 이 낮은 버전을 관리하게 되면 CVE-2026-54291 대역으로 조용히 내려갈 수 있다
  - 고르는 것 : enforcer 규칙 / 검사표 확장 / 재고정. **근거가 한 건뿐일 때 게이트를 만들지 않는다**(#55 기준)는 판단으로 지금은 기록만 둔다
- [ ] 첨부 저장소 S3 이전 검토 (`FileStorage` 구현 교체)

## 낮음

- [x] 배포 절차 문서화 — **정본은 [backend](../02_architecture/backend.md) 「배포」다**(#127 · #129).
  호스트 선정 근거 · 절차 · 환경변수 · `scripts/deploy.sh` 옵션 · 메모리 상한 · 방화벽 규칙이 그쪽에 있다.
  `docs/01_specs/` 로 옮기지 않는다 — 배포는 명세가 아니라 아키텍처 결정이고, 같은 사실을 두 곳에 적지 않는다
- [ ] **런칭** — 절차는 위에서 끝났으나 **실제로 수행된 적이 없다.** 남은 것 :
  - `TUNNEL_TOKEN`(Cloudflare 터널 생성) · `OPENAI_API_KEY` · `OPENAI_VECTOR_STORE_ID` 확보
  - `bash scripts/deploy.sh` 로 실배포 — **이 스크립트는 통짜로 실행된 적이 없다.** `--dry-run` 을 먼저 볼 것
  - Vercel 프로젝트에 `VITE_API_BASE_URL`(터널 도메인) 설정 후 **재배포**.
    현재 프로덕션 번들에 `http://localhost:8080` 이 박혀 있다(배포된 `/assets/index-*.js` 에서 확인)
  - 백엔드 `ALLOWED_ORIGINS` 에 Vercel 도메인 — 빠뜨리면 CORS 로 전 API 차단

---

## 사용자 문의가 예상되는 미구현 항목 (기록용)

현재 요구 범위 **밖**이라 구현하지 않았지만, 실제 사용자가 붙으면 문의가 올 것들이다.
착수 결정 전까지는 기록만 둔다 — 판단이 필요할 때 여기서부터 보면 된다.

| 항목 | 지금 상태 | 문의 형태 | 착수 시 결정할 것 |
|------|-----------|-----------|-------------------|
| 비밀번호 **셀프** 재설정 | 없음. 관리자 발급(`POST /api/admin/users/{id}/password-reset`)이 유일한 경로라 **관리자가 없으면 못 푼다** | "비밀번호를 잊었어요" | 이메일 발송 수단(SMTP·외부 서비스) 선택 |
| 이메일 인증 | 없음. 도메인 제한(`ALLOWED_EMAIL_DOMAINS`)은 걸리지만 **그 도메인 안의 오타 주소는 그대로 통과** | 관리자가 오타 주소로 발급한 뒤 "임시 비밀번호가 안 왔어요" | 위와 같은 발송 수단이 전제 |
| 오류 관측 | `ErrorBoundary` 에 연결 지점만 있고 콘솔 기록뿐 | "화면이 하얘졌어요" 를 재현할 근거가 없음 | 관측 도구 선택 |
| 다중 인스턴스 | 레이트리밋·동시 스트림이 인메모리(단일 인스턴스 전제) | 스케일아웃 후 "상한이 안 걸려요" | 공유 저장소(Redis 등) 도입 여부 |
| 첨부 영속성 | 로컬 디스크. 볼륨 없으면 재시작마다 소실 | "어제 올린 사진이 사라졌어요" | 호스트 볼륨 vs `FileStorage` S3 구현 |
| 파일 URL 수명 | 서명 토큰 TTL 15분 | 페이지를 오래 열어둔 뒤 새로 그려지는 이미지가 깨질 수 있음 | TTL 연장 vs 만료 시 재발급 |
