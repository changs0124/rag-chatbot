# 02_운영 - rag-chatbot

> 시크릿은 **값이 아니라 보관 위치만** 적음(S-1).

## 상태 마커 범례

> **이 표가 마커 범례의 정본임**(S-6 규칙 5). 다른 문서는 여기를 가리키기만 하고 범례를 복제하지 않음. **상태의 정본도 아래 미결 표 하나**이며, 다른 문서는 마커와 항목명만 참조하고 상태 문장을 옮겨 적지 않음.

| 색 | 뜻 | 다음 행동 |
|----|----|-----------|
| 🔴 | 판단이 필요함 | 무엇이 맞는지 **사람이 정해야 함**. 선택지를 갖춰도 대신 정할 수 없음 |
| 🟠 | 실행 권한 · 채널이 필요함 | 무엇을 할지는 정해졌고 **손이 닿지 않아 못 하는 것**. 계정 · 도메인 · 결제가 있어야 함 |
| ⚪ | 선행이 풀리면 개발이 진행 | 사람 몫이 아님. 🔴 🟠 가 닫히면 자동 착수 |
| 🟡 | 보류 | 지금 판단하지 않기로 함. 조건이 바뀌면 다시 봄 |
| 🟢 | 🟢 종결 | 처리 완료. 기록으로만 남김 |

## 접근 권한 표

| 대상 | 계정 소유자 | 접근 있는 사람 | 시크릿 보관 위치 | 없으면 막히는 게이트 |
|------|-------------|----------------|------------------|----------------------|
| GitHub 저장소 `changs0124/rag-chatbot` | changs0124 | changs0124 | 해당 없음 - 시크릿 아님 | CI 6잡 전체(원격 실행 주체 - 잡 목록은 `docs/03_PR로그.md` 절차 메모) |
| OpenAI API KEY | changs0124(결제 주체) | 미발급 - 없음 | **미정 - 발급 시 결정**. 저장소에 값 비커밋 원칙은 확정(`backend/.env` 로컬만, `.env.example`엔 키 이름만) | live 기동 · M2 스파이크 · 이관-2·6·7 |
| OpenAI 공용 Vector Store ID | changs0124 | 미구축 - 없음 | 상동(`OPENAI_VECTOR_STORE_ID`) | live RAG 검색 · AC-7 실연동 |
| JWT 서명 시크릿 | changs0124 | changs0124(로컬만) | 로컬 `backend/.env`(git 비추적) · CI는 테스트 기본값 사용 | 인증 전 경로 로컬 기동 |
| 로컬 PostgreSQL(Docker `rag-pg`) | changs0124 | changs0124(로컬만) | 로컬 환경변수 | 로컬 실동작 확인(CI는 Testcontainers라 무관) |
| 배포 대상(프론트 호스팅 + API 서버) | 미정 - 5단계에서 결정 | - | 미정 | 5단계 · 이관-3·4 · AC-8 · AC-19 · 예산 게이트 |

---

## 미결 (사람 결정을 기다리는 항목)

> **이 표가 미결 상태의 정본임.** 다른 문서는 항목명과 마커만 참조하고 상태 문장을 복제하지 않음(S-6 규칙 4). 마커 뜻은 위 범례.

| 항목 | 누가 | 없으면 무엇이 멈추는가 | 진행을 막는가 | 상태 | 대기 사유 | 갱신일 |
|------|------|------------------------|----------------|------|-----------|--------|
| 실 OpenAI API KEY | changs0124(결제 주체) | live 모드 실동작 · 이관-2·6·7 판정 | 아니오 - 목업으로 진행 | 🟠 대기 | 결제/발급 미정. **확보 시 할 일은 「실 키 확보 후 체크리스트」 9항목 참조**(2026-07-27 작성) | 2026-07-27 |
| 실 Vector Store 구축 + 전처리 데이터 업로드 | changs0124 | live RAG 검색 | 아니오 | 🟠 대기 | 데이터 준비 후 OpenAI 플랫폼에서 업로드 예정 | 2026-07-24 |
| `openai-rag-bot` 참조 구현 여부 | changs0124 | 참조 착지값 활용 여부 | 아니오 | 🟢 종결 - 강한 참조로 확정(신규 유지) | - | 2026-07-24 |
| 파일 저장소 위치 | changs0124 | 첨부 저장 경로 | 아니오 | 🟢 종결 - 로컬/서버 디스크(S3 교체 가능 경계) | - | 2026-07-24 |
| 성능 목표치 | changs0124 | 비기능 성능 판정 | 아니오 | 🔴 대기 | 5단계 배포 실측 후 확정 | 2026-07-24 |
| 접근성 요구 수준 | changs0124 | 비기능 접근성 판정 | 아니오 | 🔴 대기 | 사용자 확인 대기 | 2026-07-24 |
| DB 종류 · 인증 방식 확정 | changs0124(승인) | 기술 스택 확정 | 예 - 1단계 | 🟢 종결 - PostgreSQL + JWT 승인 | - | 2026-07-24 |
| Spring Boot 4.x 업그레이드 재검토 | changs0124 | 없음 - 3.5.16으로 진행 | 아니오 | 🟡 대기 | 3.5.16 OSS EOL. **런칭(대외 공개) 전 반드시 재검토** - "개발 단계라 감수"의 근거가 런칭 시 소멸함. MyBatis SB4 스타터 성숙도 함께 판단 | 2026-07-24 |
| 공식 openai-java SDK 전환 재검토 | changs0124 | 없음 - RestClient로 진행 | 아니오 | 🟡 대기 | Vector Stores API GA 확정 또는 RestClient DTO/SSE 수작업 부담 과다 시 4.45.0으로 전환 | 2026-07-24 |
| 배포 대상 선정(프론트 호스팅 + API 서버) | changs0124 | 실기기·HTTPS 검증(AC-19), 리스크 R-6·R-12 완화 | 아니오 - 목업/로컬 개발은 진행 | 🔴 대기 | 5단계 배포 시 결정 | 2026-07-24 |
| 레이트리밋 저장소 · 영속 스토리지(리스크 R-6·R-12) | changs0124 | 확장 시 한도 보장 · 첨부 영속성 | 아니오 | ⚪ 대기 | 배포 형태 확정에 의존(단일 인스턴스 전제 시 로컬로 충분) | 2026-07-24 |
| **[리뷰] AC-18 가드 활성화 방식** | changs0124(승인) | 운영에서 목업 응답이 나가는 것을 구조적으로 막는 방법 확정 | 아니오 - 목업 개발은 진행 | 🟢 종결 | (a)안 채택. `app.mode` 기본값 제거로 **미설정이면 기동 실패**, mock은 명시 옵트인. `ProdModeGuard` → `AppModeGuard` 개명하고 AC-18 문안·P-2·R-3 갱신 | 2026-07-28 |
| **[리뷰] 무자료 접두 표기 방식(P-8)** | changs0124(승인) | 라이브에서 화면과 재조회 내용이 어긋나는 문제 해소 | 아니오 | 🟢 종결 | (a)안 채택(플래그 + 화면 표시). R-4·P-8·AC-6 문언 정정 완료 | 2026-07-28 |
| **[리뷰] 문서(PDF) 첨부 처리 + OpenAI 파일 업로드 미구현** | changs0124(승인) | R-2 입력 4종 중 문서 첨부의 실효성 · AC-12 실체 | 아니오 | 🟢 종결 | (b)안 채택(업로드 거부 + 명시). R-2 범위 축소로 등재. **OpenAI 파일 업로드는 계속 미구현**이며 AC-12 문안 정정이 잔여 | 2026-07-28 |
| **[리뷰] 대화 삭제의 트랜잭션 경계** | changs0124(승인) | 삭제 정합성 - 커밋 실패 시 파일만 사라지거나 그 반대 | 아니오 | 🟢 종결 | DB 먼저 삭제 후 파일·원격 정리로 순서 교체. **정리 실패분은 행이 cascade 로 이미 사라져 고아 회수가 찾지 못함**(재리뷰에서 드러남) - 경고 로그가 유일한 흔적이며 회수 방법은 아래 「삭제 후 정리 실패분 회수」 행으로 분리 등재 | 2026-07-28 |
| **[리뷰] 재조회 응답의 첨부 포함 여부** | changs0124(승인) | 새로고침 시 이미지 소실(AC-7·15 인접) | 아니오 | 🟢 종결 | `MessageResponse`에 첨부 추가 + 조회 시점 재서명 | 2026-07-28 |
| **[리뷰] 스트림 중단 시 상태 정책 통일** | changs0124(승인) | AC-9 판정 재개 | 아니오 | 🟢 종결 | 부분 저장 + `complete`로 통일. SSE 전송 실패(클라이언트 종료)를 전용 예외로 분리해 진짜 오류와 구분하고, 받은 것이 없으면 서버·프론트 모두 빈 메시지를 남기지 않음. AC-9 문안 정정 + `ChatStreamAbortTest` 4건 | 2026-07-28 |
| **[리뷰] AC-17 문안 정정** | changs0124(승인) | AC-17 판정 재개 | 아니오 | 🟢 종결 | "렌더 예외는 바운더리 · SSE 실패는 인라인 오류 표시 · 공통으로 백지 화면 0"으로 갈라 적고 인라인 경로 훅 테스트 추가 | 2026-07-28 |
| **[리뷰] 잔여분 - 보안·정합성 5건** | changs0124 | 인증 강화 · 고아 회수 실기동 | 아니오 | 🟢 종결 | 이메일 소문자 정규화(+`lower(email)` 유일 인덱스) · 계정별 로그인 시도 상한 · 비밀번호 변경 시각 기준선으로 이전 발급 JWT 거부 · 고아 회수 스케줄러 기동 + cutoff 클램프 · 파일 서빙 `nosniff` 테스트 고정 | 2026-07-28 |
| **[리뷰] 대화 전환 시 이전 스트림 abort** | changs0124 | 대화를 바꿔도 이전 스트림이 계속 돌고 단계 줄이 잔류함 | 아니오 | 🟢 종결 | 떠날 때 `abortActiveStream`(abort + 단계 비움)을 부르도록 함. 중단 후 처리는 정해진 정책 그대로임(부분 저장 + `complete`)이라 새 결정 0건. **삭제 갈래를 함께 넣음** - 스트리밍 중인 활성 대화를 지우는 것도 전환이며, 안 끊으면 사라진 대화에 대고 스트림이 계속 돎 | 2026-07-28 |
| **[리뷰] 채팅 스레드 풀 back-pressure** | changs0124 | 동시 스트림이 풀을 넘으면 큐가 무한히 자람 | 아니오 | 🟢 종결 | **진입 시점 동시성 상한** 채택(사용자 결정 2026-07-28). 큐에 쌓지 않고 429로 거절하며, 거절은 **`prepare` 앞**에서 함(뒤면 답변 없는 사용자 메시지가 남음). 상한은 **사용자별 1 + 전역 N**이고 전역 N은 `chatExecutor` 풀 크기와 같은 프로퍼티라 갈릴 자리가 없음. SSE 타임아웃 10분은 AC-8과 충돌하므로 손대지 않음 | 2026-07-28 |
| **[리뷰] 레이트리밋 카운터 회수** | changs0124 | 사용자·계정이 늘수록 맵이 단조 증가함 | 아니오 | 🔴 대기 | 2026-07-28 잔여분 분리 등재. 지난 분(minute)의 창을 정리하는 주기 작업 또는 만료 캐시로 교체. 로그인 상한이 추가돼 키 종류가 둘로 늘었음 | 2026-07-28 |
| **[리뷰] AC-12 문안 정정** | changs0124(승인) | AC-12 판정 - 지금 문안은 없는 기능(OpenAI 파일 업로드)을 전제함 | 아니오 | 🔴 대기 | 이미지는 인라인(base64) 전송이라 OpenAI 파일 자체가 만들어지지 않아 `deleteResources` 인자가 늘 비어 있음. 문안을 현 구조("대화 삭제 시 DB·로컬 파일·Vector Store 정리")에 맞게 고칠지, 파일 업로드를 실제로 구현할지 결정 필요. **종전에는 PDF 첨부 행 본문에만 매달려 있어 담당 없는 잔여였음**(2026-07-28 재리뷰 지적으로 분리) | 2026-07-28 |
| **[리뷰] 삭제 후 정리 실패분 회수** | changs0124 | 대화 삭제 시 파일 삭제가 실패하면 디스크에 영구 잔류 | 아니오 | 🔴 대기 | 2026-07-28 재리뷰로 드러남. cascade 로 `attachments` 행이 먼저 사라져 `findOrphans`(행 기준)가 볼 수 없음. 파일 삭제를 cascade 앞으로 옮기거나(트랜잭션 경계 결정을 되돌리는 셈) 실패 경로를 별도 정리 큐로 남기는 두 갈래 | 2026-07-28 |
| **[리뷰] 토큰 저장 위치(localStorage) 결정 기록** | changs0124(승인) | XSS 시 토큰 탈취 노출 범위 | 아니오 | 🔴 대기 | 2026-07-28 잔여분 분리 등재. 현행은 localStorage. 감수하고 남길지, httpOnly 쿠키로 옮길지 결정하고 **결정 자체를 문서에 남길 것**(지금은 기록이 없어 선택한 적 없는 기본값처럼 보임) | 2026-07-28 |
| 실 Vector Store 응답 스키마 확인 스파이크(M2) | changs0124 | 출처·file_search DTO 실연동 정합 · 무자료 임계 · 라이브 스트림 무자료 접두 표기 방식 | 아니오 - Mock 우선 진행 | ⚪ 대기 | `OpenAiRealService` 구조 구현 완료(2026-07-24). 실 API KEY·공용 Store 확보 시 실호출로 응답 스키마(annotations/file_search_call.results·완료 이벤트명)·무자료 판정 검증·미세조정. **7단계 재리뷰 이월 3건 함께 처리** : ① 인용 상관을 filename→`file_id` 기준으로(동명이인 파일 오귀속 방지) ② mime을 확장자 추측 대신 검증된 업로드 타입에서 도출 ③ 대용량 이미지 base64 메모리(동시성 증가 시 스트리밍/총량 캡). **추가(2026-07-27, 이관-6·7)** : ④ 진행 단계(R-11) 원천 이벤트 실호출 확인 - `file_search_call.*` 발생 순서와 무검색 시 미발생 여부 · **검색 결과 0건에도 `.completed`가 오는지**(무자료 분기 정합 · `citing` 재검토 조건) · `response.in_progress` 도달 시점 · **첫 토큰 전 체감 구간 실측**(이관-7 - 목업으로는 판정 불가) | 2026-07-27 |

## 실 키 확보 후 체크리스트 (M2 스파이크 · 이관-2·6·7)

> 🟠 선행 = 위 미결 표 「실 OpenAI API KEY」 · 「실 Vector Store 구축」. 열리면 **이 순서로** 실행하고 각 항목의 통과 근거(명령 출력 · 응답 본문 · 화면)를 `docs/00_계획.md` 진행 로그에 남김. 지금은 전부 **미실행**이며, 실행 전까지 어떤 항목도 통과로 적지 않음(S-1).

| # | 확인할 것 | 통과 기준 | 걸린 문서 항목 |
|---|-----------|-----------|----------------|
| 1 | `APP_MODE=live` 기동 | 키가 빈 상태면 부팅 실패(fail-fast) · 키가 있으면 정상 기동 | P-2 · `OpenAiRealService` 가드 |
| 2 | 공용 Store 연결 | `OPENAI_VECTOR_STORE_ID`로 `file_search`가 붙고 실 문서에서 답이 나옴 | 미결 "실 Vector Store 구축" |
| 3 | 출처 스키마 | `annotations.file_citation` 구조와 `file_search_call.results` 스니펫이 `CitationData` 매핑과 맞음 | AC-7 · M2 |
| 4 | 무자료 판정 | 검색 0건일 때 규정 접두가 붙고 출처가 빔. **검색 결과 0건에도 `response.file_search_call.completed`가 오는지** 확인 | AC-6 · P-8 · `citing` 재검토 조건 |
| 5 | 단계 원천 순서 | `response.file_search_call.in_progress`/`.searching`가 첫 `output_text.delta` 앞에 오고, 검색이 없으면 검색 단계가 0건임 | AC-24 · 이관-6 |
| 6 | 체감 구간 실측 | `meta`부터 첫 토큰까지 실제 지연을 재고, 최소 표시 시간 350ms가 그 지연보다 짧은지 확인(짧으면 표시가 실제를 가리지 않음) | 이관-7 · 실측 표 |
| 7 | 비전 실제 반영 | 이미지 첨부가 답변 내용에 실제로 반영됨 | AC-14 · 이관-2 |
| 8 | 완료 이벤트명 | 스트림이 `response.completed`로 끝남(다르면 현재 코드가 예외를 던지므로 즉시 드러남) | `OpenAiRealService.consumeStream` |
| 9 | 이월 3건 처리 | ① 인용 상관을 `file_id` 기준으로 ② mime을 검증된 업로드 타입에서 도출 ③ 대용량 이미지 base64 메모리 캡 | 7단계 재리뷰 이월 |

---

## 처리 이력 (기준을 못 채웠을 때의 조치)

> **기준을 낮춘 적이 있으면 그 사실이 여기 남아야 함** - 남지 않으면 다음 사람은 원래 그 기준이었다고 읽음. **다른 문서에서 이 표를 가리킬 때는 행 번호가 아니라 항목명으로 참조함**(행이 늘면 번호가 밀려 실제로 두 번 어긋났음). 2026-07-24·27 행은 2026-07-28 소급 등재분임.

| 일자 | 항목 | 유형 | 조치 | 낮춘 기준 | 대체 방어가 못 막는 것 |
|------|------|------|------|-----------|------------------------|
| 2026-07-24 | 4단계 CI 게이트 3종(시크릿 스캔 · 의존성 취약점 · 계획서 필수 표 검사) 미구현 | 환경·플랜 제약 | 핵심 4종(lint · test · build · 문서 참조)으로 CI 초록을 먼저 확보하고 3종을 "후속"으로 미룸. **2026-07-28 전건 구현으로 종결** | 4단계 통과 조건 "게이트 전체가 스켈레톤에서 한 바퀴" · S-5 "시크릿 스캔 · 의존성 취약점은 어느 규모에서도 생략 대상이 아님" | 구현 전까지 : 시크릿이 diff에 들어가도 검출 0 · high 취약점이 있어도 초록 · 계획서 표의 빈 칸을 사람이 놓치면 그대로 통과. **게이트 체크 표 4단계 판정을 "통과"로 적은 것 자체가 이 누락을 덮었음**(2026-07-28 "부분 통과"로 정정 → 같은 날 게이트 전건 구현 후 원격 6잡 초록(run 30321202240 · 최종 30321437472) 확인하고 "통과"로 재판정) |
| 2026-07-24 | 4단계 케이스 수 하한이 게이트에 실재하지 않음(문서에만 숫자) | 환경·플랜 제약 | "Phase별 실측 시 추가"로 미룸. **2026-07-28 `check-case-floor.sh`로 구현 종결** | 4단계 통과 조건 "케이스 수 하한이 게이트에 들어 있음" | 구현 전까지 : 테스트를 지워도 CI가 통과함(래칫이 스모크에 고정) |
| 2026-07-24 | 5단계 배포 경로 개통 전에 6단계 구현 착수 | 환경·플랜 제약(배포 대상이 S-3 미결) | 목업 모드로 6단계를 병행하고 게이트 체크 5단계 행을 "대기"로 열어 둠 | 가이드 단계 순서(`… → 5 → 6 → …`) | 예산 게이트가 미설정으로 남음 · AC-8(무절단) · AC-19(실기기)가 미판정으로 적체 · **배포 빌더 환경과 CI 환경의 차이를 늦게 만남**(가이드 5단계 경고) |
| 2026-07-24 | 1-A 산출물(폴더 구조 표 · 구조 출처 표) 미작성 | 기준 자체가 없었음 - 착수 시점 가이드 v2.4에 1-A 장이 없었고 v3.0(2026-07-27)에서 신설됨 | 2026-07-28 소급 작성(`docs/01_기술선택.md`) + 게이트 체크 표에 1-A 행 추가 | 없음 - 소급으로 원복함 | 소급 전까지 기본값 이탈이 문서에 한 건도 없었고, 4단계 계획서 필수 표 검사가 대조할 표 자체가 없었음 |
| 2026-07-24 | 미결 표를 `docs/02_운영.md`가 아니라 `docs/00_계획.md`에 둠 | 기준 자체가 틀렸음(산출물 지도와 실제 배치 불일치) | 2026-07-28 본 문서로 이관하고 `docs/00_계획.md`에는 포인터만 남김 | 없음 - 배치만 바로잡음 | 이관 전까지 `backend/.env.example` · `docs/04_런칭.md`가 실재하지 않는 `docs/02_운영.md`를 가리켰고, **참조 실재 검사는 백틱 표기라 이를 잡지 못했음**(게이트 사각지대) |
| 2026-07-27 | Phase 0~6 구간에 PR 절차 미적용(`main` 직접 push) | 환경·플랜 제약 | 커밋별 push 트리거 CI 초록으로 갈음하고, 2026-07-27부터 PR 흐름 적용. 소급은 히스토리 재작성이 필요해 하지 않음 | 6단계 "각 PR이 단독으로 CI를 통과함" → 커밋별 초록으로 갈음 | PR 단위 검사가 아니었음 · **Phase 크기 판정(단독 통과/쪼갬)을 PR 단위로 잰 적이 없음** - 해당 Phase 행은 `03_PR로그.md`에서 `측정 불가 - 사유`로 남음 |
| 2026-07-28 | **문서 참조 실재 검사가 링크를 한 건도 수집하지 못한 채 "정상"으로 통과하고 있었음** | 기준 자체가 틀렸음(게이트가 아무것도 재지 않음) | 스크립트 재작성 - 마크다운 링크 형태 확장(앵커·타이틀·꺾쇠·참조형·`src=`) + **백틱으로 적은 저장소 경로도 수집** + **0건 수집을 실패로 단언** + `.linkignore` 예외(예외가 죽으면 STALE로 실패). 수집 53건 · 실재 검사 50건으로 바뀜 | 없음 - 기준을 되레 올림 | 수정 전까지 : 문서가 없는 파일을 가리켜도 초록이었음(실제로 `docs/02_운영.md`·`backend/.env` 참조가 그 상태였음). **백틱이 아닌 코드블록 안의 경로는 여전히 검사 대상이 아님** |
| 2026-07-28 | 프론트 라우터가 채택 표(8.3.0)와 다른 7.18.1로 설치돼 있었고 그 범위가 CVE에 걸림(GHSA-qwww-vcr4-c8h2, `>=7.12.0 <8.3.0`) | 구현이 기준에 못 미침 | 같은 작업에서 고침 - `react-router-dom` 제거 후 `react-router` 8.3.0 설치(v8에서 패키지가 통합됨), 임포트 7파일 수정. lint 0 · 테스트 16/16 · build 성공 · `npm audit` 0건 | 없음 - 문서 기준에 코드를 맞춤 | **번들 gzip이 60.67KB → 82.27KB로 늘어남**(이 구간 유일한 코드 변경이라 전환 영향으로 봄). 5단계 예산 기준을 세울 때 이 값이 착지값이 됨 |
| 2026-07-28 | 백엔드 pgjdbc가 채택 표(42.7.13)와 달리 Boot BOM 관리분 42.7.11로 해석돼 CVE-2026-54291(HIGH)에 걸림 | 구현이 기준에 못 미침 | 같은 작업에서 고침 - `pom.xml`에 `<postgresql.version>42.7.13</postgresql.version>` 고정(Maven Central 최신 릴리스 확인 2026-07-28). `./mvnw -B verify` EXIT=0(42건) · Trivy 0건 | 없음 - 문서 기준에 코드를 맞춤 | **BOM이 올려 주는 다른 라이브러리도 같은 방식으로 문서와 어긋날 수 있음** - 이번엔 Trivy가 잡았지만, high 미만으로 어긋나면 게이트가 잡지 않음 |
| 2026-07-28 | 7단계 리뷰가 "Phase마다 1회"에 못 미침(7단계 행 4개 · Phase 8개) | 기준 자체가 없었음 - 가이드 v3.8(2026-07-28) 신설 요건 | 잔여 과제로 등재하고 **8단계 이전에 Phase별 리뷰 패스를 채움**. 기존 4개 라운드는 `docs/01_기술선택.md` 리뷰 라운드 표에 그대로 둠 | 7단계 통과 조건 "리뷰 라운드 표의 7단계 행 수가 Phase 수 이상" | Phase 0~6 개별 구현이 별도 리뷰 패스를 거치지 않았음 - 2026-07-24 재리뷰 1회가 그 구간 전체를 갈음했으므로 **Phase 단위로 놓친 지적이 있는지 확인된 바 없음** |
| 2026-07-28 | **[종결]** 리스크 표의 테스트 이름 12개 중 **9개가 저장소에 부재**(당시 기준. 같은 날 중단 정책 반영으로 `ChatStreamAbortTest`가 실재하게 되어 **현재 잔여 8개**이며, 아래 목록은 등재 시점 그대로 둠) (`ChatStreamAbortTest` · `CrossAccessOwnershipTest` · `NoSourcePrefixTest` · `FileValidationTest` · `OrphanCleanupTest` · `OpenAiServiceBoundaryTest` · `FileServingAuthTest` · `VectorStoreLifecycleMockTest` · `TypeScriptToolingSpike`) | 기준 자체가 틀렸음 - 완화는 실제로 구현됐으나 다른 테스트 클래스(`ChatFlowTest` · `ConversationCrossAccessTest` · `FileFlowTest` 등)에 흡수돼 이름만 낡음 | 이번 PR 범위 밖이라 **잔여 과제로 등재**하고 게이트 체크 8단계 행에 불통과로 명시. **2026-07-28 후속 작업에서 종결** - 아래 「리스크 표 완화 공백 3건」 행 참조 | 없음 - 낮춘 것이 아니라 미정비 상태를 드러냄 | **완화가 실제로 걸려 있는지 이름만으로는 확인 불가** - 8단계 통과 조건("리스크 표의 모든 테스트 이름이 저장소에 실재")이 등재 시점에는 불통과였음. `ProdMockGuardTest`는 오기라 같은 날 `ProdModeGuardTest`로 정정함 |
| 2026-07-28 | **리스크 표 완화 공백 3건** - 이름 부재 8건을 정비하려고 저장소와 대조하니, **이름만 낡은 것이 아니라 완화 자체가 없던 것**이 나옴 : ① R-5 용량 상한(10MB)이 `FileService`에만 있고 케이스 0건 ② R-10 이 서명 검증(무토큰·변조)만 재고 **소유자 대조(`findByIdAndUser`)는 안 쟀음** ③ R-9 「Mock 주입 시 Real 미호출」을 재는 케이스가 아예 없었음 | 기준 자체가 틀렸음(완화가 걸려 있다고 표만 적고 있었음) | 테스트 5건 신설 - `FileFlowTest#upload_oversized_image_400` · `#valid_token_of_other_user_404` · `#stranger_cannot_delete_others_file_404` · `OpenAiServiceBoundaryTest` 2건. 백엔드 64 → **69**, 하한도 69로 상향. 표의 이름 표기를 **`클래스#메서드`로 바꿈**(클래스명만으로는 그 안에 케이스가 있는지 확인되지 않아 같은 낡음이 반복됨) | 없음 - 없던 완화를 채우고 기준을 올림 | **표에 완화가 적혀 있다는 것이 완화가 걸려 있다는 뜻이 아니었음.** 이번엔 이름 대조를 계기로 드러났을 뿐이고, **표와 저장소를 대조하는 기계 게이트는 없음** - 다음에 완화가 비어도 사람이 대조하기 전까지는 초록임. 또 `#valid_token_of_other_user_404`는 응답을 `Map`으로 받으면 관문이 뚫렸을 때 단언 대신 컨버터 오류가 나 실패가 안 읽혀 `byte[]`로 바꿈 |
| 2026-07-28 | **경계 격리 테스트(R-9)의 실패 형태가 단언이 아님** - `OpenAiRealService`의 `@ConditionalOnProperty` 값을 `live`→`mock`으로 되돌려 확인했더니, 빨간불은 나지만 **컨텍스트 적재 실패**로 남(Real 생성자의 blank api-key fail-fast가 먼저 걸림) | 기준 자체가 없었음(그 조건에서 잴 수 있는 형태가 아님) | 그대로 두고 **실패 형태를 여기 적음**. 테스트 컨텍스트에는 `app.openai.api-key`가 없어 Real이 뜨는 순간 무조건 예외이며, 이 상태에서는 "빈이 둘"을 단언으로 잡을 수 없음 | 없음 - 잴 수 없는 형태를 잰 척하지 않음 | **이 테스트가 실제로 잡는 것은 「키가 있는 환경에서 Real이 조용히 Mock을 밀어내는 경우」임** - 키가 없으면 fail-fast가 먼저 잡으므로 테스트가 없어도 빨간불임. 다음 사람이 "어차피 fail-fast가 잡으니 필요 없다"고 읽고 지울 수 있는데, **키가 주입된 CI·운영 환경에서는 fail-fast가 안 걸려 이 테스트만 남음** |
| 2026-07-28 | PR #3 최종 확인에서 `backend` 잡이 5분 14초 만에 실패 - 원인은 코드가 아니라 러너에서 `testcontainers/ryuk:0.12.0` 이미지 내려받기가 반복 실패한 것(`Retrying pull for image` 6회 후 타임아웃 → `NoClassDefFound: AbstractPgIntegrationTest` 36건으로 파급) | 환경·플랜 제약 | 실패 잡만 재실행 → **39초 만에 초록**. 코드·설정 변경 0건이며, 같은 기준을 그대로 다시 통과시킴 | 없음 - 기준을 낮추지 않음 | **재실행이 습관이 되면 진짜 실패를 덮음.** `backend` 잡이 빨간불이면 재실행 전에 로그에서 `Retrying pull for image` 유무를 먼저 볼 것 - 없으면 그것은 단언 실패이므로 재실행 대상이 아님. 이미지 캐시·레지스트리 미러는 도입하지 않음(발생 1회 대비 비용 과다)이라 **같은 플레이크는 또 날 수 있음** |
| 2026-07-28 | **새로 만든 계획서 필수 표 검사 자체에 통과 구멍 3건** - ① 정렬 콜론 구분선(`\|:---\|`)이면 구분선이 데이터 행으로 계수돼 판정표 데이터 행을 전부 지워도 초록 ② 폴더 구조 표 토큰을 표 전체에서 무앵커로 찾아 사유 행을 지워도 다른 행 본문에 그 문자열만 있으면 초록 ③ 1-A 폴더 구조 표에는 빈 칸 검사가 없어 이탈 사유 칸이 비어도 초록 | 기준 자체가 틀렸음(게이트가 검사 대상 소실을 못 봄) | 별도 레인 리뷰(`gate-reviewer`)에서 지적 → 파싱 규약 재작성(끝 파이프 정규화 · 백틱 내부 파이프 가림 · 정렬 구분선 인식 · 열 수 대조 · 1-A 표에도 빈 칸/열 수 검사 · 토큰을 첫 칸에서만 매칭). 5형태 전부 깨뜨려 확인 | 없음 - 기준을 되레 올림 | 수정 전까지 : **표를 통째로 비우는 조작이 게이트를 통과했음.** 사람 리뷰가 아니었으면 드러나지 않았을 형태이며, 실제로 이 PR의 표 2건(진행 로그 · PR 표)이 열 수 어긋난 채 초록으로 머지 직전까지 갔음 |
| 2026-07-28 | **7단계 Phase별 리뷰에서 판정 표가 실제 검증 범위보다 넓게 적힌 AC 6건 발견** - AC-9(근거로 적힌 `ChatStreamAbortTest`가 저장소에 없음) · AC-11(용량 400·multipart 413 미테스트) · AC-12(Mock 호출 횟수만 봐 no-op를 검증) · AC-14(첨부 개수만 단언) · AC-17(SSE 실패는 에러 바운더리에 구조적으로 도달 불가) · AC-18(`prod` 프로필을 켜는 경로가 저장소에 없음) | 기준 자체가 틀렸음 | 6건을 **부분/미판정으로 정정**하고, 실자원 없이 닫을 수 있던 2건은 즉시 수정 - AC-2(서명·만료·변조 3건 추가, `verify`→`decode` 치환으로 3건 실패 확인) · AC-20(`pg_indexes` 5건 단언 추가, `create index` 제거로 실패 확인) · AC-18 컨텍스트 테스트 4종(프로필 오타로 3건 실패 확인) | **낮춘 것이 아니라 과대 기재를 실제 범위로 되돌림** | 정정 전까지 : 여섯 AC가 통과로 읽혀 **다음 단계가 잘못된 전제 위에 쌓이는 상태**였음. 특히 AC-12는 미구현 기능(OpenAI 파일 업로드)을 초록으로 덮고 있었음 |
| 2026-07-28 | 7단계 Phase별 리뷰에서 **제품 결함 후보 다수**(전 Phase REVISE · 결정 변경 51건) - 대화 삭제가 커밋 전에 파일·원격 삭제를 수행 · 무자료 접두가 스트림에는 없고 저장 텍스트에만 있어 화면/DB 불일치 · 문서(PDF) 첨부가 모델에 전달되지 않음 · 재조회 응답에 첨부가 없어 이미지 소실 · 중단 상태 정책이 서버(error)와 프론트(complete)에서 상충 · 대화 전환 시 이전 스트림이 중단되지 않고 단계 줄이 잔류 | 구현이 기준에 못 미침 | **AC 변경·기능 범위가 걸린 항목이라 임의로 고치지 않고 미결 표에 결정 항목으로 등재**(S-3 : 승인된 범위·AC의 변경은 사람이 결정) | 없음 - 판정을 정정했고 기준은 그대로 둠 | **지금 상태로는 라이브에서 드러날 결함들임** - 목업만으로는 무자료 접두 불일치·문서 첨부 미전달이 보이지 않음 |
| 2026-07-28 | 7단계 리뷰 지적 중 **라이브 정합성 4건** 처리(사용자 결정 후) - 무자료 접두 · 문서 첨부 · 삭제 트랜잭션 경계 · 재조회 첨부 | 구현이 기준에 못 미침 | 네 건 모두 수정하고 관련 요구·원칙 문언을 함께 정정(R-2 축소 · R-4·P-8·AC-6 표기 방식 · 범위 변경 이력 2행). 역방향 확인 2회(PDF 재허용 · 재조회 첨부 제거) | 없음 - 기준을 실제 동작에 맞추고 결함을 없앰 | **AC-12 는 여전히 부분임** - 이미지는 인라인 전송이라 OpenAI 파일 업로드 자체가 없어 `deleteResources` 인자가 계속 비어 있음. AC-12 문안을 현 구조에 맞게 고치는 것은 잔여 결정 항목으로 남음 · 삭제 후 파일 정리 실패는 이제 경고 로그로만 남고 고아 회수에 의존함(고아 정리 스케줄러는 아직 미기동 - 같은 날 후속 작업에서 기동함) |
| 2026-07-28 | **AC-18 가드가 프로필에 묶여 사실상 비어 있던 자물쇠** - `@Profile("prod")`가 있어야 빈이 생기는데 저장소 어디에도 그 프로필을 켜는 경로가 없었음 | 기준 자체가 틀렸음(가드가 켜질 보장이 없었음) | 사용자 결정으로 잠금 지점을 **`app.mode` 필수화**로 옮김 - 기본값 `mock` 제거, 미설정·오타면 기동 실패. `AppModeGuard`로 개명하고 AC-18 문안을 실제 잠금에 맞춰 정정. 기본값을 되돌려 실패 확인 | 없음 - 기준을 되레 올림 | **테스트·로컬 실행에서 `APP_MODE=mock`을 반드시 줘야 함**(안 주면 기동 실패). 통합 테스트는 `AbstractPgIntegrationTest`가 주입하지만, **새 컨텍스트 테스트를 그 베이스 밖에서 만들면 같은 함정을 다시 만남** |
| 2026-07-28 | **중단과 오류를 서버·프론트가 다르게 기록** - 서버 `error` / 프론트 `complete` | 구현이 기준에 못 미침(정책 부재) | 사용자 결정으로 부분 저장 + `complete`로 통일. SSE 전송 실패를 전용 예외로 분리해 상류 오류와 구분하고, 받은 것이 없으면 양쪽 다 빈 메시지를 남기지 않음 | 없음 - 미정이던 정책을 정함 | **"클라이언트 연결 종료 = 사용자 중단"으로 간주함** - 네트워크 절단도 같은 취급이라, 회선이 끊긴 사용자에게는 답변이 중간에서 끝난 채 정상 메시지로 남음. 그 구분은 서버가 알 수 없음 |
| 2026-07-28 | **로컬 케이스 수 실측을 6건 부풀려 재고 하한을 잘못 올림**(실측 68로 적었으나 실제 62) - 개명 전 `ProdModeGuardTest`의 surefire XML이 `target/`에 남아 계속 계수됨 | 기준 자체가 틀렸음(측정이 틀림) | 원격 CI(클린 체크아웃)가 `실측 62 / 하한 68`로 잡아 실패시킴 → `clean verify`로 재측정해 62 확인, `scripts/case-floors.env`와 실측 표를 62로 정정하고 `check-case-floor.sh` 머리에 "로컬은 clean 후 측정" 주의를 적음. **이때 CI 게이트 표의 하한 표기(51·18)를 함께 못 고쳤고, 같은 날 문서 재리뷰가 그 누락을 잡아 64·24로 맞춤** | 없음 - 잘못 올린 하한을 실측으로 되돌림 | **로컬 실측은 `clean` 없이는 신뢰할 수 없음**(지운·개명한 클래스가 계속 잡힘). 스크립트가 리포트의 신선도를 검사하지는 않으므로, **같은 실수를 로컬에서는 또 할 수 있고 원격이 잡아 주는 구조임**. 또 **하한이 `case-floors.env`·CI 게이트 표·실측 표·비고 4곳에 흩어져 있어** 한 곳만 고치면 문서가 거짓이 됨 - 기계 게이트는 `env` 하나만 보므로 나머지는 사람이 맞춰야 함 |
| 2026-07-28 | **비밀번호 변경이 본인 세션까지 끊던 회귀** - 변경 시각 이전 토큰을 전부 무효화하면서 새 토큰을 주지 않아, "변경했습니다"를 띄운 직후부터 본인의 모든 요청이 401 이었음 | 구현이 기준에 못 미침(같은 PR 안에서 만든 회귀) | 별도 레인 코드 재리뷰가 **차단**으로 잡음 → 변경 응답에 새 토큰을 실어 주고(200 + `AuthResponse`) 프론트가 교체하도록 고침. 백엔드·프론트 테스트 각 1건으로 잠그고 깨뜨려 확인 | 없음 - 망가뜨린 것을 되돌림 | **AC-15·16 으로 이미 동작하던 기능을 새 보안 기능이 조용히 깨뜨렸음.** 기존 테스트(`update_password_changes_login`)는 로그인 재시도만 봐서 이 경로를 못 잡았음 - **보안 강화가 인접 기능을 깨뜨리는지는 그 기능의 기존 테스트로는 안 잡힘** |
| 2026-07-28 | **중단된 답변에 "자료 없음"이 거짓으로 붙음** - 중단을 `complete`로 저장하기로 한 결정과 무자료 배너 조건(`complete` + 출처 0건)이 맞물려 발생. 인용은 스트림 끝에 오므로 중단 시점에는 늘 0건이라 100% 오표시였음 | 구현이 기준에 못 미침(결정 간 상호작용) | 별도 레인 재리뷰가 잡음 → `messages.stopped` 컬럼(V3)을 두고 배너만 억제함. **상태는 사용자 결정대로 `complete` 유지**이며 화면에 중단 전용 표시도 만들지 않음 | 없음 - 거짓 표시를 없앰 | **결정 하나가 다른 화면 규칙을 무효로 만들 수 있음** - 두 PR 어느 쪽 테스트도 이 조합을 밟지 않았고, 사람 리뷰가 아니었으면 라이브에서야 드러났을 형태임. D-4(신뢰성)에 직접 걸리는 오표시였음 |
| 2026-07-28 | **역방향 확인을 원복하면서 아직 커밋하지 않은 수정까지 함께 날림** - `git checkout -- frontend/src/hooks/useChat.ts` 가 일부러 넣은 파손과 **그 직전에 만든 라운드 3 가드 수정**을 한꺼번에 되돌렸고, 그 상태로 커밋·push 됨 | 절차가 틀렸음 | 원격 CI 프론트 잡이 실패시켜 잡음 → 가드를 다시 넣고 재검증. **로컬에서는 못 봤음** - 원복 직후 돌린 것이 `check-case-floor.sh`(케이스 **수**만 셈)였고 실패 수를 보지 않았음 | 없음 - 잃은 수정을 되돌려 놓음 | **역방향 확인의 원복 수단으로 `git checkout` 을 쓰려면 그 앞에 커밋이 있어야 함.** 커밋 전 상태에서 쓰면 검증 대상 자체가 사라지며, 그때 **케이스 수 게이트는 통과함**(수는 그대로이고 결과만 빨간불이라). 같은 함정을 또 밟을 수 있으므로 원복 뒤에는 반드시 **테스트 결과(실패 수)** 를 확인할 것 |
| 2026-07-28 | **회귀 테스트가 없는 수정 2건** - ① 레이트리밋 카운터 `volatile`(JMM 가시성) ② 무효화 기준선을 앱 시계로 통일(시계 스큐) | 기준 자체가 없었음(테스트로 잴 수 없는 종류) | 그대로 두되 **테스트가 없다는 사실을 여기 적음**. ①은 가시성이라 결정적 재현이 불가능하고, ②는 앱·DB가 같은 호스트 시계인 CI에서는 `#{changedAt}`를 `date_trunc('second', now())`로 되돌려도(밀리초가 남는 `now()` 단독은 초 절삭인 iat 와 어긋나 오히려 빨간불) 전부 초록임 | 없음 - 잴 수 없는 것을 잰 척하지 않음 | **둘 다 되돌려도 CI가 초록임** - 다음 사람이 "테스트가 없으니 필요 없는 코드"로 읽고 지울 수 있음. 특히 ②는 단일 호스트에서는 증상이 아예 나타나지 않아 배포를 분리한 뒤에야 드러남. 또 **가입 경로의 기본값은 여전히 DB 시계**라 "한 시계로 통일"은 비밀번호 변경 경로 한정임 |
| 2026-07-28 | **`backend` 잡이 10분 51초 만에 실패 - `ChatFlowTest.attachments_survive_reload_with_fresh_url` 이 `POST /api/chat` 에서 600초간 응답 0바이트 후 SSE 타임아웃(P-5)에 걸림.** 단언 실패가 아니라 **행(hang)** 이며, **ryuk 이미지 pull 은 1.9초에 정상**이라 기존 플레이크 갈래가 아님. 문서 3개만 바뀐 PR 이고 백엔드는 직전 초록 run 과 동일 코드였음 | 환경·플랜 제약(**원인 미확정**) | 사용자 결정으로 **실패 잡만 1회 재실행 → 45초 만에 초록**(run 30348734609). 코드·설정 변경 0건이며 같은 기준을 그대로 다시 통과시킴 | 없음 - 기준을 낮추지 않음 | **원인을 밝힌 것이 아님.** 600초 무응답은 클라이언트 스택만 남기고 **서버가 왜 안 썼는지는 로그에 없음** - 확정하려면 스레드 덤프나 `chatExecutor` 큐 길이 로깅이 필요함. 유력 지점은 `ChatExecutorConfig` 의 `newFixedThreadPool(8)` + **무제한 큐**이며 이는 열려 있는 🔴 미결 「채팅 스레드 풀 back-pressure」가 지목하는 바로 그 코드임 - **그 미결이 닫히기 전까지 같은 행이 또 날 수 있고 대체 방어는 없음**. 또 **기존 판별 규칙(「`Retrying pull for image` 가 없으면 단언 실패이므로 재실행 대상 아님」)이 이 형태를 가르지 못함** - 행은 제3의 갈래라 그 규칙만 따르면 재실행을 금지하게 됨. 2회째가 나오면 플레이크가 아니라 결함으로 다루고 back-pressure 미결을 먼저 닫을 것(S-2 : 같은 실패 2회면 승격) |
| 2026-07-28 | **S-6 적용 1차가 문서 머리만 훑고 섹션 머리글을 빠뜨렸음** - 파일 머리의 자기소개는 걷었으나 표별 머리글에 배경이 그대로 남았고, **리스크 표 머리에는 정비 경위 문단을 새로 넣기까지 했음**(처리 이력과 중복). 사용자 지적으로 드러남 | 구현이 기준에 못 미침 | 섹션 머리글 전수(31개)를 훑어 배경 7건 제거 - 리스크 표 경위 문단 · CI 게이트·AC 표·스파이크 표의 `가이드 N단계 산출물` 자기소개 · 미결·운영 절의 건수 복제 2줄 · 산출물 지도 상태 칸 경위. 부수로 `처리 이력 표 5행` 행 번호 참조 1건을 없앰(같은 문서가 금지한 형태) | 없음 - 빠뜨린 적용을 마저 함 | **배경인지 지침인지는 기계가 못 가름** - 참조 실재·표 열 수 검사는 문장 성격을 안 봄. 이번에도 사람 지적이 아니었으면 남았을 것이고, **다음에 문서를 늘릴 때 같은 형태가 또 들어올 수 있음**. 「무엇을 하라」가 아닌 문장은 지우는 것이 유일한 기준이며 그 판단은 매번 사람이 해야 함 |
| 2026-07-28 | **문서 참조 실재 검사가 로컬에서 거짓 초록** - 위 프론트 갈래 주의를 적으며 `frontend/vitest-report.json` 을 백틱 경로로 문서에 썼는데, 이 파일은 `.gitignore` 대상이라 클린 체크아웃에 없음. **로컬에는 방금 만들어 둔 실물이 있어 통과했고 원격이 MISSING 으로 잡음** | 기준 자체가 틀렸음(로컬과 원격이 다른 것을 봄) | `.linkignore` 에 사유와 함께 예외 등재. 커밋되면 STALE 로 빨간불이 되므로 방치되지 않음(예외 판정은 작업 트리가 아니라 `git ls-files` 기준이라 로컬에 파일이 있어도 STALE 이 아님) | 없음 - 예외를 규약대로 등재함 | **참조 실재 검사는 작업 트리(`-e`)를 봄** - `.gitignore` 대상 경로를 문서에 적으면 **적은 사람 로컬에서는 늘 통과하고 원격에서만 터짐**. 예외에 넣기 전까지는 이 비대칭이 매번 반복되며, 게이트가 잡아 주긴 하나 **한 바퀴를 원격까지 돌아야 알 수 있음** |
| 2026-07-28 | **케이스 수 하한 게이트의 로컬 구멍 - 프론트 갈래** : `check-case-floor.sh` 는 `frontend/vitest-report.json` 을 **읽기만 하고 만들지 않음**. 그냥 `vitest run` 을 돌리면 리포트가 갱신되지 않아 이전 실행의 수가 남음. 스크립트 머리의 「clean 후 측정」 주의는 백엔드 surefire 에만 적혀 있었음 | 기준 자체가 틀렸음(게이트가 낡은 값을 볼 수 있음) | 스크립트 머리에 프론트 갈래 주의와 측정 명령을 적음. 이번엔 낡은 값이 **작아서**(26 < 29) 빨간불로 드러났고, 리포트 재생성 후 29로 통과 | 없음 - 사각지대를 적었을 뿐 기준은 그대로 | **반대 방향이 진짜 위험함** - 테스트를 지운 뒤 리포트를 다시 만들지 않으면 낡은 **큰** 수가 남아 게이트가 통과함. 곧 이 게이트가 막으려던 「테스트를 지워도 초록」이 로컬에서 그대로 성립함. **주의 문구는 사람이 읽어야 작동하므로 방어가 아님** - 스크립트가 리포트를 직접 생성하거나 신선도(파일 시각)를 단언하지 않는 한 남아 있음. 원격 CI는 클린 체크아웃이라 해당 없음 |
| 2026-07-28 | **가이드 v3.9 `S-6. 문서 작성 규칙`을 5개 문서가 한 건도 지키지 않고 있었음** - 배경 설명·자기소개가 전 문서 머리에 있었고, 케이스 하한이 4곳에 흩어졌으며(스스로 「어긋난 적 있음」이라 적어 둠), 상태 마커는 0건이라 대기 항목과 종결 항목이 같은 회색 표에 섞여 있었음 | 기준 자체가 없었음 - 가이드 v3.9 신설 요건(볼트 변경 이력에 3.9 행이 없어 전문 대조로 확인) | 전 문서에 S-6 적용 - 배경 제거 · 진행 로그를 `결정 + 근거 + 정본 포인터`로 압축 · **케이스 하한 숫자를 문서에서 제거하고 정본을 `scripts/case-floors.env` 하나로** · 마커 5색 도입(범례·상태 정본은 이 문서) · 「일어나지 않은 일」 1행 삭제 | 없음 - 기준을 되레 올림 | **압축이 사실을 지웠는지는 기계가 재지 않음.** 진행 로그를 줄이면서 세부를 리뷰 라운드 표·이 표로 넘겼는데, **포인터가 끊겨도 게이트는 초록임**(참조 실재 검사는 파일 경로만 보고 표 안의 항목명 참조는 안 봄). 또 **마커와 상태 열의 정합은 사람이 대조해야 함** - 어긋나도 검사가 없음 |
| 2026-07-28 | **인증 요청마다 사용자 행을 1회 더 읽게 됨** - 비밀번호 변경 시각 대조 때문 | 감수한 비용(구조 선택) | stateless JWT라 서버가 토큰을 회수할 수 없어, 대조 없이는 "비밀번호를 바꿔도 기존 토큰이 만료까지 유효"가 유지됨. 조회 1회를 감수하고 무효화를 얻음 | 없음 - 보안 기준을 올리고 비용을 적음 | **단일 인스턴스 전제(R-6)에서만 비용이 작음.** 인스턴스·트래픽이 늘면 이 조회가 병목이 될 수 있고, 그때는 캐시나 토큰 버전 클레임으로 바꿔야 함. 또 **변경과 같은 초에 발급된 직전 토큰 1개는 살아남음**(초 눈금 비교) |

---

## 참고

- 미결 항목의 런칭 시점 실행 목록 : `docs/04_런칭.md`
- 단계별 판정 원장 : `docs/00_계획.md` 게이트 체크 표 · 진행 로그
- **가드 등록표 · 깨뜨릴 수 없는 가드 표는 행이 0이라 만들지 않음** - 사유는 `docs/00_계획.md` 산출물 파일 지도에 적음(가이드 : 행 0인 표는 만들지 않되 사유를 지도에 남김)
