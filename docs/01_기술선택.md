# 01_기술선택 - rag-chatbot

> 1단계 산출물. 채택안이 아니라 **탈락안의 무효화 사유**를 남기는 것이 목적. 모든 버전은 공식 문서로 확인(확인일 2026-07-24). 사용자 승인 완료.

## 채택 표

| 영역 | 채택 | 버전 | 확인한 공식 문서 URL | 확인일 |
|------|------|------|----------------------|--------|
| 백엔드 프레임워크 | Spring Boot | 3.5.16 (**OSS EOL 감수** - 미결 표에 업그레이드 재검토 등재) | https://github.com/spring-projects/spring-boot/releases · https://docs.spring.io/spring-boot/3.5/system-requirements.html | 2026-07-24 |
| 백엔드 언어 | Java(JDK) | 17.0.19 LTS (로컬 확인) | Spring Boot 3.5 system-requirements(Java17 최소) | 2026-07-24 |
| 빌드 | Maven Wrapper(`mvnw`) | 스캐폴드 시 wrapper가 Maven 배포판 고정(전역 mvn 미설치) | https://maven.apache.org/wrapper/ | 2026-07-24 |
| 퍼시스턴스 | MyBatis(mybatis-spring-boot-starter) | 3.0.5 (SB3 대상 트랙) | https://github.com/mybatis/spring-boot-starter/releases | 2026-07-24 |
| DB | PostgreSQL + JDBC 드라이버 | 드라이버 42.7.13 | https://jdbc.postgresql.org/download/ | 2026-07-24 |
| DB 마이그레이션 | Flyway(Spring Boot 통합, 기동 시 자동 적용) | Boot 3.5.16 BOM 관리 | https://docs.spring.io/spring-boot/reference/data/sql.html#data.sql.migration | 2026-07-24 |
| 인증 | Spring Security + JWT(java-jwt) + BCrypt | Security 6.5.11(SB3.5.16 BOM) · java-jwt 4.6.0 | https://docs.spring.io/spring-security/reference/features/authentication/password-storage.html · https://github.com/auth0/java-jwt | 2026-07-24 |
| OpenAI 연동 | Spring RestClient 직접 호출 | Spring Boot 동봉 | https://developers.openai.com/api/reference/resources/vector_stores | 2026-07-24 |
| 백엔드 테스트 | JUnit Jupiter + Testcontainers(PostgreSQL) | 5.12.2 · 1.21.4 (SB3.5.16 BOM) | https://github.com/junit-team/junit5/releases · https://testcontainers.com/modules/postgresql/ | 2026-07-24 |
| 프론트 프레임워크 | React | 19.2.8 | https://react.dev/versions | 2026-07-24 |
| 프론트 빌드 | Vite | 8.1.5 (Node 22 지원) | https://vite.dev/guide/ | 2026-07-24 |
| 프론트 언어 | TypeScript | 7.0.2 (스파이크 통과 조건, 폴백 6.0.3) | https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/ | 2026-07-24 |
| 라우팅 | React Router | 8.3.0 | https://reactrouter.com/changelog | 2026-07-24 |
| 서버 상태/데이터 패칭 | TanStack Query | 5.101.4 | https://tanstack.com/query/latest | 2026-07-24 |
| 스타일 | Tailwind CSS + `@tailwindcss/vite` | 4.3.3 | https://tailwindcss.com/docs/installation/using-vite | 2026-07-24 |
| 프론트 테스트 | Vitest + React Testing Library | 4.1.10 · 16.3.2 | https://vitest.dev/guide/ | 2026-07-24 |
| SSE 수신 | fetch + ReadableStream(또는 @microsoft/fetch-event-source) | 브라우저 API | https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events | 2026-07-24 |
| CI | GitHub Actions | - | https://docs.github.com/actions | 2026-07-24 |
| 진행 단계(R-11) 원천 | OpenAI Responses API 스트리밍 이벤트 → 자체 SSE `stage` 이벤트로 재발행. 단계를 만드는 원천은 `response.file_search_call.in_progress` · `response.file_search_call.searching` · 첫 `response.output_text.delta` 3종이고(`response.created`·`response.in_progress`는 단계를 만들지 않는 도달 확인 신호로만 둠 - 요청 업로드·TTFB 구간 공백과 목업 비대칭을 피하려고 `analyzing`은 서버 경계에서 발생시킴), **단계 ↔ 이벤트 대응의 단일 출처는 `docs/00_계획.md`「진행 단계(R-11) 이벤트 매핑」표임**(여기에는 채택 근거만 남김) | 버전 표기 없음 - Responses API 현행(이벤트명만 문서로 확인, 발생 순서는 미확인) | https://developers.openai.com/api/reference/resources/responses/streaming-events | 2026-07-27 |

## 탈락 표

| 후보 | 무효화 사유 | 전제가 바뀌어도 성립? |
|------|-------------|------------------------|
| Spring Boot 4.1.0 | MyBatis SB4 스타터(4.1.0)가 2026-07-16 릴리스로 안정성 검증이 얕고, 참조 구현과 라인이 달라 초기 리스크가 큼. EOL 보안 공백은 목업/개발 단계라 트레이드오프로 수용 | **아니오(시간 의존)** - 성숙하면 사유 소멸. 그래서 두 안을 사용자에게 제시해 3.5.16을 택했고, **"SB4 업그레이드 재검토"를 미결 표에 등재**함 |
| MySQL / MariaDB | uuid 네이티브 타입 미지원(CHAR(36)/BINARY(16) 우회) + 참조 구현 Postgres 스키마와 불일치로 이식 비용 발생 | 예 - uuid/jsonb 활용과 참조 정합성은 오래가는 기준 |
| 공식 openai-java SDK 4.45.0 | Vector Stores API가 아직 `OpenAI-Beta` 헤더를 요구(완전 GA 미확인)하고, SDK 마이너 업데이트마다 시그니처 변화 위험. 참조 검증 패턴(RestClient) 미활용 | **아니오(부분/시간 의존)** - SDK가 GA로 안정화되면 유리. **2차 폴백으로 유지**하고 미결 표에 전환 트리거 등재 |
| 서버 세션(쿠키) | SSE `fetch`·모바일·수평 확장에서 stateless JWT가 유리. 상태 저장소 불필요 | 예 - stateless 요구는 오래감 |
| EventSource(SSE 표준 API) | Authorization 헤더·POST 불가라 JWT 인증과 구조적으로 불일치 | 예 - 인증 헤더 필요성은 오래감 |
| Tailwind CDN 방식 | 실앱 번들의 공식 권장 경로가 아님(플러그인 방식). CDN은 Supernova 디자인 시안 산출용 | 예 - 빌드 파이프라인 표준은 오래감 |
| 진행 단계를 프론트 타이머로 연출(1초마다 다음 라벨) | 실제 동작과 무관한 연출임 - 검색을 안 한 응답에도 "검색 중"이 보여 P-10·D-4를 정면으로 위반함. 백엔드가 이미 실 이벤트를 파싱하고 있어 대체 근거도 없음 | 예 - 표시가 실제를 반영해야 한다는 요구는 오래감 |
| 목업에서 라이브와 같은 "참조 문서 검색 중" 라벨 사용 | 목업의 코퍼스 매칭은 사용자 문자열 키워드 비교이지 문서 검색이 아니고(`OpenAiMockService` `CORPUS_KEYWORDS`), 화면에 목업 모드 표시가 없어 시연 관람자가 실 검색으로 오인함(S-1 위장 금지 · D-4). 사용자 결정으로 **목업 전용 문구**를 채택함(2026-07-27) | 예 - 목 데이터를 실물로 보이게 하지 않는다는 요구는 오래감 |
| 진행 단계를 별도 REST 폴링으로 조회 | 같은 턴에 이미 SSE 스트림이 열려 있어 추가 왕복·서버측 진행 상태 저장이 순증 비용임(휘발 표시에는 과함) | 예 - 스트림이 있으면 같은 스트림에 싣는 것이 표준 |

> **탈락 사유가 "아니오(시간 의존)"인 2건**(SB4.1.0 · 공식 SDK)은 가이드 통과 조건상 그대로 두면 안 되므로, **미결 표에 "재검토 트리거"로 등재**해 처리함. 두 안 모두 사용자에게 제시 후 현 채택안을 승인받음.

## 스파이크 표

> 3단계 플랜 리뷰(별도 critic 패스, 2026-07-24) 결과 아래 스파이크가 추가됨. 리뷰 판정·처리는 문서 하단 「플랜 리뷰 기록」 참조.

| 항목 | 왜 리뷰로 못 닫는가 | 스파이크 범위 | 결과 | 플랜 반영(Phase · AC) |
|------|---------------------|---------------|------|------------------------|
| TypeScript 7.0 채택 가능 여부 | 7.0은 프로그래매틱 API가 없고(7.1 예정) `typescript-eslint` 등 툴이 이에 의존 - 실제 스캐폴드로 lint가 도는지 만들어 봐야 앎 | Phase 0에서 Vite+React+TS 최소 스캐폴드로 `npm run lint`·`tsc`·build 동작 확인 | **완료(2026-07-24)** : 공식 Vite react-ts 템플릿이 **TS ~6.0.2 + oxlint(러스트, TS API 비의존)** 를 기본 제공 → `typescript-eslint` 의존이 없어 **R-8 리스크 소멸**. lint/test/build 전부 통과. **TS7 미채택, 템플릿 기본 TS 6.0.2 + oxlint 채택**(플랜의 6.0.3 폴백 취지). React 19.2.7·Vite 8.1.5 확인 | Phase 0 완료 판정 충족 |
| RestClient의 OpenAI SSE 증분 수신(M3) | 동기형 RestClient로 `text/event-stream` 토큰 스트리밍이 자명하지 않음. 틀리면 OpenAI 연동 채택을 되돌림. 참조 구현은 RestClient 응답 InputStream을 `BufferedReader`로 수동 파싱함(조사로 확인) - 우리 설정에서 재확인 필요 | Phase 4/5 착수 전, `OpenAiService` 경계 뒤에서 스트리밍 응답 수동 파싱 실증. 불가 시 WebClient/InputStream 대체 | (Phase 4/5) | Phase 5, R-9 |
| `OpenAiService` 인터페이스 시그니처(M1) | Mock↔Real이 첨부·Vector Store 수명(create/upload/attach/delete)·citation 반환을 표현하려면 시그니처를 먼저 확정해야 함 | Phase 4 착수 게이트로 시그니처 + 호출을 기록하는 Mock 확정 | (Phase 4) | Phase 4, AC-12·AC-14 |
| MyBatis + PostgreSQL uuid 왕복(M7) | insert→select 시 TypeHandler/`jdbcType` 마찰이 매퍼 전반에 반복 | Phase 1에서 uuid PK insert 후 select 왕복 1건 검증 | (Phase 1) | Phase 1, AC-20 |
| stateless JWT 하 파일/이미지 서빙 인증(M6) | `<img src>`가 Bearer를 못 실어 서명 경로 토큰 vs blob-fetch 결정을 만들어 봐야 앎 | Phase 3에서 서명 경로 토큰(짧은 만료) 방식 실증, 로컬 디스크 기준 | (Phase 3) | Phase 3, AC-22·R-10 |
| 실 Vector Store/Responses 응답 스키마(M2) | `annotations.file_citation` 구조 + file_search "무자료" 판별법을 실 응답 없이 확정 불가. **진행 단계(R-11)도 이벤트명은 문서로 확인됐지만 발생 순서·무검색 시 미발생은 실 응답에서만 확인됨** | 실 API KEY 확보 시(S-3 비용 대기) 실행 - annotations 구조·무자료 임계 확인 + `file_search_call.*` 발생 순서/유무 확인(이관-6) | (미결 - 실키 대기) | Phase 4, AC-6·AC-7 / Phase 7, AC-24 |

## 플랜 리뷰 기록 (3단계)

> 이 블록의 `R-n`은 2026-07-24 당시 **리스크** 번호이고, 기록 보존을 위해 계열 접두 없이 그대로 둠. 2026-07-27에 요구 계열과 값이 겹친 `R-11` 한 곳만 `리스크 R-11`로 보정함.

- 리뷰 담당 : **critic 서브에이전트(별도 패스)** - 작성 세션과 분리(S-1 자기 승인 금지 충족)
- 리뷰일 : 2026-07-24
- 판정 : **REVISE** (착수 막는 치명 결함 0건, 결정 변경 소견 다수)
- 지적 계수 : **결정 변경 10건**(M1~M9 + m4) · **표현 다듬기 4건**(m1~m3·m5) · **스파이크 추가 5건**
- 처리 결과(라운드 1, 전건 반영) :
  o M1 `OpenAiService` 시그니처 → Phase 4 착수 게이트 + 스파이크 등재
  o M2 실 응답 스키마 → 실키 대기 스파이크(미결 등재)
  o M3 RestClient SSE 증분 → 스파이크 등재(참조가 RestClient+수동 파싱 사용 확인이 완화 근거)
  o M4 무자료 판정 → Phase 4 산출물에 규칙 + Mock 무자료 트리거, AC-6 보강
  o M5 문서 RAG 수명주기 → Phase 4 수명주기 호출 + 리스크 R-11 + AC-12
  o M6 이미지 서빙 인증 → Phase 3 서명 경로 토큰, AC-22 + R-10 + 스파이크
  o M7 uuid 왕복 → Phase 1 스파이크, AC-20 보강
  o M8 Phase 6 과대 → 6a~6d PR 분해
  o M9 R-6·R-9 테스트명 → R-9 `OpenAiServiceBoundaryTest`, R-6는 미결 이관, R-10~12 신설
  o m4 이미지 단독 전송 → AC-21
  o m1 이관-1 문구 · m2 운영 프로필(Phase 0) · m3 JWT 시크릿(`.env.example`) · m5 SB EOL 런칭 전 재검토(미결) 반영
- 재리뷰(라운드 2) : **APPROVE** - 14건 전건 닫힘, 새 결정 변경 0건. 잔여 문구 3건(스파이크 라벨·AC-22 404·AC-21 검증 위치) 정리 완료. 참조 구현이 실제 `RestClient`+`BufferedReader` 수동 파싱을 씀이 코드로 확정(M3 완화·탈락 사유 사실 검증)
- 라운드별 결정 변경 카운트 : 라운드1=10 → 라운드2=0. 잔여는 순수 문구라 실질 통과. 잔여 스파이크는 착수 시점(Phase 0~5) 게이트로 처리됨

## 확인일 · 승인

- 버전 확인일 : 2026-07-24 (공식 문서/릴리스 직접 조회) · 진행 단계 이벤트명 확인일 2026-07-27
- 사용자 승인(스택) : Spring Boot 3.5.16 · PostgreSQL · JWT(java-jwt+BCrypt) · OpenAI RestClient 직접 호출 (2026-07-24)
- 사용자 승인(R-11 표시 방식) : 한 줄 교체형(첫 토큰 시 소멸) · 진행 단계 이력 비저장(휘발, 스키마 변경 0) (2026-07-27)
- 사용자 승인(R-11 목업 라벨) : 목업은 라이브와 다른 전용 문구(예 : "목업 코퍼스 조회 중")를 써 목 데이터임을 화면에 드러냄 - 배지 컴포넌트 추가 없이 라벨 문자열만 모드별로 분기 (2026-07-27)
- 플랜 승인(Phase·완료 판정) : 대기
