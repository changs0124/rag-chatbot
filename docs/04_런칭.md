# 04_런칭 - rag-chatbot

> 8단계 런칭 표를 **2단계에서 미리 만들어 둠**(가이드 지시). 8단계에서 만들면 이미 늦음 - 한 곳만 빠져도 조용히 실패함. 각 항목은 실행 전 되돌리는 방법·소요·비용을 채움. 시크릿 값은 여기 적지 않고 보관 위치만 적음(`docs/02_운영.md` 접근 권한 표).

## 런칭 표

| 손댈 지점 | 담당 | 상태 | 확인 방법 | 되돌리는 방법 | 되돌림 소요 | 월 비용 |
|-----------|------|------|-----------|---------------|-------------|---------|
| 실 OpenAI API KEY 발급·등록 | changs0124 | 대기 | live 응답이 실제로 나옴 · M2 스파이크 실행(출처 스키마 + 진행 단계 원천 이벤트·체감 구간, 이관-6·7) | **폐기·재발급**(회수 불가 부류 - 되돌림 아님) | 즉시 | 사용량 기반 |
| 실 Vector Store 구축 + 전처리 데이터 업로드 | changs0124 | 대기 | `file_search` 응답에 실제 출처 표시 | Store 삭제 | 즉시 | 저장/호출 기반 |
| 배포 대상 선정(프론트 호스팅 + API 서버) | changs0124 | 대기 | 실 URL로 전 흐름 동작 | 배포 롤백(리비전 되돌림) | 즉시~수분 | 호스팅 요금 |
| HTTPS 도메인 연결(카메라 getUserMedia 요건) | changs0124 | 대기 | 모바일 실기기 카메라 촬영 동작(AC-19) | DNS 해제 | DNS TTL(수분~수시간) | 도메인 요금 |
| 대외 공개 승인 | changs0124 | 대기 | 외부에서 접근 가능 | 비공개 전환 | 즉시(색인은 비대칭) | - |
| 모니터링·에러 로깅 연결 | changs0124 | 대기 | 대시보드에 이벤트 수신 | 연동 해제 | 즉시 | 도구별 |

> [!warning] 되돌림 비용이 비대칭인 조작
> API KEY·Store 데이터는 되돌림이 아니라 **폐기·로테이션** 절차가 필요함(이미 나간 것은 회수 불가). 색인 차단 해제·실도메인 연결은 켜는 것은 즉시지만 끄는 것은 DNS TTL·크롤러 재색인 때문에 며칠 걸릴 수 있음.

## 미결과 연결

- 배포 대상·HTTPS 도메인·실 API/Store는 전부 `docs/00_계획.md` 미결 표 항목임. 런칭 시점에 결정·실행함
- 이 표의 항목은 8단계에서 실물로 확인하고 상태를 갱신함
