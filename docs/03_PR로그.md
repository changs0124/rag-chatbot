# 03_PR로그 - rag-chatbot

> PR 단위와 그 PR이 **단독으로** CI를 통과했다는 근거(run 번호)를 남김. 커밋만 있고 PR이 없는 구간은 그대로 적음(S-1).

## PR 표

> 당시 재지 않은 항목은 `측정 불가 - 사유`로 적음(사후에 지어내지 않음, S-1).

| 행 | PR | 플랜 항목 | AC | 역방향 확인 | 문서 동반 수정 | Phase 크기 판정 | CI 근거 | 상태 | 일자 |
|----|----|-----------|-----|-------------|----------------|------------------|---------|------|------|
| 1 | 없음(직접 push) | Phase 0~6 구현 전체 · 7단계 재리뷰 반영 · 실 OpenAI 연동 구조 | AC-1~7·9~18·20~22 | **부분** - 4단계 문서 참조 게이트는 깨뜨려 확인. Phase 1~6 테스트의 개별 역방향 확인은 기록 없음(당시 절차 미도입) | 예 - 진행 로그·게이트 체크를 같은 커밋에서 갱신 | **측정 불가 - PR 단위 검사가 아니었음**(커밋별 push 트리거로만 초록. `docs/02_운영.md` 처리 이력 「PR 절차 미적용」 행) | push 트리거 CI run 30056454074 ~ 30065976032 전부 success | 통과 - PR 절차는 미적용 | 2026-07-24 |
| 2 | 없음(직접 push) | Phase 7 진행 단계 표시(R-11) 구현 + 문서 | AC-23·24 | **예 - 3회** : 목업 `searching` 제거 → `ChatStageHonestyTest` 3건 실패 · 프론트 라벨을 고정 문구로 → 단계 테스트 1건 실패 · 검색 근거 없이 `response.created`에 검색 단계 부여 → `OpenAiRealStageMappingTest` 1건 실패(전부 원복 후 재초록) | 예 - Phase 표·AC-23·24·실측·이벤트 매핑 동반 갱신 | **단독 통과** - 이 커밋만으로 3잡 초록 | run 30230499675 3잡 success | 통과 - PR 절차는 미적용 | 2026-07-27 |
| 3 | 없음(직접 push) | 목업 단계 최소 표시 시간(Phase 7 보완) | AC-23 | **예** - 최소 표시 큐 제거 → 훅 테스트 2건 실패 후 원복 | 예 - P-10·AC-23·실측 표·탈락 표 동반 갱신 | 해당 없음 - Phase 7 보완분 | run 30231123362 3잡 success | 통과 - PR 절차는 미적용 | 2026-07-27 |
| 4 | #1 | 문서 - 실 키 확보 후 체크리스트(9항목) + PR 로그 신설 | 해당 없음 - 문서 전용 | 해당 없음 - 새 테스트 0건 | 예 - 문서 전용 PR | 해당 없음 - 문서 PR | run 30231409938 3잡 success(PR 단위 검사) | 통과 - squash 머지 `1feb854`, 브랜치 삭제 | 2026-07-27 |
| 5 | #2 | 문서 - PR 표 #1행 근거 기입 + 기입 규칙 | 해당 없음 - 문서 전용 | 해당 없음 - 새 테스트 0건 | 예 - 문서 전용 PR | 해당 없음 - 문서 PR | PR #2 체크 3잡 pass(머지 전 확인) | 통과 | 2026-07-27 |
| 6 | #3 | 가이드 v3.8 적합성 정비 : `docs/02_운영.md` 신설 · 1-A 소급 · **CI 게이트 4종 신설** · 문서 참조 게이트 재작성 · 의존성 취약점 2건 수정 | 해당 없음 - 새 AC 0건(기존 AC 회귀만 확인) | **예 - 8회** : 케이스 하한(백 42/999) · 계획서 표 3형태(빈 칸 · 헤더 누락 · STALE) · 문서 참조 3형태(MISSING · STALE · WIKILINK) · gitleaks 픽스처 커밋 → **전부 EXIT=1 확인 후 원복 EXIT=0**. 의존성 2종은 **실제 취약점 검출(EXIT=1) → 수정 후 0건(EXIT=0)** 이 곧 역방향 근거임 | 예 - `00_계획`·`01_기술선택`·`02_운영`·`03_PR로그`·`04_런칭`을 같은 작업에서 수정 | 해당 없음 - Phase 산출이 아님(게이트·문서 정비) | **PR 단위 검사** : **PR #3 체크 6잡 pass**(머지 전 확인 - 기입 규칙에 따라 자기 머지 결과는 적지 않음). 중간 근거 run 30321437472 · 최종 리뷰 반영분 run 30323175290 모두 6잡 success(backend 51s · frontend 24s · docs 7s · secrets 9s · deps-backend 20s · deps-frontend 12s). **실패 2건** : run 30321076637의 `deps(백엔드)` 잡이 `Unable to resolve action aquasecurity/trivy-action@0.36.0`(태그 `v` 접두 누락)로 2초 만에 실패 → `@v0.36.0`으로 수정 · run 30321308959의 `docs` 잡이 브랜치명을 백틱 경로로 적은 탓에 MISSING → 표기 수정. 로컬 근거는 `mvnw -B verify` EXIT=0(42건) · `oxlint` 0 · `vitest` 16/16 · `vite build` 성공 · `npm audit` 0 · Trivy 0 · gitleaks 0 | **통과 - squash 머지 `23dd2de`** | 2026-07-28 |
| 7 | #4 | 문서 - CI 플레이크(러너 이미지 내려받기 실패) 처리 이력 등재 + 백엔드 잡의 네트워크 의존 명시 | 해당 없음 - 문서 전용 | 해당 없음 - 새 테스트 0건 | 예 - 문서 전용 PR | 해당 없음 - 문서 PR | **PR #4 체크 6잡 pass**(머지 전 확인 - 기입 규칙) | 통과 | 2026-07-28 |
| 8 | #5 | 7단계 Phase별 리뷰(Phase 0~6) 실시 + 지적 1차 반영 | AC-2 · AC-18 · AC-20 보강 / AC-9·11·12·14·17 판정 정정 | **예 - 3회** : `verify`→`decode` 치환 → AC-2 신규 3건 실패 · `@Profile("prod")`→`"production"` → 컨텍스트 3건 실패 · `create index` 제거 → 인덱스 단언 실패(전부 원복 후 재초록) | 예 - `00_계획`·`01_기술선택`·`02_운영`을 같은 작업에서 수정 | 해당 없음 - Phase 산출이 아님(리뷰·테스트 보강) | **PR #5 체크 6잡 pass**(머지 전 확인 - 기입 규칙) | 통과 | 2026-07-28 |
| 9 | #6 | 라이브 정합성 4건(무자료 표기 · 문서 첨부 거부 · 삭제 트랜잭션 경계 · 재조회 첨부) | AC-6 표기 방식 정정 · AC-11 문서 갈래 소멸 · AC-7 재조회 첨부 | **예 - 2회** : `application/pdf`를 허용 목록에 되돌림 → 거부 테스트 실패 · 재조회 응답에서 첨부를 다시 뺌 → 재조회 테스트 실패(둘 다 원복 후 재초록) | 예 - `00_계획`(R-2·R-4·P-8·AC-6·범위 변경 이력) · `02_운영`(미결 4건 종결) 동반 수정 | 해당 없음 - Phase 산출이 아님(리뷰 지적 처리) | **PR #6 체크 6잡 pass**(머지 전 확인 - 기입 규칙) | 통과 | 2026-07-28 |
| 10 | #7 | 대기 결정 4건(AC-18 모드 필수화 · 중단 상태 정책 · AC-17 문안 · 보안·정합성 5건) | AC-9 · AC-17 · AC-18 문안 정정 후 전부 통과로 재판정 | **예 - 10회** : 열 군데를 한꺼번에 되돌려 실패 목록이 의도한 테스트와 1:1로 맞는지 확인함(모드 기본값 복원 → 미설정 기동 테스트 · 중단 저장을 `error`로 · 빈 텍스트 저장 생략 제거 · 이메일 정규화 무력화 · 로그인 상한 제거 · 토큰 대조 제거 · cutoff 클램프 제거 · `nosniff` 비활성 · 변경 시각 갱신 제거 · 프론트 빈 버블 제거 생략) → **실패 11건**(백엔드 10 · 프론트 1) 후 전건 원복 재초록 | 예 - `00_계획`(AC-9·17·18 · P-2 · R-3 · 범위 변경 이력 3행 · 진행 로그 · 게이트 7·8단계) · `01_기술선택`(가드 이름) · `02_운영`(미결 4건 종결 + 잔여 4건 분리 · 처리 이력 3행) 동반 수정 | 해당 없음 - Phase 산출이 아님(결정 반영) | **PR #7 체크 6잡 pass**(run 30328639691 - 머지 전 확인, 기입 규칙). **실패 1건** : 첫 run 30328463120의 `backend` 잡이 `실측 62 / 하한 68`로 실패 = 개명 전 `ProdModeGuardTest`의 surefire XML이 로컬 `target/`에 남아 실측이 6건 부풀려진 것(`docs/02_운영.md` 처리 이력 「로컬 케이스 수 실측」 행). `clean verify` 재측정 62로 정정 후 초록 | 통과 - 재리뷰 반영분은 11행 | 2026-07-28 |
| 11 | #7(같은 PR) | 별도 레인 재리뷰(코드·문서) 지적 반영 | AC-9·15·16 회귀 수정 - 중단 답변의 거짓 무자료 배너 · 비밀번호 변경 후 세션 유지 | **예 - 6회** : 새 토큰 발급 제거 → `update_password_returns_usable_token` 실패 · 프론트 토큰 교체 제거 → `MyPage.test` 실패 · `stopped` 저장 제거 → `ChatStreamAbortTest` 실패 · 배너 조건에서 `stopped` 제거 → `MessageList.test` 실패 · 훅 `stopped` 제거 → `useChat.test` 실패 · 로그인 성공까지 계수 → `successful_logins_are_not_rate_limited` **와 `login_attempts_are_rate_limited` 두 건 동시 실패**(성공분까지 세면 한도 경계가 한 칸 당겨져 3회째가 이미 429). **실패 7건 = 백엔드 4 · 프론트 3**이며 지점 수(6)보다 하나 많은 이유가 이 한 지점임(전건 원복 후 재초록) | 예 - `00_계획`(CI 게이트 표 하한 · 실측 표 · 착지값) · `01_기술선택`(리뷰 라운드 표 분리 복구 + 재리뷰 2행) · `02_운영`(미결 2행 신설 · 처리 이력 2행) · `04_런칭`(착지값) | 해당 없음 - 리뷰 지적 처리 | **PR #7 체크 6잡 pass**(머지 전 확인 - 기입 규칙). run 번호를 적으면 그 run 의 `headSha` 가 이 줄을 쓴 커밋을 덮지 못해 자기 참조가 됨 | 통과 | 2026-07-28 |
| 12 | #7(같은 PR) | 재리뷰 라운드 2 지적 반영 - 카운터 가시성 · 시계 통일 · 무자료 규칙 문안 | AC-6·R-4·P-8 표시 조건 정정(중단 갈래 추가) | **예 - 2지점** : 기준선을 미래로 밀어 새 토큰이 자기 기준선에 걸리게 함 → `update_password_returns_usable_token` 실패 · 완료 가드를 상수 `false` 로 → `정상 완료 뒤 abort 예외가 나도 stopped 를 세우지 않음` 실패(둘 다 원복 후 재초록). **`volatile`(N-1)과 시계 통일(N-2)은 되돌려도 CI가 초록이라 잠글 테스트가 없음**(단 `now()` 단독이 아니라 초 절삭을 유지한 형태로 되돌렸을 때임) - 사유는 `docs/02_운영.md` 처리 이력 「회귀 테스트가 없는 수정 2건」 | 예 - `00_계획`(P-8·R-4·AC-6 · 범위 변경 이력 1행 · 진행 로그 · 7단계 행 수) · `01_기술선택`(리뷰 라운드 2행) · `02_운영`(처리 이력 1행) · `03_PR로그`(11행 기입 규칙 복귀) | 해당 없음 - 리뷰 지적 처리 | **PR #7 체크 6잡 pass**(머지 전 확인 - 기입 규칙) | 통과 | 2026-07-28 |
| 13 | #7(같은 PR) | 재리뷰 라운드 3 지적 반영 - 기준선 계산 위치 · 주석 정확도 · 빈 버블 가드 | 해당 없음 - 새 AC 0건(표현 지적만) | **예 - 1지점** : 빈 버블 제거의 `complete` 가드를 없앰 → 신규 테스트 `내용이 비어도 onDone 이 확정했으면 버블을 지우지 않음` 실패 후 원복 재초록. 나머지 2건은 주석·계산 순서라 동작 차이가 없어 잠글 테스트가 없음(그대로 적음) | 예 - `00_계획`(진행 로그 · 7단계 판정 · 행 수 18) · `01_기술선택`(리뷰 라운드 2행) · `02_운영`(처리 이력 1행) · `03_PR로그`(12·13행) | 해당 없음 - 리뷰 지적 처리 | **PR #7 체크 6잡 pass**(머지 전 확인 - 기입 규칙) | 통과 | 2026-07-28 |
| 14 | #8 | 문서 - 다음 작업 순서 5항목 체크리스트 신설 | 해당 없음 - 문서 전용 | 해당 없음 - 새 테스트 0건 | 예 - 문서 전용 PR | 해당 없음 - 문서 PR | **PR #8 체크 6잡 pass**(머지 전 확인 - 기입 규칙) | 통과 | 2026-07-28 |
| 15 | #9 | 8단계 전제 정비 - 리스크 표 테스트 이름 8건 부재 해소 + 완화 공백 3건 | 해당 없음 - 새 AC 0건(기존 완화의 검증 범위를 메움) | **예 - 2회** : 용량 상한과 `findByIdAndUser` 의 `user_id` 조건을 함께 제거 → **실패 3건**(신규 3건과 1:1) 후 원복 재초록 · `OpenAiRealService` 조건값을 `live`→`mock` → 경계 2건 빨간불. **후자는 단언 실패가 아니라 컨텍스트 적재 실패**(Real 의 blank-key fail-fast 가 먼저 걸림 - `docs/02_운영.md` 처리 이력 「경계 격리 테스트의 실패 형태」) | 예 - `00_계획`(리스크 표 · 게이트 체크 8단계 · 다음 작업 순서 · 진행 로그 · 실측 · CI 게이트) · `02_운영`(처리 이력 2행) · `scripts/case-floors.env` | 해당 없음 - Phase 산출이 아님(8단계 전제 정비) | **run 30345860901 6잡 success**(backend 47s · frontend 22s · docs 8s · secrets 8s · deps-backend 15s · deps-frontend 12s). 로컬 `mvnw -B clean verify` EXIT=0 69건 · 게이트 4종 EXIT=0 | 통과 - squash 머지 `7c281df`, 브랜치 삭제 | 2026-07-28 |
| 16 | #10 | 문서 - 가이드 v3.9 적합성 정비(S-6 문서 작성 규칙 적용) | 해당 없음 - 문서 전용 | 해당 없음 - 새 테스트 0건 | 예 - 5개 문서 전부 | 해당 없음 - 문서 PR | **run 30347328569 6잡 success**(backend 50s · frontend 26s · docs 6s · secrets 8s · deps-backend 19s · deps-frontend 13s). 로컬 `check-doc-refs`(수집 100 · 검사 96) · `check-plan-tables` EXIT=0 · 마커 없는 상태 셀 0건 | 통과 - squash 머지 `a8820b2`, 브랜치 삭제 | 2026-07-28 |
| 17 | #11 | 대화 전환 시 이전 스트림 중단(미결 `[리뷰]` 1건 종결) | 해당 없음 - 새 AC 0건(중단 정책은 기존 결정 그대로) | **예 - 1지점** : `abort()` 호출 제거 → 신규 3건 실패 후 원복 재초록 | 예 - `00_계획`(진행 로그 · 다음 작업 순서 · 실측 3행) · `02_운영`(미결 1행 종결 · 처리 이력 1행) · `03_PR로그` | 해당 없음 - Phase 산출이 아님(미결 처리) | **run 30348323965 6잡 success**. 첫 run 30348087061 의 `docs` 잡 실패 = `.gitignore` 대상 경로를 문서에 백틱으로 적어 클린 체크아웃에서 MISSING(로컬엔 실물이 있어 통과했음) → `.linkignore` 예외로 수정 | 통과 - squash 머지 `d346dbe` | 2026-07-28 |
| 18 | #12 | 문서 - S-6 규칙 1 잔여 배경 제거(섹션 머리글 31개 전수) | 해당 없음 - 문서 전용 | 해당 없음 - 새 테스트 0건 | 예 - `00_계획`·`01_기술선택`·`02_운영` | 해당 없음 - 문서 PR | **run 30354993411 6잡 success**. 앞선 run 30348734609 의 `backend` 잡이 10분 51초 행으로 실패 → **사용자 결정으로 1회 재실행 후 45초 초록**이며 경위는 `docs/02_운영.md` 처리 이력 「`backend` 잡이 10분 51초 만에 실패」 행 | 통과 - squash 머지 `198df9e` | 2026-07-28 |

## 절차 메모

- **2026-07-27부터 PR 흐름을 적용함** : 작업 브랜치 → PR → CI 3잡 초록 → 머지. 그 이전 구간은 `main` 직접 push였고, CI는 push 트리거로 매 커밋 검사됨(게이트 6 "Phase별 PR이 단독 CI 통과"의 취지는 커밋별 초록으로 충족했으나 **PR 단위 검사는 아니었음**)
- 이미 `main`에 올라간 커밋을 PR로 되돌리려면 히스토리 재작성 + 강제 push가 필요해 하지 않음. 되돌림 비용이 기록 정확성보다 큼
- **기입 규칙** : 자기 행에 자기 머지 결과를 적으려면 PR이 끝없이 이어지므로, **run 번호는 뒤이은 PR이 있을 때만 채우고** 없으면 "PR 체크 3잡 pass(머지 전 확인)"로 적음. 머지 커밋 해시는 git 히스토리가 원장임
- **CI가 검사하는 잡(2026-07-28~ 6잡)** : `backend (test + build + 케이스 수 하한)` · `frontend (lint + test + build + 케이스 수 하한)` · `docs (참조 실재 + 계획서 필수 표)` · `secrets (시크릿 스캔)` · `deps (백엔드 의존성 취약점)` · `deps (프론트 의존성 취약점)`
  o 2026-07-27까지는 3잡이었음(`backend (test + build)` · `frontend (lint + test + build)` · `docs (참조 실재 검사)`). **위 표 1~5행의 "3잡"은 당시 사실이므로 그대로 둠**
